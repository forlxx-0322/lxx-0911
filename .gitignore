# 客户管理系统 · Git 忽略规则
#
# 原则：只提交代码与文档；**本机数据、备份、测试产物一律不提交**，
#       避免把真实客户资料推到公开仓库。

# ---------- 本机运行数据（含真实客户资料，绝不提交）----------
data/
*.db
*.db-wal
*.db-shm

# ---------- 测试产物 ----------
# 仓库根的 .fixtures/ 是各套件跑出来的日志、结果 JSON 与截图，
# 其中 data 快照类文件可能含真实客户资料，绝不提交。
# 注意用 /.fixtures/* 而不是 .fixtures/：后者会连 tools/.fixtures/ 一起忽略。
/.fixtures/*
!/.fixtures/mock-imap.js
/.shots/

# tools/.fixtures/ 放的是**手工维护**的测试夹带（模拟 SMTP 服务器、xlsx 解析器），
# 缺了它们新克隆的仓库跑不了测试，所以逐个放行。
# 其中的自签证书不入库：mock-smtp.js 检测不到会自己生成（openssl 或 Node crypto）。
tools/.fixtures/*
!tools/.fixtures/mock-smtp.js
!tools/.fixtures/xlsx-inspect.js
!tools/.fixtures/quotation-export.js

# ---------- 工具运行目录（非本项目内容）----------
.npm-cache/
.wbhome/
workbuddy-bridge/

# ---------- 编辑器 / 系统 ----------
.idea/
.vscode/
*.swp
Thumbs.db
desktop.ini

# ---------- 日志与临时文件 ----------
*.log
*.tmp
*.bak
