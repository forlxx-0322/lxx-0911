# 变更记录（CHANGELOG）

本文件按**版本号倒序**归档每一次功能改动与缺陷修复。
规则：**每完成一次功能修改或 Bug 修复，就归档一个版本号**（见文末「版本归档规范」）。

> 版本号与代码里的 `APP_VERSION`（`server/server.js`）保持一致；
> 数据库结构版本单独用 `SCHEMA_VERSION`（`server/db.js`）管理，两者独立演进。

---

## [1.16] — 2026-09-14

本次归档：**报价明细列顺序** —— 报价模板里「规格明细」的**每一列都能移动位置**
（内置列与自定义列一视同仁），顺序是**全局一套**：报价单明细、报价模板明细、
导出的 Excel 单据三处一致。数据库结构无变化（仍是 **v7**）。

### 一、需求与做法

**需求**：报价模板的规格明细里，列是固定顺序，用起来不顺手——
比如想把「备注」放到最前、把「数量」挪到压力后面。1.15 只能挪自定义列，
内置列动不了。

**做法：把"列顺序"抽成一条全局设置**

| 存什么 | 存在哪 |
|---|---|
| 列顺序 | 设置项 `quotation_column_order`（JSON 数组，元素是内置列 key 或 `f:<列id>`） |
| 列定义 | `quotation_fields`（1.15 已有，不变） |

- **不动表结构**：顺序属于"界面偏好"，不是业务数据，存设置项即可 ——
  **不需要迁移**，升级后老库直接可用（新库会种子出这条设置，值为空 = 用默认顺序）
- **三处一致**：报价单明细、报价模板明细、导出单据都按这一套顺序排
  （模板不含价格列，所以「单价/折扣/小计」只在报价单与单据里出现）
- **缺列自动补齐**：顺序数组里没有的列（新加的自定义列、将来新版本才有的内置列）
  会自动插回合理位置，**绝不会因为顺序数组不完整而丢列**；
  已删除的列 key 在读的时候被过滤掉
- **新列落位**：新建的自定义列自动接在已有自定义列之后（没有则接在「连接」后），
  随后可用表头上的按钮挪到任意位置

### 二、功能

- **表头上的 ◀ ▶**：报价模板与报价单的明细表，**每一列**（除「#」和「操作」）的表头
  右侧都有左移/右移按钮，鼠标移上去才显形，点一下立即保存；
  已在最前/最后时按钮置灰
- **列管理里也能排**：`⚙ 自定义列` 抽屉现在列出**全部列**（内置列带「内置」标注、
  不能改名/删除但能上下移；仅报价单才有的价格列标注「仅报价单」），
  与表头按钮改的是同一套顺序
- **导出单据跟着排**：单据里的列按全局顺序排；
  「规格型号」是 5 个规格字段合并的一列，取这 5 个字段里最靠前的位置；
  「合计」金额跟着「小计」列走，挪列也不会错位

### 三、影响范围

- 既有数据不受影响；没调过顺序时，表格与单据与 1.16 之前**完全一样**（默认顺序未变）
- 报价单/模板明细表的表头改为按列顺序动态渲染（原先写死的表头结构）
- 新增接口：
  - `GET /api/quotation-fields/order`（读列顺序）
  - `POST /api/quotation-fields/order`（整体保存列顺序）
  - `POST /api/quotation-fields/move`（移动任意一列：`{key, dir}`）
  - 原 `POST /api/quotation-fields/:id/move` 保留，语义改为"在全局列序里前后移动"
- `/api/quotation-fields` 的返回增加 `builtins` / `order` / `columns` 三个字段
- `/api/quotations/:id/export` 的返回增加 `column_order`

### 四、测试

- **新增套件** `tools/test-quotation-column-order.js`：**33 项断言，全部通过**
  - 接口：列顺序读取、内置列左右移动、边界提示、非法入参（404/400）、
    整体保存、漏传补齐、未知/重复键过滤、持久化到设置项
  - **浏览器实测**：报价模板表头点 ◀ 真能把列挪到前面、挪到第一位后按钮置灰、
    服务端顺序同步保存、报价单明细跟着用同一套顺序
  - **导出实测**：真出一份 xlsx 读回，把「小计」挪到最前，单据列序与合计金额都跟着走
  - **列顺序是使用者的真实配置**：套件开跑前先记下原顺序，跑完在 `finally` 里
    原样恢复，并断言恢复成功、设置项里不残留已删列的 key
- **既有套件同步更新**（语义变更必须改测试，且改的是断言口径不是放宽标准）：
  - 报价自定义列套件：`列顺序可调整` 改为断言**全局列序**（原来断言列自己的 sort）；
    `已在最前时给出提示` 改为断言"自定义列可以越过内置列、第一列再往前才有提示"；
    导出断言改为**按列名定位**（自定义列现在可能夹在内置列之间）；
    常用列区断言改为"只列还没添加过的预设"（使用者已把 14 个预设都加过，这里就该是空的）
  - 该套件新增 2 项断言：列管理里内置列带「内置」标注、说明了列顺序

**全量回归：1045 / 1045 全部通过**（39 个套件，较 1.15 的 1009 项新增 36 项：
列顺序套件 33 项 + 自定义列套件新增 3 项）。

> 最后一轮回归是**在库里已有真实客户与 14 个自定义列的情况下跑完的**
> （清库脚本的安全闸门按设计拒绝清空真实数据，于是整套回归直接带着真实数据跑）——
> 所有套件仍然全绿，说明这些套件在"有真实数据"的环境下也是稳的。

**过程中回归抓出的两个真问题**（都不是新功能的 bug，是旧测试太脆）：

1. `报价单全流程` 与 `报价模板全流程` 两个套件在浏览器里**按输入框序号**取单元格
   （`inputs[7]` 当单价用）。使用者的库里已经有 14 个自定义列，它们会插在内置列之间，
   序号全部错位 → 钱填进了别的列，小计与合计算出来是 0。
   已把两处改成**按列名定位单元格**（先读表头，再取对应那一格），
   以后再加多少自定义列都不会错位。
2. 报价自定义列套件里几处断言原本假定"自定义列紧跟在内置列之后"，
   在已有自定义列的环境下不再成立，已改为按列名/按全局列序断言。
3. 另有两处语义变更必须同步的断言（列顺序改为全局、常用列区不再重复提示已添加过的预设），
   改的是**断言口径**，不是放宽标准：新增了「自定义列可以越过内置列」
   「内置列带『内置』标注」「列管理里说明了列顺序」等更强的断言。

补齐后重跑全量回归，39 个套件全绿。

---

## [1.15] — 2026-09-14

本次归档：**报价自定义列** —— 报价单与报价模板的规格列不再固定，
介质、设计压力、设计温度、操作压力、操作温度、环境温度、泄露等级、阀门标准、
执行器型号、定位器、电磁阀、限位开关、过滤减压阀、气控阀……都可以自己加，
**列数不封顶**（上限 60 列），不需要改代码、不需要再迁移。数据库结构 **v6 → v7**。

### 一、需求与做法

**需求**：阀门报价要填的规格项因客户而异。同一张单，炼化客户要「介质 + 设计温度 +
泄露等级」，水务客户可能要「阀门标准 + 执行器型号 + 限位开关」。
固定列永远差一两项，而"将来还要加别的"是必然的——所以要做的是**能自由加列**，
不是再加几个固定列。

**做法：列定义 + JSON 值**

| 存什么 | 存在哪 | 说明 |
|---|---|---|
| **列定义** | 新表 `quotation_fields` | 名称、类型（文本/数字/下拉候选）、单位、候选值、排序、是否启用 |
| **每行的值** | 明细表新增列 `extra`（JSON） | 键 = **列 id**，值 = 文本 |

三个关键取舍（都写进了代码注释与测试）：

1. **不动态加列（不做 ALTER TABLE）**。
   若每个新列都改表结构：一是每加一列就要迁移一次，二是列名一旦被改名，
   数据库里就留下一堆对不上号的列。JSON 值 + 列定义表可以任意增删改。
2. **JSON 的键用「列 id」而不是「列名」**。
   这样**改名不丢值**：「泄露等级」改成「泄漏等级」后，历史报价单里的值
   照样跟着新列名显示（测试里专门断言了这一条）。
3. **列类型不做数值强转**。`number` 只影响录入控件与导出对齐方式，
   免得把「≤80」「PN16」「常温」这类合法规格挡在门外。

**边界处理**：

- **删列不删值**：删掉的列不再显示，但历史单据里的值仍留在 `extra` 里
  （删列时接口会返回 `used_in` / `used_in_templates`，界面提示"已有 N 张报价单填过"）。
  下一次保存该单据时，取不到列定义的孤儿值才会被自然清理。
- **停用不删值**：停用只是界面先不显示这一列，已有值保留。
- **越界值处理**：不存在的列 id 直接丢弃；空值不落库；超长值截断到 200 字（而不是写坏数据）。

### 二、数据库结构 v6 → v7

迁移内容（**纯新增，不回填、不动既有数据**）：

- 新建表 `quotation_fields`（11 列）+ 索引 `idx_quotation_fields_sort`
- `quotation_items` 加列 `extra TEXT NOT NULL DEFAULT '{}'`
- `quotation_template_items` 加列 `extra TEXT NOT NULL DEFAULT '{}'`

`default '{}'` 是刻意的：老库升级后既有明细行的 `extra` 必须是**可解析的空对象**，
不能是 NULL —— 迁移测试里专门断言了「迁移后既有明细行的 extra 都是合法空对象」。

回退方式：删掉两张明细表的 `extra` 列与 `quotation_fields` 表即可退回 v6；
迁移前框架已自动备份（`data/backups/before-migrate-v6-*.db`）。

### 三、功能

- **列管理抽屉**（报价单抽屉与报价模板页右上角「⚙ 自定义列」）：
  常用列一键添加、手工新增任意列名、改类型/单位/候选值、上下移排序、启用停用、删除
- **常用列预设 14 项**：介质、设计压力(MPa)、设计温度(℃)、操作压力(MPa)、操作温度(℃)、
  环境温度(℃)、泄露等级(A~G 级)、阀门标准(GB/API 6D/…)、执行器型号、定位器、电磁阀、
  限位开关、过滤减压阀、气控阀 —— 已添加过的不再重复提示
- **报价单与报价模板共用同一套列**：加一次，两处都有（模板本来就是"完整规格组合"）
- **模板联动**：从报价单沉淀为模板、套用模板，自定义列的值一起走（价格仍然不带）
- **复制新版本**：自定义列的值一并复制
- **行操作**：「复制本行」会把自定义列一起复制（同规格多行时才真的省事）
- **导出 Excel 报价单**：自定义列插在「规格型号」之后、「数量」之前；
  只导出**本单真的填过值**的列（免得一堆空列把单据撑宽）；数字列右对齐；列宽随列数扩展

### 四、上限与校验

| 项 | 限制 | 说明 |
|---|---|---|
| 列数 | 60 列 | 超出会明确报错并说明当前值 |
| 列名 | 20 字 | 括号、斜杠、空格、井号都允许；重名（忽略大小写）拒绝 |
| 单位 | 10 字 | 导出时并进表头，如「设计压力（MPa）」 |
| 单元格值 | 200 字 | 超长截断 |
| 下拉候选 | 60 项 | 支持中英文逗号、分号、换行分隔，自动去重 |

### 五、影响范围

- **既有数据不受影响**：没建过自定义列时，界面与导出的表格与 1.14 完全一致
- 报价单/模板明细多了一列 `extra`（默认 `'{}'`），旧版本读它不受影响
- 导出单据的列数由 8 列变为 `8 + 有值的自定义列数`
- 新增接口 `/api/quotation-fields`（列表 GET、新建 POST、修改 PUT、删除 DELETE、排序 POST …/move）

### 六、测试

- **新增套件** `tools/test-quotation-custom-fields.js`：**49 项断言，全部通过**，
  含接口 CRUD、越界值处理、规模验证（**40 列 × 20 行 = 800 个值逐个比对**）、
  模板联动、改名/停用/删列的数据保留行为、真出一份 xlsx 再读回校验、
  以及浏览器实测（列管理抽屉、表头即时展开、界面填值落库与回显、页面导出函数带上自定义列）
- **迁移套件扩展**：`tools/test-migration-full.js` 增加 v6 → v7 的 6 项断言
  （新表、extra 列与默认值、索引、既有明细行 extra 合法性），26 → **32 项**
- 自定义列是**全局配置**而非测试数据：套件建的列一律带 `【列测试<tag>】` 前缀，
  跑完在 `finally` 里删干净，`tools/cleanup-test-data.js --all` 也会兜底清理带该前缀的列，
  **绝不动使用者自己建的列**

**全量回归：1009 / 1009 全部通过**（38 个套件，较 1.14 的 954 项新增 55 项：
自定义列套件 49 项 + 迁移套件 6 项）。

**过程中修掉的一个真问题**：首次全量回归时 `报价单服务层` 与 `报价模板服务层`
两个套件挂了（`no such table: quotation_fields` / `no column named extra`）。
原因是这两个套件用的是**自己搭的最小库**（只建它用到的那几张表），而新代码会去查
`quotation_fields`。两处都按真实结构补齐了表与列；同时把服务层对"缺表"做了兼容
（`hasFieldTable()` —— 缺表按"没有自定义列"处理，而不是抛错），
这样极端情况下（比如迁移中途被中断）报价单功能也不会整体不可用。
补齐后重跑全量回归，38 个套件全绿。

---

## [1.14] — 2026-09-14

本次归档：**目录与文档整理**，无功能变更，数据库结构仍是 **v6**。
外加修正一处**仓库完整性问题**：手工维护的测试夹带此前没被提交。

### 一、文档归位：根目录 11 份文档收进 `docs/`

**问题**：仓库根目录平铺着 4 份方案 + 7 份阶段验收报告，代码目录被挤在中间；
`后续功能_技术方案_v1.11.md` 这个文件名还停在 v1.11，内容却已经写到 1.13，
文件名与内容版本对不上，容易拿错。

| 原位置 | 新位置 |
|---|---|
| `客户管理系统_实施方案.md` | `docs/方案/客户管理系统_实施方案.md` |
| `后续功能_技术方案_v1.11.md` | `docs/方案/后续功能_技术方案.md`（去掉版本后缀） |
| `新增功能_工业园区标注_技术方案.md` | `docs/方案/新增功能_工业园区标注_技术方案.md` |
| `界面美化说明.md` | `docs/方案/界面美化说明.md` |
| `阶段一 ~ 阶段七验收测试报告.md`（7 份） | `docs/验收报告/` 下同名 |

新增 `docs/README.md` 作为**文档索引**（哪份文档解决什么问题、各自状态、维护约定）。
根目录只保留 `README.md`、`CHANGELOG.md`、三个 `.bat` 启动器与代码目录。
文档之间的交叉引用同步改正（本文件两处、阶段一与阶段六报告各一处）。

### 二、《后续功能技术方案》重写为 v2.0

原文档 483 行里，A1/A3/B1 的实现细节占了三分之二，真正要看的「下一版做什么」
被埋在中间。重写原则：**已完成项压缩成结论表（细节指向本文件对应版本条目），
待实施项按当前代码复核后写成可照单执行的方案**。

主要变化：

- **A2 合同/质保期到期预警提为下一版（1.15）第一件事**，并按 1.14 的实际代码复核：
  - 迁移编号由「v4 → v5」更正为 **v6 → v7**（v5、v6 已被报价单与报价模板占用）
  - 不再是"新起一套"：扩展既有 `services/reminders.js` 与顶栏铃铛面板，
    `/api/reminders/due` 的返回契约保持不变
  - **月份加法明确为「日号截断」**（1 月 31 日 + 1 个月 = 2 月 28/29 日）：
    质保期是商务约定，直接用 SQLite 的 `date(x,'+1 month')` 会溢出到 3 月 3 日，对不上账
  - 明确 A2 **不过滤项目终态阶段**（质保期恰恰发生在项目做完之后）——与跟进提醒的口径相反
  - 改动点落到具体文件，并保留「新字段必须同步进 `pm.js` 列表 SELECT」这条 1.10 踩过的坑
- 新增 **5 个待确认决策点**（预警范围、邮件形态、首页是否加卡片、提前量默认值、未验收项目的口径）
- 排期表刷新：报价模板（已完成）移出，补入回款核销界面、报价单导出排版、
  回款计划「款项性质」字段、「录入客户闪退」待排查等条目

### 三、修正：手工测试夹带没进版本库

**现象**：`.gitignore` 里写的是没有前导斜杠的 `.fixtures/`。它不只忽略仓库根的测试产物目录，
**连 `tools/.fixtures/` 一起忽略了**——而那里放的是手工维护、无法自动生成的东西：

- `mock-smtp.js`（本地模拟 SMTP 服务器，`tools/test-notify-email.js` 依赖）
- `xlsx-inspect.js`、`quotation-export.js`（xlsx 是 zip，必须解压看 `sheet1.xml`）

同一原因的还有仓库根的 `.fixtures/mock-imap.js`（三个招标采集套件依赖它）。

**根因**：gitignore 的模式不带前导斜杠时会匹配**任意层级**的同名目录，
而本意只想忽略仓库根那个产物目录。

**修复**：仓库根改为 `/.fixtures/*` 并单独放行 `/.fixtures/mock-imap.js`；
`tools/.fixtures/` 下逐个放行三个手工夹带。共 4 个文件从此进版本库。

自签证书（`tools/.fixtures/smtp-cert/`）**仍不入库**：`mock-smtp.js` 检测不到证书会自己生成
（优先 openssl，退化为 Node crypto），且把私钥提交到公开仓库会触发 GitHub 的推送保护。

**影响范围**：新克隆的仓库从此可以直接跑完整测试（此前会有 5 个套件因缺夹带而失败）。
不涉及任何运行时行为。

### 四、清理与其它

- 删除 `.fixtures/` 下 90 个陈旧测试日志（约 296 KB，均为历史回归的中间输出）
- `README.md`：目录结构补上 `docs/`、`server/notify/`、`tools/.fixtures/`，
  版本号升至 1.14，新增「文档」索引表

**验证**：无功能改动，`SCHEMA_VERSION` 保持 v6。按约定仍跑完整全量回归
以确认整理过程没有碰坏任何东西：

### 五、全量回归（整理后复测）

**954 / 954 全部通过，37 个套件全绿**（`node tools/run-all-tests.js`，含真实浏览器
Chrome + Edge 交互实测、真实 xlsx 文件读回校验、本地模拟 IMAP/SMTP 服务器全流程）。
与 1.13 的成绩逐项一致，说明整理工作没有碰坏任何东西。

**顺带更正一处长期记错的数字**：此前各版本条目里写的「31 个套件 / 29 个套件 / 28 个套件」
是人工数的，一直少算了 6 个（断言总数 954 是对的）。实际套件数以
`tools/run-all-tests.js` 的 `SUITES` 数组为准，整理后实测为 **37 个**。
README 与本条目已按 37 更正；历史条目保留原样，不再改写。

---

## [1.13] — 2026-09-14

本次归档：**报价单模块补齐** —— 新增**报价模板**，并补上报价单管理缺的两块
（独立报价单列表页 + 客户详情页报价记录）。数据库结构 **v5 → v6**。

### 一、新增：报价模板

**解决什么**：阀门报价的规格组合高度重复（同样几个阀种/口径/压力/材质反复敲），
每次报价都要从零敲一遍明细行。

**表结构**（v6，两张新表）：
- `quotation_templates`：名称、类别、说明、默认单位、使用次数、最近使用时间、排序、启用开关
- `quotation_template_items`：规格明细行

**关键设计：模板不含价格。**
单价随项目、行情、客户议价能力变化，把价格存进模板很容易报出过期价。
因此模板只沉淀"规格 + 数量"，套用后由使用者填价。
这一点做成了**结构性约束**——明细表里根本没有 `unit_price` / `discount` / `subtotal` 列，
不是靠界面自律，测试里也专门断言了这一点。

**功能**：
- 模板 CRUD：新建 / 编辑（明细行整体覆盖）/ 删除（软删除进回收站）
- **排序**：上移 / 下移（已在首尾时给出明确提示），新模板自动排到末尾
- **启用 / 停用**：停用的模板不出现在套用列表里，直接调接口套用会被拒并说明原因
- 筛选：按名称/说明关键词、按类别；列表带出类别清单供下拉
- **从报价单沉淀为模板**：实际报过好用的组合一键存下来，不用重敲；
  未填名称时自动生成带单号的名字（便于辨认来源）
- **套用模板**：返回可直接插入报价单的明细行（单价留空）；
  明细已有内容时询问「替换」还是「追加」；记录使用次数与最近使用时间

**界面入口**：
- 功能设置 → 新增「报价模板」标签页（列表 + 新建/编辑抽屉，含明细行内编辑）
- 报价单抽屉里新增「套用模板」条：选模板 → 带出规格；也可「把当前明细存为模板」

### 二、补齐：报价单管理缺的两块

**① 独立报价单列表页（新增导航项「报价单」）**

原先报价单只能进某个项目详情里才看得到，"我一共报了多少、中了几单、哪张快过期"
没有一处能一眼看完。新增页面提供：
- **汇总卡**：报价单总数、报价合计、已中标数、已落标数、**中标率**、中标金额
- **状态快捷筛选**：全部 / 草稿 / 已报出 / 已中标 / 已落标 / 已过期（带各自单数）
- 筛选：关键词（单号/项目/客户）、报价日期区间；排序：日期 / 金额 / 状态 / 版本
- 每行操作：查看详情、编辑、复制新版本、导出 Excel、删除；客户与项目名可点击跳转
- 详情抽屉：单头信息 + 明细表 + 合计 + 落标竞对信息 + **版本链一览**

**② 客户详情页「报价记录」区**

跨该客户**全部项目**汇总报价单，便于比价与回看历史报价，并可导出。
原先只能逐个进项目看。

### 三、后端新增接口

| 接口 | 说明 |
|---|---|
| `GET /api/quotation-templates` | 模板列表（关键词/类别/仅启用） |
| `GET /api/quotation-templates/:id` | 模板详情（含明细） |
| `POST/PUT /api/quotation-templates[/:id]` | 新建 / 修改 |
| `DELETE /api/quotation-templates/:id` | 软删除 |
| `POST /api/quotation-templates/:id/move` | 排序上移/下移 |
| `POST /api/quotation-templates/:id/apply` | 套用（返回明细行，不写库） |
| `POST /api/quotation-templates/from-quotation/:qid` | 从报价单沉淀 |
| `GET /api/quotations/overview` | 跨项目总列表（含汇总统计、筛选、分页） |
| `GET /api/quotations/status-counts` | 各状态单数（供筛选标签） |

### 四、本版验证

**新增 3 个测试套件、95 项断言**：

| 测试文件 | 项数 | 覆盖 |
|---|---|---|
| `tools/test-migration-full.js` | 26 | 完整迁移链（起点自适应，见下） |
| `tools/test-quotation-template-service.js` | 35 | 模板 CRUD/归一化/沉淀/套用/排序/筛选/删除/日志 |
| `tools/test-quotation-template-flow.js` | 34 | 接口全流程 + 浏览器建模板、套用模板、独立页、客户报价记录 |

**迁移测试的起点自适应**（本版新做法）：
原先的迁移测试假定"真实库是旧版本"，但全套件连跑时运行器会先把库清空重建为最新版，
此时没有旧库可迁移，测试必然失败。现在按起点分两种准备：
① 真实库是旧版本 → 复制它来测（最贴近实际升级场景）；
② 真实库已是最新 → **自造一个 v1 旧库**（含 `reg_capital` 列与数据）跑完整迁移链。
两种准备都断言"数据一条不少"，**不因换了起点就降低标准**。

**其它修正**：
- 1.12 新增「报价单」导航项后，验收项 24b 的导航项数断言由 6 改为 7，
  并把 Chromium 与 Edge 两个浏览器的遍历路由都补上 `quotations`
- 新增 `tools/cleanup-quotation-template-test.js`（模板测试数据清理）

**全量回归：954 / 954 全部通过**（31 个套件，较 1.12 的 859 项新增 95 项）。

### 五、已知限制与下一阶段

- **报价单导出的 Excel 无单元格样式**（SheetJS 社区版不写出加粗/边框；结构与列宽正常）
- **A2 合同/质保期预警**：方案已就绪（见 `docs/方案/后续功能_技术方案.md` §3），顺延
- 其余排期：C3 数据体检 → 落标分析 → C1 经营月报 → C2 重复客户合并 → C4 日志回滚 → D1 全局搜索

---

## [1.12] — 2026-09-14

本次归档：**修复两个已知小缺陷**（1.11 遗留）。数据库结构无变化，仍是 v5。

### 修复 1：内联新增字典项时提示文案总是「已新增」

**现象**：在表单里点下拉旁的「+」新增选项时，即使输入的是**已存在的选项**，
提示也总是"已新增选项「XXX」"，让人以为字典里多了一条。

**根因**：`web/js/components2.js` 的 `submitAdd` 用
`this.list.includes(r.value)` 判断"该选项是否已存在"。
但 `list` 在前一版已改成 `{value,label}` 对象数组，
拿字符串去 `includes` 永远为 false，于是两种情况都走了"已新增"分支。

**修复**：改用**服务端返回的 `existed` 标志**判断，而不是在前端猜。
`/api/dict/quick-add` 本来就返回 `existed`（以及 `reenabled` / `restored`），
据此可给出准确提示：
- `existed=false` → 「已新增选项」
- `existed=true` → 「已选用已有选项」
- `existed=true + reenabled` → 「之前已停用，已重新启用并选用」
- `existed=true + restored` → 「已恢复并选用」

顺带修掉了同类隐患：这个判断原来依赖"前端列表里有没有"，
而列表可能因缓存/刷新时机与库不一致；改看服务端标志后不再有这个耦合。

### 修复 2：查重面板 `v-if` 与 `v-for` 写在同一元素上

**现象**：功能可用，但属 Vue 3 官方明确不推荐的写法
（同元素上 `v-if` 优先级高于 `v-for`，容易误读作用域，也不利于后续改动）。

**位置**：`web/js/customer-edit.js` 中重复客户卡片那一行
`<div v-if="showDup" class="mini-card" v-for="d in duplicates">`。

**修复**：拆成 `<template v-if="showDup">` 包住 `<div v-for="d in duplicates">`，
语义清晰；并全项目扫描确认无其它同元素写法。

### 本版新增的回归测试

`tools/test-customer-create-crash.js` 注册进运行器（13 项断言），并在原有基础上**加强**：

- 内联新增**全新**选项 → 必须提示「已新增选项」
- 内联输入**已存在**的同名选项 → 必须提示「已选用已有选项」且**不得**出现"已新增"
  （这两条正是修复 1 的回归，此前无人断言过提示文案本身）
- 造 3 家同电话客户后新增同号客户 → 查重面板必须**逐条渲染成独立卡片**
  （修复 2 的回归；此前只断言"面板出现"，未验证多条渲染）

**测试过程本身修掉的三个测试缺陷**（都不是产品问题，但会误导判断）：
1. 填字段前未展开折叠区块 → 「公司电话」在折叠区里，`setText` 静默失败
   （现在填报后会回报各字段实际值）
2. 固定等待 2.6 秒后断言"抽屉已关" → 改为**轮询等待**到稳定状态，
   避免"数据已落库、抽屉还没关完"的瞬间误判
3. 硬编码电话 `0991-0000000` 与另一套件留下的客户**撞号** → 正确触发查重反被误判为缺陷；
   改为带时间戳的唯一号码

**验证**：该套件连跑 3 次均 13/13；**全量回归 859 / 859 全部通过**（29 个套件）。

---

## [1.11] — 2026-09-14

本次归档两组内容：
1. **A1 跟进提醒（软件内 + 邮件）**、**A3 地图客户点**、**B1 报价单管理** 三项新功能
2. **启动器（`启动.bat`）健壮性与启动速度修复**（前一天顺延过来的，与本版功能一并发布）

数据库结构 **v4 → v5**（新建报价单两张表）。

---

### 一、新增功能

#### A1 客户跟进提醒（软件内 + 邮件）

**解决什么**：`customers.next_follow_at` 有值，但过去只在打开软件被动看到；
软件开一下午也不会提醒，容易漏跟进。

**软件内提醒**（主路径，始终可用）：
- 顶栏铃铛 + 角标计数；有逾期时红色脉冲提示
- 面板按紧急度分组：**逾期**（红）/ **今天**（橙）/ **临近**（灰，默认折叠避免一次几十条）
- 每项显示客户、天数文案（"逾期 4 天"）、计划时间、联系人电话
- 「记跟进」直接开抽屉、「稍后」当日不再提醒该客户（存 localStorage，按天失效）
- 到提醒时间且有待办时**每天自动弹一次**；过了安静时间（默认 18:00）不再弹
- **已成交 / 已流失客户自动排除**，不再催

**邮件提醒**（可选，**默认关闭**）：
- **零依赖手写 SMTP 客户端**（与招标采集的 IMAP 同一套模式，未引第三方包）
- 只走 465 隐式 TLS；已实测 QQ / 163 / 126 / 腾讯企业邮 / 天翼 189 均可用
- 中文主题走 RFC 2047 编码字、正文 base64（实测解码无乱码）
- 认证失败给出可操作提示："请确认填的是授权码而不是登录密码"
- **连续失败 3 次自动关闭**邮件提醒并显示原因，不重试轰炸
- 当天无待跟进客户**不发空邮件**
- **未启用时零外部网络请求**（用 socket 计数实测确认）
- 授权码**不回传前端**（只回"是否已配置"），错误信息里脱敏

**设置页**新增「邮件提醒」卡片：5 家国内邮箱预设、各自的授权码获取路径指引、测试发送、通道状态。

#### A3 地图客户点

**解决什么**：客户存了经纬度，但地图只画地州色块、图上找不到客户——
录了坐标却看不到点，等于白录。

- 地州色块之上叠加**散点图层**：有坐标的客户按真实位置显示
- 点大小 ≈ 年需求量/成交额，颜色 = 客户等级（A 红 / B 蓝 / 其他灰）
- 点击点进客户详情；点少时自动显示名称
- **只画有坐标的客户**：没有坐标的画到地州中心会误导，改由侧栏提示"还有 N 家没录经纬度"
- **点数超过 300 时按重要性取前 N**，并如实标注省略了多少家（不静默裁剪）
- **坐标异常检测**：明显不在新疆范围内的客户不绘制，侧栏列出并给「去修正」入口
- 下钻到地州时显示该地州全部有点客户

#### B1 报价单管理

**解决什么**：阀门销售里报价版本多、改价频繁，"上次报的什么价"常要翻记录。

- **报价单 + 明细行**两张表；明细含阀种/口径/压力/材质/连接方式/数量/单价/折扣/交期
- **金额一律服务端计算**：小计 = 数量 × 单价 × (1 − 折扣)，合计 = 各行之和；
  客户端提交的 `total_amount` 会被忽略（防篡改、防两边算不一致）
- 折扣按百分比填写（填 10 = 让 10%），也兼容小数比例；超额折扣封顶为全免
- **单号自动取号**：`前缀-日期-序号`（如 `BJ-20260914-001`），同日多单不重号
- **复制为新版本**：版本 +1、状态重置为草稿、原单保留可对比
- **状态流转**：草稿 → 已报出 → 已中标 / 已落标 / 已过期
- **落标登记**：记录竞争对手、对手报价、落标原因（后续"落标分析"的数据来源）
- **中标后一键回填**：弹确认框逐条列出将改动字段（合同额 `旧→新`、阶段、投标结果），
  确认后才写入；**只改状态时绝不动项目合同额**
- **导出 Excel 报价单**：单独排版（抬头、单据信息、明细表、合计、条款、落款），
  含列宽、行高、合并单元格；实测生成的文件可被 Excel 正常读回
- 项目详情新增「报价单」标签页；客户维度可跨项目查报价记录
- 软删除进回收站

**已知限制**：SheetJS 社区版**不写出单元格级样式**（加粗、边框），
故导出文件有完整结构与列宽，但字体加粗与边框不生效。
测试中把这一点显式记录为已知限制，而非假装通过。

---

### 二、启动器修复（原 1.11 顺延内容）

#### 修复 1：启动慢——探测死端口等满超时

**根因**：健康探测用 `curl -s --max-time 2`，而**这台机器上连本机死端口不会立即拒绝，
会一直等到超时**。实测：探测有服务的端口 19 ms，探测无服务的端口 **2022 ms**。
于是当 `.run.json` 缺失或过期、需要扫描 11 个端口范围时，白等 11 × 2 秒 ≈ **22 秒**。

**修复**：探测命令加 `--connect-timeout 0.1`。实测对比：

| 探测方式 | 死端口 | 有服务端口 |
|---|---|---|
| `--max-time 2`（修复前） | 2022 ms | 19 ms |
| `--connect-timeout 0.3 --max-time 1` | 322 ms | 19 ms |
| **`--connect-timeout 0.1 --max-time 1`（修复后）** | **125 ms** | **19 ms** |

**实测效果**：

| 场景 | 修复前 | 修复后 |
|---|---|---|
| 服务已在运行（常规双击） | 约 2 秒 | **0.18 秒** |
| `.run.json` 失效需扫范围 | 约 22 秒 | **0.17 秒** |
| 冷启动（扫完范围再起服务） | 22 秒以上 | **2.45 秒** |

### 修复 1：启动器可能卡死不启动服务（严重）

**现象**：双击 `启动.bat` 有时服务起不来（窗口停住不继续），或窗口一闪就没。

**根因**：启动器为"挑一个空闲端口"执行了
`netstat -ano | findstr /r /c:"LISTENING" | findstr /c:":端口 "` 这条**双层管道**。
实测该管道在"启动器以分离进程运行且输出被重定向"的情况下会**挂死**
——本次排查中真的复现了：两个 `findstr` 进程持续存活、`:findport` 永不返回，服务因此从未启动。
而单独执行同一条管道只需 0.02 秒，属 Windows 管道在继承句柄下的边界情况。

**修复**：**删除端口预检查**，改由服务端自己选端口
—— `server.js` 本来就会从 `CRM_PORT` 起向后扫描 11 个端口并写入 `data/.run.json`，
启动器再查一遍既多余又脆弱。
- 移除 `:findport` / `:portfree` 两个子过程及 `netstat` 管道
- 启动器直接把 `CRM_PORT` 交给服务端，并提示"若被占用会自动顺延"

### 修复 2：`rem` 注释行改用纯 ASCII（防被解析成命令）

**现象**：报错形如 `'xxx含本软件标识' is not recognized as an internal or external command`。

**根因**：启动器的 `rem` 注释行含中文（多字节字符）。当控制台代码页与文件编码不一致时，
cmd 解析这类行可能被截断，把注释正文当成命令执行，于是出现上述报错。
（本次核对：磁盘上的 `启动.bat` 本身完好——无 BOM、合法 UTF-8、全 CRLF、与提交版本一致，
因此该报错来自**环境侧的解析**，而非文件损坏。）

**修复**：`.bat` 中**所有 `rem` 注释改为纯 ASCII**（面向用户的 `echo` 中文文案保留不动），
从根上消除代码页差异导致的解析风险。生成器 `tools/make-bats.js` 同步写入说明注释，
避免以后又改回中文。

### 修复 3：端口范围与实际运行端口不一致时误判"服务未运行"

**根因**：服务端的单实例锁依据 `data/.run.json`（**与端口无关**），
而启动器原来只扫描自己配置的端口范围。两者不一致时，启动器误判为"未运行"→ 去启动新实例
→ 新实例被单实例锁挡掉、打印提示后退出 → 窗口消失，看起来就像启动失败。

**修复**：`:probe` 改为两级探测
1. **优先读 `data/.run.json` 记录的端口**（权威依据）并探测
2. 记录失效时再退回扫描配置范围

### 其他改动

- 健康探测由 `powershell Invoke-WebRequest` + 临时文件 + `findstr`
  改为 **`curl -s --max-time 2`**（Windows 10 1803+ / Windows 11 自带）。
  PowerShell 首次启动要 1~2 秒，curl 更快也更少依赖。
- 新增工具：
  - `tools/check-launcher.js` —— 启动器静态检查（无 BOM / 全 CRLF / `rem` 全 ASCII / 探测逻辑）
  - `tools/dump-bat-bytes.js` —— 逐行转储 bat 字节，排查编码问题
  - `tools/test-launcher-e2e.js` —— 端到端验证（停服务 → 用启动器启动 → 校验 → 二次启动识别）

**验证结果**：
- 服务未运行时启动成功，8899 就绪（冷启动 2.45 秒）
- 服务已运行时 **0.18 秒**识别并打开页面，不起重复实例
- `.run.json` 失效时范围扫描 **0.17 秒**（原 22 秒）
- 强制把启动器端口范围设为 8901~8905，仍正确识别 8899 上运行的服务（修复 3 的回归）
- 冒烟回归 67/67 通过（地址保存、地址归属、下拉取值、批量导入）

### 附带发现：bat 文件必须用 CRLF

排查过程中我用脚本生成临时 bat 时写成了 LF 换行，cmd 直接解析失败
（首行报错后整段错乱，无法自愈）。因此 `tools/check-launcher.js` 的静态检查
把「无 BOM / 全 CRLF / `rem` 全 ASCII」列为必检项——这三项任一不满足都可能让启动器失效。

### 三、本版验证

**新增 6 个测试套件、124 项断言**：

| 测试文件 | 项数 | 覆盖 |
|---|---|---|
| `tools/test-remind-ui.js` | 19 | 提醒铃铛/角标/分组/稍后/记跟进/跳转/排除已成交 |
| `tools/test-notify-email.js` | 29 | SMTP 完整投递、认证失败、超时、报文编码、零请求、失败降级 |
| `tools/test-prefs-ui.js` | 10 | 邮件配置区字段、服务商预设、保存、原有设置未受影响 |
| `tools/test-map-points.js` | 14 | 取点规则、上限省略、坐标异常、地州筛选、图层渲染、点击跳转 |
| `tools/test-quotation-service.js` | 29 | 金额计算、取号、复制版本、状态、回填、导出、软删除 |
| `tools/test-quotation-flow.js` | 31 | 接口全流程 + 真实 xlsx 生成与解压校验 + 浏览器建单与回填确认 |

**邮件用本地模拟 SMTP 服务器验证**（`tools/.fixtures/mock-smtp.js`，零依赖自签证书），
无需真实邮箱即可验证完整协议链路；**已另用真实 QQ 邮箱完成真机联调**。

**xlsx 校验方式**：xlsx 是 zip 压缩的，**不能靠在二进制里搜字符串判断内容**。
新增 `tools/.fixtures/xlsx-inspect.js` 手工解压读 `sheet1.xml`，
可准确判断列宽/行高/合并/样式是否真的写入（此前的错误判断方法一度导致误判）。

**全量回归：844 / 844 全部通过**（28 个套件）。

### 四、已知问题（下一版处理）

- **导出报价单无单元格样式**：SheetJS 社区版不写出加粗/边框（结构与列宽正常）
- **`c-select` 内联新增选项的提示文案恒为"已新增"**：`components2.js` 中
  `this.list.includes(r.value)` 判断失效（`list` 已是 `{value,label}` 对象数组），
  导致复用已有选项时也提示"已新增选项"
- **`customer-edit.js` 查重面板 `v-if` 与 `v-for` 同元素**：Vue 3 下属不推荐写法
- **客户录入"填字段过程中闪退"未能复现**：已测约 35 个场景（60 个字段逐一交互、真实窗口 Chrome、
  多标签长表单、窗口缩放、中文输入、标签新建、各类保存路径）均无异常；
  现场留 5 个排查脚本于 `tools/`，待提供控制台报错后继续
- **A2 合同/质保期预警**：方案已就绪（见 `docs/方案/后续功能_技术方案.md` §3），
  实施时需给项目表加 `accepted_at` / `warranty_months`，质保期按**验收日**起算
- 报价模板、经营月报导出、数据体检、日志回滚、全局搜索等见方案文档 §6

---

## [1.10] — 2026-09-13

本次归档三项：**修复详细地址被清空**、**修复地图不显示已填地址的客户**、**新增批量导入客户的 Excel 模板与入口**。

### 修复 1：客户「详细地址」保存后被自动清空

**现象**：客户信息里填了详细地址，保存后再打开发现变成空的。

**根因**（两层叠加）：
1. 客户列表接口 `listCustomers` 的 SELECT 只返回了 34 个字段，**漏掉 `address` 等 26 个字段**；
   而列表页「编辑」是把列表行直接当表单初始值的，表单里没有的字段就是空值，
   整体保存时就把库里已有的值覆盖成了空。
2. 项目列表接口同样漏字段（`valve_needs` / `win_rate_note` / `start_date` / `remark`），
   从项目列表点「编辑」也会清掉这四项。

**修复**：
- `server/services/crm.js`：客户列表字段由 34 列补到 **74 列**（表格展示字段 + 编辑表单字段的并集）
- `server/services/pm.js`：项目列表字段由 27 列补到 **33 列**
- `web/js/pages/customers.js`、`web/js/pages/projects.js`：打开编辑抽屉时
  **先取一次完整记录再打开**，即使将来列表字段又有遗漏也不会再误清空

**影响范围**：所有从列表页编辑客户/项目的操作。

### 修复 2：客户地址已填但地图不显示

**现象**：客户填了地址，首页新疆地图上却没有这家客户，还提示「未归属地州（未填市/地区）」。

**根因**（两层叠加）：
1. **前端下拉把"地州名"当编码存了**：「归属地州」下拉的选项值是「克拉玛依市」这样的名称，
   而该字段存的是地州编码、地图按编码聚合，于是地图认不出这家客户。
   （这也是历史上库里出现 `region_code='克拉玛依市'` 这种脏数据的原因。）
2. **地址匹配只在「区/县」字段里找区县**：用户在「市/地区」字段填了县级市/区/县
   （如「库尔勒市」「独山子区」「乌鲁木齐县」——这是很自然的填法），区县留空时就匹配不到。
   界面提示还把"填了但认不出"说成"未填市/地区"，进一步误导。

**修复**：
- `web/js/components2.js`：`c-select` 支持 `{value,label}` 对象选项（显示名称、提交编码）
- `web/js/customer-edit.js`：归属地州改用 `{value: 编码, label: 名称}` 选项
- `server/services/crm.js`：`syncCustomerRegion` 重写为"越具体越优先"的匹配顺序
  （区县字段的二级区划 → 地州 → **市字段的二级区划** → 市字段的地州 → 去括号补充说明 → 全字段包含匹配）
- `server/services/crm.js`：新增 `resolveRegionInput`，`region_code` 收到名称时自动解析成编码
- `server/services/crm.js`：新增 `fixLegacyRegionCodes`，服务启动时幂等纠正历史脏数据
- `server/map/service.js` + `web/js/components-map.js`：地图侧栏如实区分
  「没填市/地区」与「填了但系统认不出」，并列出具体客户 + 「去补全」直达链接

**影响范围**：地图统计、按区域筛选、客户归属地州。

### 新增：批量导入客户的 Excel 模板与入口

**背景**：导入功能原本已存在（功能设置 → 导入导出，58 列模板 + 校验 + 预览 + 错误报告），
但**入口太深不易发现**，且模板缺少填写参考。

**改动**：
- `web/js/pages/customers.js`：客户列表右上角新增「**批量导入**」按钮，三步抽屉
  （下载模板/拖入文件 → 校验预览 → 结果），无需离开客户页
- `web/js/settings-data.js`：抽出公共实现 `CRM.importXlsx`（模板生成 + 文件解析），
  客户页与管理页共用一套逻辑
- **模板增强为 3 页**：
  - 导入模板页：第 2 行完整示例，**第 3 行只填必填项**（示意"最少填什么"）
  - 填写说明页：12 条（日期写法、多值分隔、数字不带单位、地图归属、重名处理…）
  - **可选值参考页（新增）**：列出各字典列的当前可选值，照着填不会在字典里造出重复选项
- `server/services/excel.js`：模板接口返回字典可选值；必填列与界面表单对齐为
  **客户全称 / 客户简称 / 主体类型 / 下游行业**（原来只有前两项）

**顺带修复的两个隐藏缺陷**：
- **"与库中重名"原被当作校验错误**（`errors.push`），导致重复行连"跳过"都不算、
  既不能跳过也不能更新，前端若有"重复怎么办"的选项形同虚设。
  现改为**重复**语义：`duplicateMode='skip'` 跳过 / `='update'` 更新已有记录；
  更新时**只覆盖表格里填了值的列**，留空的保留库中原值，匹配键（客户全称）不改写。
- `runImport` 注释里写了 `skipDuplicate` 选项但函数体从未读取，已改为真正生效的 `duplicateMode`。

**影响范围**：设置 → 导入导出；客户列表页。

### 本次新增的测试（共 6 个套件、92 项断言）

| 测试文件 | 项数 | 覆盖 |
|---|---|---|
| `tools/test-customer-address.js` | 13 | 详细地址保存/局部更新/列表编辑路径/回显 |
| `tools/test-region-map.js` | 22 | 地址→地州归属→地图显示端到端 |
| `tools/test-region-legacy-fix.js` | 6 | 历史脏数据（region_code 存名称）纠正 |
| `tools/test-region-select.js` | 8 | 归属地州下拉：显示名称、提交编码 |
| `tools/test-import-template.js` | 10 | 模板三页结构、必填标记、示例、字典可选值 |
| `tools/test-import-customers.js` | 24 | 预校验、skip/update 重名、字典补全、归属 |
| `tools/test-import-ui.js` | 9 | 真实浏览器拖入真实 xlsx 全流程 |

**另修复一个会导致整页白屏的缺陷**：客户页导入抽屉模板里写了嵌套反引号
（`` `开始导入（${...} 条）` ``），而 Vue 的 template 本身是反引号字符串，
字符串被截断导致 `customers.js` 解析失败、**整个客户页打不开**。已改为字符串拼接，
并全项目扫描确认无第二处。

**回归结果**：全量 **712 / 712 通过**（本次新增 92 项）。

### 本次新增/修改的辅助工具

| 工具 | 用途 |
|---|---|
| `tools/find-customer-in-backups.js` | 在备份里查找某客户及其关联数据 |
| `tools/restore-customer-from-backup.js` | **从备份单独回填一家客户**（不整库回滚） |
| `tools/export-customer-by-id.js` | 按 ID 导出客户+关联数据为 JSON |
| `tools/import-customer-json.js` | 从 JSON 回填客户 |
| `tools/probe-region-match.js` | 地址匹配能力探测（43 种真实填法） |
| `tools/security-scan.js` | 提交前安全扫描（凭据 / 本机路径 / 数据是否被纳入版本控制） |

### 安全加固：清理脚本增加防误删闸门

**背景**：开发期清理脚本的匹配规则里曾混入真实企业名（`%独山子石化%`），
导致真实客户被误删两次；且 `--all` 会无条件清空全部业务数据，在已录入真实数据的库上
误跑验收脚本会造成不可恢复的损失。

**加固为三道闸门**：
1. **按名称清理**：执行前先列出将删除的客户；
   若匹配项中包含"名称不含测试特征词"的客户，未加 `--force` 直接拒绝执行
2. **`--all` 必须显式加 `--yes`**（打印当前数据量后退出）
3. **`--all --yes` 仍会二次校验**：检测到真实业务数据则拒绝执行（退出码 3），
   并提示"先备份、或改用不带 `--all` 的清理、或加 `--force` 明确确认"

`tools/run-all-tests.js` 同步处理退出码 3：**中止而不代为跳过**，并给出三条处理建议。

配套地，`PATTERNS` 中已移除全部真实企业名，只保留测试专用词。

### 仓库与版本管理

- 建立 `.gitignore`：**本机数据（`data/`）、测试产物（`.fixtures/`）、截图（`.shots/`）、
  工具目录一律不纳入版本控制**，避免公开仓库泄漏客户资料
- 建立 `.gitattributes`：`.bat` 固定 CRLF（启动器依赖 UTF-8 无 BOM + CRLF）
- 建立 `README.md` 与 `CHANGELOG.md`（含**版本归档规范**）
- 仓库：<https://github.com/forlxx-0322/lxx-0911>

---

## [1.9] — 2026-09-13

### 修复：待办关联字段可能被写成 0

**根因**：`saveTask` 未归一化 `customer_id` / `project_id`，表单清空选择时传来的 `0`
会原样入库。`0` 不是合法主键，按 `customer_id IS NULL` 查询会漏掉这些待办，
统计口径出现「孤儿」数据。

**修复**：`saveTask` 把 `0`/空串统一归一化为 `NULL`；新增 `normalizeTaskLinks`
在服务启动时幂等纠正历史数据。

---

## [1.8] — 2026-09-13

### 新增：招标信息采集模块（邮件订阅解析路线）

按实施方案**附录 A** 落地，路线为 A.6 推荐的「邮件订阅解析」——不抓取任何网站。

- **零依赖实现**：手写 IMAP 客户端（基于 Node 内置 `tls`）+ MIME 解析器，
  未引入任何第三方包，保持「断网可装可用」
- **数据库结构 v3 → v4**：新增 `collect_sources` / `collect_staging` / `collect_logs`
  三张表，项目表增加 5 个溯源字段（`source_url` / `source_platform` / `collected_at` /
  `source_notice_id` / `confidence`）
- **采集范围**：仅新疆本地项目 + 仅阀门相关关键词
- **铁律代码级强制**：来源类型白名单（只实现邮件订阅）、来源默认全部停用、
  IMAP 全程只读（`EXAMINE` + `BODY.PEEK`，不标记已读、不改动邮箱）
- **不采集任何个人联系方式**：检测到姓名/手机号/邮箱只记录类型标记，
  字段与摘要均不含原值，界面引导用户点原公告自行查看
- **采集结果先入暂存区**，人工审核后才写正式项目库
- **调度规则**（附录 A.7）：每 24 小时采集一次、有则更新无则不更；
  连续 3 次失败自动暂停该来源
- **离线可用性不受影响**：未启用来源时，启动与页面访问均**零外部网络请求**

**界面**：新增「招标采集」页（待审核 / 采集来源 / 采集记录）。

**测试**：5 个套件共 188 项断言，配 8 封多平台形态邮件夹具与本地模拟 IMAP 服务器。

---

## [1.7] — 2026-09-13

### 界面美化（UI 精修）

- **设计令牌精修**：配色、圆角、冷调双层阴影、聚焦光环
- **提升信息层级**：统计卡数字放大加粗 + 语义化强调条（回款绿 / 逾期红）；
  表格表头加深、行高放松、hover 高亮 + 首列指示条；金额列等宽数字
- **消除无效留白**（用实测数据决策）：地图画布限宽居中
  （地图本体占宽从 47.6% 提升到 75.5%）、看板改为 6 列 + 横向滚动
- **补齐深色主题**：原 `app.css` 已备好深色令牌但**没有任何代码启用它**。
  新增 `web/js/theme.js`（响应式状态 + localStorage 持久化 + 跟随系统偏好），
  顶栏加主题开关，设置页加主题下拉；图表配色接入主题（含地图深色色阶）
- **交互反馈**：按钮按下位移与键盘焦点光环、快捷筛选选中态、搜索框聚焦、
  字典内联新增改圆形按钮、任务逾期改左侧红条 + 极浅底色、滚动条统一、
  尊重 `prefers-reduced-motion`

**回归**：432 / 432 通过（美化未影响功能）。

---

## [1.6] — 2026-09-13

### 阶段七最终验收

按实施方案 §7.3 执行 **31 项验收标准**，全部通过；全套件 **432 / 432**。

**本轮发现并修复 3 项缺陷**：
1. `GET /api/dict` 的 `items` 未返回 `enabled` 字段 → 设置页字典管理里
   已停用项永远不显示「已停用」标记、按钮永远是「停用」，无法重新启用
2. 高级筛选缺少「认证要求」条件（验收标准第 9 项明确要求）
3. 待办关联字段可被写成 `0`（同 1.9）

---

## [1.5] — 2026-09-13

### 阶段六：新疆客户分布地图

- 地州级矢量地图（24 个地州级要素含兵团市）+ 县市级下钻（96 个）
- 本地 GeoJSON（16 个文件、929 KB），断网可用
- 坐标转换：WGS84 ↔ GCJ-02 ↔ BD-09（纯本地计算，往返误差 0.0003 m）
- 地址自动归属地州、手工坐标精确定位、「在线地图核对」跳转
- 客户列表高级筛选含「归属地州」

---

## [1.4] — 2026-09-13

### 阶段五：附件管理

- 图片 / PDF 页内预览（PDF 支持 Range 请求），其他类型下载
- 拖拽上传、大文件（≥10MB）不卡死
- 附件按年月分目录、uuid 命名；支持按分类分组与容量统计

---

## [1.3] — 2026-09-13

### 阶段四：首页总览与设置

- 首页 6 张数字卡 + 4 张图表 + 待跟进 / 回款 / 招投标日历 / 待办 / 动态
- 设置页 9 个标签页（字典 / 标签 / 提醒偏好 / 备份恢复 / 导入导出 / 附件存储 /
  回收站 / 操作日志 / 关于）
- 每日自动备份（主目录保留 30 份 + 镜像目录保留 7 份），恢复带兜底备份
- Excel 导入导出（浏览器端 SheetJS 生成/解析，后端只处理 JSON）
- 操作日志

---

## [1.2] — 2026-09-13

### 阶段三：项目管理

- 14 阶段看板（拖拽改阶段）、项目列表、项目详情
- 回款计划 / 实收登记，欠款、回款率、回款状态自动计算
- 客户累计成交额自动同步
- 待办中心（跟进计划 / 回款提醒 / 手动待办汇总）

---

## [1.1] — 2026-09-13

### 阶段二：客户管理

- 客户 8 区块 60 字段（阀门行业定制），多选标签 / 开关 / 级联字段
- 联系人管理、跟进记录（自动回填统计 + 生成待办）
- 查重提示、软删除与回收站、标签管理
- 字典内联新增（表单里直接建选项）

---

## [1.0] — 2026-09-13

### 阶段一：环境与服务骨架

- **零依赖技术栈**：Node.js 内置 `node:sqlite` + `node:http`，前端 Vue 3 / ECharts /
  SheetJS 全部内置于 `web/vendor/`，**无需 npm install、无需任何联网配置**
- 13 张业务表 + `schema_version`，迁移框架（迁移前自动备份、逐级幂等）
- 数据库结构 v1
- 一键启动（`启动.bat` / `停止.bat`）、单实例锁、优雅停机
- 全部数据仅存本机、仅监听 `127.0.0.1`

---

## 版本归档规范

从 1.10 起，**每次功能修改或 Bug 修复都归档一个版本号**。执行方式：

1. **升版本号**：修改 `server/server.js` 的 `APP_VERSION`
   （数据库结构有变化时同时升 `server/db.js` 的 `SCHEMA_VERSION` 并补一条迁移）
2. **写本文件**：在顶部插入新版本小节，写清
   - 现象 / 需求是什么
   - **根因**（Bug 必须写到根因，不写"已修复"了事）
   - 改了哪些文件、怎么改的
   - **影响范围**（哪些既有行为会变）
   - 新增/修改的测试与回归结果
3. **提交信息**：`<类型>(<范围>): <简述>`，类型用 `feat` / `fix` / `docs` / `refactor` /
   `test` / `chore`；正文写根因与验证方式
4. **推送**：推到 GitHub 仓库（仓库名形如 `VALVE-GUANLI-<版号>`，以 `git remote -v` 为准）

版本号取两位小数递增（1.10 → 1.11 → 1.12 …）。
