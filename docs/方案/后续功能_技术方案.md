# 后续功能技术方案

| 项目 | 内容 |
|---|---|
| 文档版本 | **v2.1**（1.15 归档：补记「报价自定义列」实现；A2 仍为下一版第一件事） |
| 软件当前版本 | **1.15**（数据库结构 **v7**，22 张表） |
| 下一版目标 | **1.16 —— A2 合同 / 质保期到期预警**（数据库结构 v7 → **v8**） |
| 已完成 | **A1** 客户跟进提醒（软件内 + 邮件）、**A3** 地图客户点、**B1** 报价单管理、**B2** 报价模板（1.11 ~ 1.13）、**B3** 报价自定义列（1.15） |
| 待实施 | **A2**（本文档 §2，方案已就绪，可直接开工）、§3 排期表中的 C / D 组 |
| 前身 | 本文件由 `后续功能_技术方案_v1.11.md` 更名而来（1.14 起全部文档收进 `docs/`） |

> **怎么读这份文档**
> - 只想知道「下一版做什么、怎么做」→ 看 **§2**（A2 方案）+ **§4**（实施计划）
> - 想知道「已完成的功能当时是怎么实现的」→ 看 **§1** 的结论表；
>   逐行细节（现象 → 根因 → 修复 → 影响文件 → 验证）在 [`CHANGELOG.md`](../../CHANGELOG.md) 对应版本条目里
>
> 排期原则：**一次只做一件事，做完就验收、就打版号**。
> 每个功能都要新增或扩展测试套件，并跑完整全量回归（当前 **38 个套件**），不得跳过、不得降低判定标准。

---

## 0. 决策记录

### 0.1 已确认（1.11 批，均已落地）

| # | 决策点 | 确认结果 | 落地情况 |
|---|---|---|---|
| 1 | 提醒方式 | 软件内提示 + 邮件提示 | ✅ `server/services/reminders.js` + `server/notify/`（手写零依赖 SMTP） |
| 2 | 报价中标后 | 允许一键把报价合计填入项目合同额 | ✅ 报价单转「已中标」时确认后回填，写变更日志 |
| 3 | 报价模板 | 留待后续 | ✅ **1.13 已做**（B2 升级为独立功能，见 §1） |
| 4 | 合同/质保期预警（A2） | 顺延 | ⏳ **本文档 §2，下一版第一件事** |

### 0.2 A2 待确认（开工前请逐条拍板）

| # | 决策点 | 建议 | 影响 |
|---|---|---|---|
| A | **预警范围** | 只做「项目」维度：合同交付期、合同结束日、质保期、待验收 | 若要把「质保金应收」也做成机器判定，需给回款计划加「款项性质」字段（预付款/进度款/到货款/质保金），那是另一件事，建议单列 |
| B | **邮件形态** | 合并进现有那封每日提醒邮件（一封邮件两个板块），不另发一封 | 现值：无待跟进客户时**不发信**；加入到期提醒后改为「两块都空才不发」 |
| C | **首页是否加卡片** | 先在顶栏铃铛面板里加「即将到期」板块；首页卡片看使用后的感受再定 | 少改一处界面，先把判定逻辑与提醒链路跑通 |
| D | **提前量默认值** | 质保期 30 天、合同（交付/结束日）14 天，均可在「设置 → 提醒与偏好」改 | 沿用既有设置项模式，不新建配置表 |
| E | **未验收项目的质保口径** | 用「交付期 + 质保月数」预估，界面明确标注「预估，验收后自动更正」 | 存量项目 `accepted_at` 必然为空，不阻塞使用 |

> 这 5 条都确认后即可开工；其中 **A 与 E** 影响数据模型，**B** 影响验收判定，是重点。

---

## 1. 已完成项记录（1.11 ~ 1.13）

### 1.1 交付清单

| 功能 | 做法（要点） | 关键文件 | 版本 |
|---|---|---|---|
| **A1 客户跟进提醒** | 纯只读查询算出「逾期 / 今天 / 临近」三级清单；设置项加提前天数与安静时段；顶栏铃铛面板 + 可「今天不再提醒」 | `server/services/reminders.js`、`server/routes/reminders.js`、`web/js/reminders.js` | 1.11 |
| **A1 邮件提醒** | 手写零依赖 SMTP 客户端（465 隐式 TLS），复用 IMAP 那套服务商预设与授权码指引；默认关闭，连续失败自动降级 | `server/notify/smtp.js`、`server/notify/index.js` | 1.11 |
| **A3 地图客户点** | 新增取点接口，在既有地州地图上叠加散点图层；点多时按策略省略并提示数量 | `server/map/*`、`web/js/components-map.js` | 1.11 |
| **B1 报价单管理** | 新增 `quotations` / `quotation_items` 两张表；金额服务端算；版本复制、状态流转、落标登记竞对、中标回填合同额、Excel 报价单导出 | `server/services/quotation.js`、`web/js/quotation-drawer.js`、`web/js/pages/quotations.js` | 1.13 |
| **B2 报价模板** | 新增 `quotation_templates` / `quotation_template_items`；**模板刻意不含任何价格列**（结构性约束，不是靠界面自觉）；可套用到报价单、可从报价单存为模板 | `server/services/quotation-template.js`、`web/js/pages/quotation-templates.js` | 1.13 |
| **B3 报价自定义列** | 报价明细的列**由使用者自己定义**（介质、设计压力、设计温度、泄露等级、执行器型号……列数不封顶）：`quotation_fields` 存列定义，明细 `extra` 存 JSON 值（键=列 id）；报价单与模板共用同一套列，导出 Excel 动态加列 | `server/services/quotation-field.js`、`web/js/quotation-fields.js`、`web/js/quotation-drawer.js` | 1.15 |

### 1.2 值得保留的工程结论

1. **联网模块的统一模式**（招标采集 → 邮件提醒）：独立子模块、**默认关闭且关闭时零请求**、
   失败降级不轰炸、凭据脱敏（只回「是否已配置」，不回明文）。
   A2 **不新增联网点**（只是往已有邮件里多写一段）。
2. **提醒的分级口径**：`逾期(红) / 今天(橙) / 临近(灰)` + 安静时段 + 单条「今天不再提醒」。
   A2 直接复用这套分级与交互，不另立一套。
3. **数据库迁移框架**：`SCHEMA_VERSION` + `MIGRATIONS[i]`（升级到 i+1），
   迁移前自动备份。A2 的 v7 → v8 走同一机制，并且 `tools/test-migration-full.js`
   做的是**全链迁移**（从真实旧库或合成的 v1 老库一路升到最新），新迁移必须进这条链。
4. **列表字段缺失 = 数据被清空**（1.10 踩过的坑）：任何给业务表新增的字段，
   必须同步补进该表的列表 SELECT，否则「从列表页打开 → 保存」会把没查出来的字段写空。
   A2 新增两个项目字段，`pm.js` 的列表查询必须同步——**并在测试里专门覆盖这一条**。

### 1.3 B3 报价自定义列（1.15）—— 设计记录

报价要填的规格项因客户而异（炼化要「介质 + 设计温度 + 泄露等级」，水务要
「阀门标准 + 执行器型号 + 限位开关」），固定列永远差一两项。所以做的不是"再加几列"，
而是**让列可以自己加**。

| 存什么 | 存在哪 | 说明 |
|---|---|---|
| 列定义 | `quotation_fields`（v7 新表） | 名称、类型（text/number/select）、单位、候选值、排序、启用 |
| 每行的值 | `quotation_items.extra` / `quotation_template_items.extra` | JSON，**键 = 列 id** |

三条设计红线（都有对应测试）：

1. **不做动态 ALTER TABLE**：每加一列就迁移一次、列名一改就在库里留下对不上号的列。
   列定义 + JSON 值可以任意增删改，**加列不需要再迁移**。
2. **JSON 键用列 id 而非列名** → **改名不丢值**（「泄露等级」→「泄漏等级」，
   历史单据照常显示）。
3. **类型不做数值强转**：`number` 只决定录入控件与导出对齐，避免把「≤80」「PN16」挡在门外。

行为边界：**删列不删值**（只是不再显示，删列接口返回 `used_in` 影响面；
下次保存该单据时孤儿值才被清理）、**停用不删值**、不存在的列 id 直接丢弃、
空值不落库、超长值截断（200 字）。

上限：60 列 / 列名 20 字 / 单位 10 字 / 值 200 字 / 候选值 60 项。

**将来要扩展时改哪里**：

- 想加"列类型"（如日期、计算公式）→ `quotation-field.js` 的 `KINDS` 白名单 +
  前端 `quotation-fields.js` 的录入控件与 `quotation-drawer.js` 的单元格渲染
- 想把自定义列用于**数据导出/打印**而不只是报价单 → 复用 `exportData()` 的
  `custom_columns` 字段（已按列顺序、带单位返回）
- 想按模板区分列（不同模板显示不同列）→ 现在刻意是**全局一套列**；
  要改成"每模板一套"需给 `quotation_fields` 加 `scope/template_id` 并改 `liveFieldIds()`
  的过滤口径，属于结构性改动（当前不做，因为"报价单与模板共用一套列"是刻意的简化）

---

## 2. A2 · 合同 / 质保期到期预警（下一版第一件事）

### 2.1 现状与痛点（已核对代码）

| 事实 | 位置 | 意义 |
|---|---|---|
| 项目有 `signed_at` / `bid_date` / `start_date` / `end_date` / `delivery_date` | `projects` 表 | 合同期与交付期**已有数据**，可直接用 |
| 项目**没有**验收日期、**没有**质保月数 | `projects` 表（已确认无这两列） | 需迁移新增 |
| 客户有 `warranty_months`（质保月数）与 `warranty_ratio`（质保金比例） | `customers` 表 | 只能当「该客户的常规约定」，用作新建项目时的默认值；质保期本质是**项目级**的 |
| 已有回款计划查询（逾期 / 近期）与核销关联 | `pm.js` 的 `paymentOverview()`、`payments.plan_id` | 将来做「质保金应收」可直接复用，但**计划本身没有款项性质字段** |
| 已有提醒服务与铃铛面板 | `services/reminders.js`、`web/js/reminders.js` | A2 是**扩展现有模块**，不是新建一套 |

**痛点**：项目有签约日、交付期、结束日，但**没有任何「快到期」提示**。

- 质保期到了没跟进（质保金该收了、续单机会来了）；
- 合同交付期临近但货还没发完；
- 已交付但忘了补验收日期 → 质保期永远算不准。

这些现在全靠记忆，而漏掉的都是钱。

### 2.2 方案

#### 2.2.1 质保期起算点：**验收日**（沿用已确认结论）

```
质保期到期日 = accepted_at（验收日期） + warranty_months（质保月数）

accepted_at 为空时（尚未验收）：
  → 退化为「delivery_date（交付期） + warranty_months」作为**预估**，
    界面上明确标注「按计划交付日预估，验收后自动更正」
```

**月份加法采用「日号截断」而不是溢出**：1 月 31 日 + 1 个月 = 2 月 28/29 日（不是 3 月 2/3 日）。
理由：质保期是**商务约定**，用户心里的「12 个月」就是「明年同一天」，
月末溢出到下一月会让人对不上账。实现上用 JS 计算并截断到目标月最后一天，
**测试必须覆盖 1 月 31 日、闰年 2 月 29 日、12 月 31 日 +1 个月**。

> SQLite 的 `date(x,'+1 month')` 对 1 月 31 日会归到 3 月 3 日，**不要**直接用它算质保期。

#### 2.2.2 四类预警与判定

| 类型 | 判定条件 | 提前量 | 级别 |
|---|---|---|---|
| **质保期到期** | `质保到期日` ≤ 今天 + N 天（N = `warranty_remind_days`，默认 30） | 30 天 | 已过期=红；≤7 天=橙；其余=灰 |
| **待验收提醒** | 已过 `delivery_date` 且 `accepted_at` 为空 | — | 橙（推动补录验收日期，否则质保期算不准） |
| **合同交付期** | `delivery_date` ≤ 今天 + N 天（N = `contract_remind_days`，默认 14），且项目未完成 | 14 天 | 逾期=红 |
| **合同结束日** | `end_date` ≤ 今天 + N 天，且项目未完成 | 14 天 | 逾期=红 |

**与跟进提醒相反的一点**：质保期提醒**恰恰发生在项目做完之后**，
所以 A2 的查询**不过滤终态阶段**（`deleted_at IS NULL` 仍然要过滤）。

**同一项目多条命中只出一条**：取最紧急的那条作为列表项，其余在项目详情里列全，
避免一个项目在面板里刷屏。

#### 2.2.3 界面

1. **顶栏铃铛面板**新增「即将到期（N）」板块，排在待跟进板块下方，样式复用现有分组；
2. 每条显示：项目名 / 客户简称 / 「质保到期还剩 X 天」/ 到期日；
3. 点击直达项目详情；支持单条「今天不再提醒」（复用现有 snooze 机制，key 换成 `project_id`）；
4. **项目详情**新增「质保到期日 + 剩余天数」小条，并解释预估口径；
5. **项目表单**新增两个字段：验收日期（日期选择）、质保月数（数字，默认取该客户的 `warranty_months`）。

### 2.3 数据与接口

#### 2.3.1 迁移（`SCHEMA_VERSION 7 → 8`）

`projects` 新增两列（**纯新增列，幂等，不回填、不动既有数据**）：

| 新列 | 类型 | 说明 |
|---|---|---|
| `accepted_at` | TEXT | 验收日期（质保期起算点） |
| `warranty_months` | INTEGER NOT NULL DEFAULT 0 | 该项目质保期月数 |

新增两个设置项（走既有 `settings` 表，不建配置表）：

| key | 默认值 | 说明 |
|---|---|---|
| `warranty_remind_days` | `30` | 质保期提前多少天提醒 |
| `contract_remind_days` | `14` | 合同交付期 / 结束日提前多少天提醒 |

#### 2.3.2 接口

| 接口 | 说明 |
|---|---|
| `GET /api/reminders/expiring` | **新增**，只读，返回 `{items, counts, window}` |
| `GET /api/reminders/due` | **保持原样**（现有测试与前端契约不变） |
| `GET/PUT /api/reminders/settings` | 扩展：多两个数字项（`notify.readRemindSettings` / `saveRemindSettings` 的字段白名单要同步加） |

#### 2.3.3 后端改动点（照单执行）

| 文件 | 改什么 |
|---|---|
| `server/db.js` | `SCHEMA_VERSION = 7`；`MIGRATIONS[6]`；settings 种子两行 |
| `server/services/reminders.js` | 新增 `expiringSoon(db, settings)`；抽出 `addMonthsClamped()`；`buildEmail()` 支持第二板块 |
| `server/routes/reminders.js` | 加 `sub === 'expiring'` 分支 |
| `server/services/pm.js` | **列表 SELECT 与详情 SELECT 补上 `accepted_at` / `warranty_months`**；字段写白名单同步 |
| `server/notify/index.js` | `sendDailyReminder()` 合并两个板块；无内容判定改为「两块都空」 |
| `server/notify/index.js` 的 `readRemindSettings` | 白名单加两个新 key（否则设置页存不进去） |

#### 2.3.4 前端改动点

| 文件 | 改什么 |
|---|---|
| `web/js/reminders.js` | 铃铛面板加板块、角标计数改为两块之和、snooze 支持项目 |
| `web/js/project-edit.js` | 两个新字段（含「质保期从验收日算起」说明文案） |
| `web/js/pages/project-detail.js` | 质保到期日与剩余天数 |
| `web/js/pages/settings.js` | 提醒偏好加两个数字输入 |
| `web/js/api.js` | `remindersExpiring` 绑定 |

### 2.4 难点与风险

| 风险 | 说明 | 应对 |
|---|---|---|
| 月末 / 闰年 | 见 §2.2.1 | 自己实现「日号截断」并写边界测试 |
| 验收日期缺失（存量项目必然为空） | 质保期算不出来 | 退化用交付期预估 + 界面标注 + 「待验收提醒」推动补录 |
| **列表字段缺失导致清空** | 1.10 已踩过 | 新字段进 `pm.js` 列表 SELECT，并**单独写一条断言**覆盖「从列表页编辑后字段仍在」 |
| 项目阶段终态 | 与跟进提醒的口径相反 | 明确：A2 **不过滤**终态阶段；测试覆盖「已完成项目的质保期仍然提醒」 |
| 邮件由「有空才发」变「两块都空才不发」 | 会影响既有邮件测试 | 更新 `tools/test-notify-email.js` 的判定，并新增「只有到期项时也要发」的用例 |
| 日期脏值 | 早期录入可能有空值 / 格式不一 | 统一走 `date()` 比较；空值跳过不参与预警 |
| 语义重叠 | 「合同结束日」与「交付期」可能同一天 | 同一项目同一天只出一条，取更紧急的类型 |

**难度：低**（一个迁移 + 一组查询 + 一处面板扩展）
**工作量：0.5 ~ 1 天**（含测试与全量回归）

### 2.5 验收标准（A2 新增 3 项，与既有 31 + 8 项并行有效）

| # | 验收项 | 判定标准 |
|---|---|---|
| **R5** | 到期判定正确性 | 质保到期日按**验收日 + 月数（日号截断）**算出；`accepted_at` 为空时按交付期预估并在界面标注「预估」；月末、闰年 2 月、12 月 31 日边界均有断言；交付期与结束日按各自提前量命中 |
| **R6** | 提醒链路 | 铃铛面板出现「即将到期」板块且计数正确；点击直达项目详情；单条「今天不再提醒」生效；**已完成项目的质保期仍然提醒**；邮件为「跟进 + 到期」一封两块，两块都空时不发信 |
| **R7** | 不回归 | 新字段在项目列表 SELECT 中（从列表页编辑不会清空）；v7 → v8 迁移幂等、迁移前自动备份；原有 31 + 8 项验收与全部既有套件继续全绿 |

---

## 3. 后续排期（A2 之后）

| 优先级 | 功能 | 难度 | 工作量 | 备注 |
|---|---|---|---|---|
| **A2** | **合同/质保期到期预警** | 低 | 0.5~1 天 | **下一版就做**，见 §2 |
| 1 | **C3 数据体检**（缺字段清单） | 低 | 0.5 天 | 「哪些客户没填地区 / 没联系人 / 没下次跟进」一屏看完，直接提升数据质量；可与「坐标异常提示」合并 |
| 2 | **落标分析** | 低 | 0.5 天 | 报价单已顺手落库竞对与落标原因，这一步只做汇总视图 |
| 3 | **C1 经营月报导出** | 低 | 1 天 | 复用 Excel 生成机制；月度签约 / 回款 / 新增客户 / 中标率 |
| 4 | **回款核销界面** | 中 | 1 天 | 一笔实收分摊到多条计划；数据结构（`payments.plan_id`）已就绪，缺的是界面 |
| 5 | **D1 全局搜索（Ctrl+K）** | 中 | 1 天 | 顶栏搜索框现在是占位，可激活 |
| 6 | **报价单导出排版升级** | 中 | 0.5~1 天 | 现状：SheetJS 社区版不写单元格样式（已知限制）。可改为「生成可打印 HTML + 浏览器打印成 PDF」，或手写 xlsx styles |
| 7 | **报价单版本差异高亮** | 中 | 1 天 | 多版本并排比较，标出改动的行与金额 |
| 8 | **C2 重复客户合并** | 中 | 1 天 | 需处理 5 张关联表的外键迁移 |
| 9 | **C4 操作日志可回滚** | 中 | 1.5 天 | 按实体保存快照；注意与审计日志的职责区分 |
| 10 | **D2 首页看板自定义** | 低 | 1 天 | 区块排序与显示开关，存 localStorage |
| 11 | **Windows 系统通知** | 低 | 0.5 天 | 需浏览器授权；与铃铛面板互补 |
| 12 | **D3 深色主题细节打磨** | 低 | 0.5 天 | 附件预览、图表提示框 |
| — | **回款计划「款项性质」字段** | 低 | 0.5 天 | 预付款/进度款/到货款/质保金；做完才能机器判定「质保金应收」（决策 A 的备选） |
| — | **自定义列：按列筛选/排序** | 低 | 0.5 天 | 报价单列表按某个自定义列过滤（现在只按单号/客户/项目/状态筛） |
| — | **自定义列：列模板/分组** | 中 | 1 天 | 现在全局一套列；若要"不同模板显示不同列"，见 §1.3 的扩展说明 |
| — | **自定义列：数据导出也带上** | 低 | 0.5 天 | 报价单之外的普通数据导出（`excel.js`）目前不含自定义列 |
| — | **「录入客户闪退」排查** | ? | ? | 1.11 起约 35 个场景均未复现，等现场控制台报错；排查脚本已在 `tools/` |

**建议顺序**：A2 → C3 → 落标分析 → C1 → 回款核销 → D1 → 报价导出排版 → 其余按需。
**原则**：先做「能直接省钱 / 少漏事」的（提醒、对账、数据质量），再做「好看好玩」的。

> 报价单这条线（B1 报价单 → B2 模板 → B3 自定义列）到 1.15 已经够用了：
> 报价、版本、模板、任意规格列、导出单据都齐了。剩下的都是锦上添花
> （导出排版升级、版本差异高亮、按自定义列筛选），可以等真正用起来再挑。

---

## 4. 下一版（1.16）实施计划

| 步骤 | 内容 |
|---|---|
| 1 | 数据库 **v7 → v8** 迁移：`projects` 加两列 + 两个设置项种子（迁移前自动备份） |
| 2 | `reminders.js` 抽出「日号截断」月份加法与 `expiringSoon()`；先写边界测试（月末、闰年） |
| 3 | `GET /api/reminders/expiring` + 设置项读写白名单扩展 |
| 4 | `pm.js` 列表/详情 SELECT 与写白名单补字段 |
| 5 | 项目表单与详情页展示（验收日期、质保月数、到期倒计时） |
| 6 | 铃铛面板加板块 + snooze 支持项目 |
| 7 | 邮件合并两个板块，无内容判定改为「两块都空」 |
| 8 | 新增套件 `tools/test-reminders-expiring.js`；扩展 `test-remind-ui.js`、`test-prefs-ui.js`、`test-notify-email.js`、`test-migration-full.js`；套件总数 **38 → 39** |
| 9 | **全量回归**（含真实浏览器 Chrome + Edge），确认无退化 |
| 10 | 升版本号至 **1.15**，写 `CHANGELOG.md`，提交、打标签 `v1.15`、推送 |

### 收尾检查清单（每版都过一遍）

- [ ] 新增字段是否进了**所有**该表的 SELECT（列表 / 详情 / 导出 / 提醒查询）
- [ ] 新增字段是否进了写白名单，脏值是否有校验
- [ ] 迁移是否幂等、是否走「迁移前自动备份」
- [ ] 设置项 key 是否同时加到 `readRemindSettings` / `saveRemindSettings` 白名单
- [ ] 前端 `api.js` 绑定、页面注册（`web/index.html` 引入、`app.js` 注册组件、`router.js` 菜单 key 一致）
- [ ] 测试数据是否自建自清（跑完不污染真实库）
- [ ] `CHANGELOG.md` 条目写清「现象 → 根因 → 修复 → 影响文件 → 验证」
- [ ] 全量回归成绩写进 README 与 CHANGELOG

---

## 5. 风险与回退

- **迁移可回退**：v7 只是给 `projects` 加两列（都有默认值），不动既有数据；
  迁移框架会自动先备份，恢复走「设置 → 备份与恢复」。即使不恢复，多出的两列也不影响旧版本读取。
- **不影响既有功能**：A2 只新增查询、面板板块与两个表单字段，不改变任何既有字段语义；
  `/api/reminders/due` 的返回结构保持不变。
- **离线承诺不变**：A2 是纯本地计算，**不新增任何联网点**；邮件只是往已有那封里多写一段。
- **提醒不吵人**：沿用安静时段 + 单条「今天不再提醒」；同一项目只出一条，避免刷屏。

---

## 6. 修订记录

| 版本 | 日期 | 变更 |
|---|---|---|
| v1.0 | 2026-09-13 | 首版：A1/A2/A3/B1 四项方案与决策点 |
| v1.1 | 2026-09-13 | 按决策补邮件提醒（SMTP 零依赖）章节 |
| v1.2 | 2026-09-13 | A2 顺延、B2 报价模板顺延，本批范围收敛为 A1 + A3 + B1 |
| v1.3 | 2026-09-14 | A1/A3/B1 全部完成并发布（1.11），A2 方案保留待用 |
| **v2.0** | **2026-09-14** | **1.14 归档整理**：文件更名去掉 `_v1.11` 后缀并移入 `docs/方案/`；已完成项压缩为记录表（细节指向 CHANGELOG）；A2 按 1.14 的实际代码复核并改写（迁移编号改为 v6→v7、月份加法明确为「日号截断」、接口与前端改动点落到具体文件）；排期表刷新（报价模板已完成移出，补入回款核销、导出排版、款项性质等新条目）；新增决策点 A~E 待确认 |
| **v2.1** | **2026-09-14** | **1.15 归档**：新增 §1.3「B3 报价自定义列」实现记录（列定义 + JSON 值的模型、三条设计红线、行为边界、上限，以及**将来扩展要改哪里**）；迁移编号随结构版本推进改为 **v7 → v8**，下一版目标改为 **1.16**；排期表补入自定义列的后续项（按列筛选、列模板/分组、数据导出带上） |
