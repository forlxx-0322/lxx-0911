# 后续功能技术方案（v1.11）

| 项目 | 内容 |
|---|---|
| 文档版本 | **v1.3（A1/A3/B1 已上线，A2 顺延至下一版）** |
| 目标版本 | 1.11 已发布（数据库结构 v4 → **v5**）；下一版目标见 §6 |
| 本批范围 | **A1 客户跟进提醒（软件内 + 邮件）**、**A3 地图客户点**、**B1 报价单管理** —— ✅ **已全部完成并发布** |
| 本批不做 | **A2 合同/质保期预警**（明确顺延至下一版）、C1~C4、D1~D3（见 §6） |
| 状态 | A1/A3/B1 已完成（全量回归 844/844）；**A2 方案保留在 §3，下一版可直接实施** |

> 本文档中 §2（A1）/§4（A3）/§5（B1）已实施完毕，保留作为实现记录；
> §3（A2）为**下一版待实施**的现成方案，无需重新设计。
> 实施完成情况与验收结果见 `CHANGELOG.md` 的 [1.11] 条目。

---

## 0. 决策记录（已确认）

| # | 决策点 | 确认结果 | 对方案的影响 |
|---|---|---|---|
| 1 | 提醒方式 | **软件内提示 + 邮件提示** | ⚠ 这是本项目**第二个会联网的功能**（第一个是招标采集）。按"独立子模块、默认关闭、关闭时零请求"的既有模式实现；SMTP 用零依赖手写客户端（见 §2.3） |
| 2 | 报价中标后 | **允许一键把报价合计填入项目合同额** | B1 状态改为「已中标」时，除提示同步项目阶段外，额外提供"用报价合计填入合同额"的动作（需用户点击确认，不静默改） |
| 3 | 报价模板 | **本次不做**，留待后续 | B1 不含"报价模板/常用规格一键带出"，列入 §6 后续计划 |
| 4 | 合同/质保期预警（原 A2） | **本批不做**，顺延到后续版本 | 连带顺延：项目表的 `accepted_at` / `warranty_months` 两个新字段**本批不加**，v5 迁移只建报价单两张表。原定的"待验收提醒"一并顺延 |

> 决策 1~3 见前一轮确认；决策 4 为本轮范围调整。
> **原 §3 的 A2 章节保留在文档中作为已完成的方案储备**，实施时跳过即可，
> 后续版本启用时无需重新设计（届时只需按决策"质保期按验收日起算"落地）。

---

## 1. 现状与前提核实

本批四项都建立在既有能力上，实施前已核对代码，结论如下：

| 能力 | 现状 | 对本批的意义 |
|---|---|---|
| 首页提醒数据 | `dashboard.js` 已产出 `payment_overdue` / `payment_upcoming` / `bid_calendar` / `today_tasks` | A1/A2 可复用同一套查询风格，不必另起炉灶 |
| 提醒设置项 | 已有 `follow_remind_days`(3) / `payment_remind_days`(7) / `birthday_remind` | A1/A2 **只需追加设置项**，不新建配置表 |
| 客户经纬度 | `customers.longitude` / `latitude` 已存在且可录入、可校验 | A3 无需改表，只需把坐标下发到前端 |
| 地图 series | `components-map.js` 的 `draw()` 目前只有一条 `type:'map'` 系列 | A3 需在同图叠加 `scatter` 系列，`geoIndex` 复用现有地图 |
| 地图接口 | `map/service.js` 的 `distribution()` **只返回地州聚合计数，不下发单体坐标** | ⚠ **A3 需新增取点接口**（这是本次唯一的新增接口） |
| 项目日期字段 | 已有 `signed_at` / `bid_date` / `delivery_date` / `end_date` / `start_date` | A2（本批不做）的合同期与交付期可直接用 |
| 项目质保字段 | **没有** `warranty_months`，也**没有验收日期** | A2（本批不做）启用时需给项目表加 `accepted_at` 与 `warranty_months` |
| Excel 导出 | `excel.js` 已有 `SCHEMAS`（customer/contact/project）与 `exportData()` | B1 的报价单导出可复用列定义与生成机制，但需新增报价单专属排版 |
| 主从表单 | 项目详情已有「回款计划 / 实收流水」两个主从结构 | B1 的报价单明细可照此实现，交互成本低 |
| 明细行编辑 | `payment-drawer.js` 已有抽屉式表单 + 校验 | B1 报价单抽屉可复用该模式 |
| **IMAP 客户端** | `server/collect/imap.js` 已手写零依赖 TLS 客户端；`mail.js` 已有 MIME 编解码 | **A1 邮件提醒可复用这套模式**：SMTP 与 IMAP 同源，服务商预设、授权码指引、失败降级策略都可直接沿用 |
| SMTP 连通性 | 已实测 QQ / 163 / 126 / 腾讯企业邮 / 天翼 189 的 **465 与 587 均通** | 邮件提醒走 465 隐式 TLS，可行 |

---

## 2. A1 · 客户跟进滚动提醒

### 2.1 现状与痛点

- `customers.next_follow_at` 已有值，首页「今日与逾期待跟进客户」会列出，但**只在你打开软件时被动看到**；
- 软件开着一下午也不会提醒你「张工说好今天回电」；
- 结果是要么漏跟进，要么频繁去翻首页。

### 2.2 方案

**提醒方式：软件内提醒 + 邮件提醒**（按决策 2）。

**软件内提醒**（主要方式，始终可用）：顶栏角标 + 点击展开的提醒面板。

> 为什么不使用系统级通知（Windows 通知中心）：需要浏览器授权，且授权被拒后无提示、失败静默；
> 个人自用软件更可靠的做法是"你开着软件就一定能看到"。若你希望要系统通知，可后续单独加（属 D 级）。

**邮件提醒**（可选，默认关闭）：即使软件没开，也能在手机上收到当天要跟进谁。
详细设计见 §2.3。

**触发规则**（可配置）：

| 设置项 | 默认 | 说明 |
|---|---|---|
| `follow_remind_time` | `09:00` | 每天几点开始提醒（避免半夜弹） |
| `follow_remind_quiet` | `18:00` | 几点后不再弹新提醒 |
| `follow_remind_on_start` | `1` | 启动时立即检查一次（补上昨天没开机漏掉的） |

判定条件：`next_follow_at` 的日期 ≤ 今天 + `follow_remind_days`（复用现有设置，默认 3 天），
且客户未删除、状态不是「已成交/已流失」这类终态。

### 2.3 邮件提醒（决策 2 新增）

**定位：本项目第二个会联网的模块。** 按招标采集模块已验证的模式实现，守住三条护栏：

| 护栏 | 做法 |
|---|---|
| 默认关闭 | 设置项 `remind_email_on` 默认 `0`；未开启时**不建立任何连接**，整套提醒仍走软件内提示 |
| 独立子模块 | 邮件发送代码单独放 `server/notify/`，与业务逻辑解耦；关闭时该目录代码完全不执行 |
| 只发给自己 | 收件人固定为你在设置里填的邮箱；**不支持群发、不支持自定义收件人列表**，避免误用成营销工具 |

**为什么手写 SMTP 而不引第三方包**：本项目的立身之本就是零依赖（Node 内置模块 + 前端全本地），
且我在招标采集里已手写过一个 IMAP 客户端，SMTP 与之同源、模式完全一致，代码可复用大半。

**SMTP 客户端范围**（`server/notify/smtp.js`）：

- 传输：**465 端口隐式 TLS**（`tls.connect`）；已验证 QQ / 163 / 126 / 腾讯企业邮 / 天翼 189 的 465 全通
- 命令：`EHLO` → `AUTH LOGIN`（或 `AUTH PLAIN`，按服务端通告选择）→ `MAIL FROM` → `RCPT TO` → `DATA` → `QUIT`
- 认证：沿用**邮箱授权码**（不是登录密码），可直接复用招标采集里已有的服务商预设与获取指引
- 报文：UTF-8 编码，主题用 RFC 2047 编码字（`=?UTF-8?B?...?=`），
  正文用 `text/html; charset=utf-8` + `Content-Transfer-Encoding: base64`
  —— 这套编码逻辑我在 `server/collect/mail.js` 里写过解码方向，这里是编码方向，同源
- **超时与失败处理**：单次发送超时 20 秒；失败只记日志并在界面提示，**不重试轰炸**；
  连续 3 次失败自动关闭邮件提醒并提示你去检查配置（与招标采集的失败策略一致）

**发送时机**：每天在 `remind_email_time`（默认 `08:30`）发送一次，
内容为当日全部待提醒项（逾期跟进 / 今天要跟进 / 3 天内跟进 / 质保期到期 / 交付期临近 / 质保金应收）。
**当日无待提醒项则不发送**（避免每天收空邮件）。

**邮件内容**：一个自包含的 HTML（内联样式，不引用任何外部图片或样式表，
与软件本身一样不依赖外部资源），分区块列出待办，每项带客户/项目名与到期信息。

**端口与网络核实结果**（已实测）：

| 服务 | 465 (SSL) | 587 (STARTTLS) |
|---|---|---|
| smtp.qq.com / smtp.163.com / smtp.126.com / smtp.exmail.qq.com / smtp.189.cn | 全通 | 全通 |

本实现只走 **465**，比 587 的 STARTTLS 少一次协议升级、更简单可靠。

### 2.4 数据与接口（含邮件）

- **表结构**：无需新增表，无需迁移（纯查询）
- **新增设置项**：

| 设置项 | 默认 | 说明 |
|---|---|---|
| `follow_remind_time` | `09:00` | 每天几点开始软件内提醒 |
| `follow_remind_quiet` | `18:00` | 几点后不再弹新提醒（角标仍保留） |
| `follow_remind_on_start` | `1` | 启动时立即检查一次（补未开机漏掉的） |
| `remind_email_on` | `0` | **是否启用邮件提醒（默认关闭）** |
| `remind_email_time` | `08:30` | 每天发送时间 |
| `remind_email_to` | `` | 收件邮箱（默认与发件相同） |
| `smtp_provider` / `smtp_host` / `smtp_port` / `smtp_user` / `smtp_pass` | `qq` / `smtp.qq.com` / `465` / `` / `` | SMTP 配置；**授权码与其他设置项一样存在本地数据库**，界面只回显"是否已配置" |

- **新增接口**：

| 方法 | 路径 | 说明 |
|---|---|---|
| GET | `/api/reminders/due` | 跟进提醒列表（只读） |
| GET | `/api/reminders/expiring` | 到期预警列表（只读，A2 共用） |
| POST | `/api/reminders/test-email` | **测试发送**：立即发一封测试邮件，用于验证配置 |
| POST | `/api/reminders/send-now` | 立即发送一次当日提醒邮件（手动触发） |
| GET | `/api/reminders/email-status` | 邮件通道状态（是否启用、上次发送时间与结果、连续失败次数） |

### 2.5 前端（含邮件）

- 顶栏右侧加角标（数字 = 待提醒条数），点击展开提醒面板
- 提醒卡片：客户简称 + 「今天 / 逾期 N 天 / N 天后」+ 「记跟进」「稍后」两个按钮
- 「稍后」= 当日不再提醒该客户（存 `localStorage`，按天失效）
- 轮询：软件打开时每 15 分钟查一次（本地接口，开销可忽略）
- **设置页「提醒与偏好」增加邮件提醒配置区**：开关、发送时间、收件邮箱、
  服务商预设（复用招标采集的预设与授权码获取指引）、「测试发送」按钮、通道状态显示

### 2.6 难点与风险（含邮件）

| 风险 | 说明 | 应对 |
|---|---|---|
| 提醒打扰 | 若把历史逾期客户全弹出来，一次可能几十条 | 分级：**逾期**红、**今天**橙、**3 天内**灰；默认只展开逾期与今天，其余折叠 |
| 未开机补偿 | 昨天没开机，今天启动时提醒是否还弹 | `follow_remind_on_start=1` 时弹一次，面板内标注「补昨日」，不重复弹 |
| 重复弹窗 | 同一客户一天弹多次 | 前端按 `客户id + 日期` 记入 `localStorage` |
| **邮件打扰** | 每天收提醒邮件可能变骚扰 | 当日无待提醒项**不发**；可随时在设置里关闭；失败自动降级为只做软件内提醒 |
| **授权码安全** | 授权码存本地数据库 | 与招标采集同一处理：界面**只回显是否已配置**，不回传明文；日志与错误信息中一律脱敏 |
| **邮件编码** | QQ/163 对中文与编码敏感，主题不编码会乱码 | 主题走 RFC 2047 编码字，正文 base64；发送后用真实邮箱实测中文显示 |
| **离线承诺被破坏** | 引入 SMTP 后"零外部请求"不再无条件成立 | 关闭邮件时**零请求**（验收 R2 专项验证）；软件内提醒与其余功能仍完全离线 |

**难度：中**（软件内提醒是低难度，邮件部分是新增的手写 SMTP 客户端）
**工作量：1 天**（原 0.5 天 + 邮件 0.5 天）

---

## 3. A2 · 合同 / 质保期到期预警（⚠ 本批不做，方案储备）

> **本批不实施。** 保留于此供后续版本直接启用，无需重新设计。

### 3.1 现状与痛点

项目有签约日、交付期、结束日，但**没有任何"快到期"提示**：

- 质保期到了没跟进（意味着质保金该收了、续单机会来了）；
- 合同交付期临近但货没发完；
- 质保金到期应收未收。

这些现在全靠记忆，而漏掉的都是钱。

### 3.2 方案

在首页新增「**即将到期**」区块，并按紧急度排序。

**质保期起算点按决策 1 修正为「验收日」**（不是签约日）：

```
质保期到期日 = accepted_at（验收日期） + warranty_months（质保月数）

accepted_at 为空时（尚未验收）：
  → 退化为「交付期 + 质保月数」作为**预估**，
    该条在界面上明确标注「按计划交付日预估，验收后自动更正」
```

预警类型与判定：

| 类型 | 判定条件 | 提前量 | 级别 |
|---|---|---|---|
| **质保期到期** | `accepted_at + warranty_months` ≤ 今天 + N 天 | 30 天（可配） | 到期日已过 = 红；≤7 天 = 橙；其余 = 灰 |
| **待验收提醒** | 已过 `delivery_date` 且 `accepted_at` 为空 | — | 橙（提醒你去补验收日期，否则质保期无法准确起算） |
| **合同交付期** | `delivery_date` ≤ 今天 + N 天 且 项目未完成 | 14 天 | 逾期 = 红 |
| **合同结束日** | `end_date` ≤ 今天 + N 天 | 14 天 | 逾期 = 红 |
| **质保金应收** | 质保期已到 且 该质保金对应回款计划未核销完 | — | 红 |

每条可点击直达项目详情；区块支持「今天不再提示」。

> 「待验收提醒」是决策 1 带出来的必要补充：质保期既然从验收日起算，
> 那"已交付但没验收日期"的项目就必须能提醒到，否则质保期永远算不准。

### 3.3 数据与接口

- **表结构（需迁移，`SCHEMA_VERSION 4 → 5`）**：`projects` 新增两列

  | 新列 | 类型 | 说明 |
  |---|---|---|
  | `accepted_at` | TEXT | **验收日期**（决策 1：质保期起算点） |
  | `warranty_months` | INTEGER NOT NULL DEFAULT 0 | 该项目质保期月数 |

  理由：质保期是**项目级**的（同一客户不同项目可能不同）。
  客户表现有的 `warranty_months` 保留为"该客户的常规约定"，仅作新建项目时的默认值带入。

  迁移内容为**纯新增列**（幂等、不影响既有数据、不需要数据回填），是本次唯一的结构变更之一。

- **项目表单/详情新增两处**：验收日期（日期选择）、质保月数（数字）。
  验收日期旁附说明：「质保期从这一天开始算，默认 12 个月」。
- **新增设置项**：`warranty_remind_days`(30)、`contract_remind_days`(14)
- **新增接口**：`GET /api/reminders/expiring`（只读）
- **复用**：`dashboard.js` 里的 `payRows()` 查询模式；前端复用首页卡片样式
- **项目列表**：`pm.js` 的列表 SELECT 需补上这两列
  —— 注意这是 1.10 刚修过的同类缺陷（列表字段缺失会导致从列表页编辑时被清空），
  本次新增字段必须同步加入列表查询，并在测试中覆盖

### 3.4 难点与风险

| 风险 | 说明 | 应对 |
|---|---|---|
| 日期脏数据 | 早期录入可能有空值、格式不一 | 统一走 `date()` 比较；空值跳过不参与预警 |
| **验收日期缺失** | 质保期从验收日起算，但存量项目该字段必然为空 | 不阻塞：退化为"交付期 + 质保月数"作为预估并在界面标注；同时用「待验收提醒」推动你补录 |
| 月数加法 | SQLite 的 `date(x, '+12 months')` 对月末日期（如 1 月 31 日 +1 月）会归到 2 月末 | 用 SQLite 日期函数计算，并在测试里覆盖月末、闰年 2 月等边界 |
| 语义重叠 | 「合同结束日」与「交付期」可能同一天 | 同一项目同一天只出一条，取更紧急的类型 |

**难度：低**（一个迁移 + 一组查询）
**工作量：0.5 天**

---

## 4. A3 · 地图上精确显示客户点

### 4.1 现状与痛点

- 客户已能存经纬度，但**地图只画地州色块，图上找不到任何客户点**；
- 你录了坐标却看不到它在哪，验收标准第 30 项「手工录入坐标的客户可在地图上精确定位」
  目前只落到"统计归属"，没落到"图上可见"。

### 4.2 方案

在现有地州色块图上叠加 **散点图层（ECharts `scatter` + `geoIndex`）**：

- **有坐标的客户** → 画点，位置即真实经纬度
- **无坐标但有归属地州** → **不画点**（画在地州中心会误导），在侧栏提示"该地州另有 N 家未录坐标"
- 点的大小 = 年需求量（或成交额，可切换）；颜色 = 客户等级（A 红 / B 蓝 / C 灰）
- 点击点 → 浮窗显示客户名与年需求，可跳客户详情
- 悬停点 → 高亮并显示简称

**性能与可读性控制**（关键设计）：

| 场景 | 处理 |
|---|---|
| 全疆视图 | 点多于 **300** 个时，只显示"年需求量前 300"或按地州取前 N，并在图上标注"已省略 N 个点" |
| 下钻到某地州 | 显示该地州**全部**有点客户（数量天然有限） |
| 缩放 | 跟随 `roam` 自动缩放，点大小用 `symbolSize` 函数按比例收敛 |

### 4.3 数据与接口

- **表结构**：无变化（坐标字段已存在）
- **新增接口**：`GET /api/map/customer-points?code=650000&limit=300`
  返回 `[{ id, name, short_name, level, longitude, latitude, annual_demand, deal_amount, region_code }]`
  只读；按 `code` 过滤（省级或某地州）
- **前端**：`components-map.js` 的 `draw()` 增加一条 scatter 系列；图例增加"客户点"开关

### 4.4 难点与风险

| 风险 | 说明 | 应对 |
|---|---|---|
| 点太多糊成一片 | 客户上千时全画会糊 | 上述"前 300 + 省略提示"策略，且下钻后显示全部 |
| 坐标质量 | 手工录入可能录错（如经纬度颠倒、超出新疆范围） | 前端点超范围不绘制，侧栏提示"N 个坐标异常待修正" |
| 与色块视觉冲突 | 深色底 + 深色点会看不清 | 点用描边（白边）保证在任意底色上可见 |
| 与现有 hover 提示冲突 | 地州块与点的 tooltip 需区分 | 点用独立 formatter |

**难度：中**（ECharts 叠加系列本身不难，难点在性能与可读性控制）
**工作量：1 天**

---

## 5. B1 · 报价单管理

这批里**唯一能改变工作方式**的功能。阀门销售里报价版本多、改价频繁，
"上次报的什么价"现在要翻聊天记录。

### 5.1 功能范围

| 功能 | 说明 |
|---|---|
| 报价单主表 | 归属项目（自动带出客户）、报价单号、版本号、报价日期、有效期至、币种、状态、备注 |
| 报价明细 | 阀门类型 / 口径 / 压力等级 / 材质 / 连接方式 / 数量 / 单位 / 单价 / 折扣 / 小计 / 交期 / 备注 |
| 金额自动算 | 行小计 = 数量 × 单价 × (1−折扣)，合计 = 各行之和；**不允许手填合计** |
| 版本管理 | 「复制为新版本」，版本号自增（V1 → V2），保留旧版可对比 |
| 状态流转 | 草稿 → 已报出 → 已中标 / 已落标 / 已过期；改为终态时**提示是否同步更新项目阶段与投标结果** |
| **中标后一键回填**（决策 3） | 状态改为「已中标」时，弹窗确认后**把报价合计填入项目合同额**、阶段置「已中标/已签约」、投标结果置「已中标」；**必须由你点确认，不静默修改** |
| 导出 | 一键导出 **Excel 报价单**（含公司抬头、报价单号、明细表、合计、有效期、联系方式） |
| 客户视角 | 客户详情页可看该客户**全部项目**的报价单（跨项目比价） |
| 落标登记 | 状态改「已落标」时可填竞对名称与对手报价（这就是 B2 的核心数据，顺手落库） |

> **报价模板（决策 4）本次不做**：即"预设常用规格组合，一键带出明细行"留待后续版本，
> 已列入 §6 开发计划。本批只做单张报价单的建立与复制新版本。

### 5.2 数据模型（新增 2 张表，随 v5 迁移）

**`quotations` 报价单主表**

| 字段 | 类型 | 说明 |
|---|---|---|
| id | INTEGER PK | |
| project_id | INTEGER | 所属项目（必填） |
| customer_id | INTEGER | 冗余存一份，便于按客户查（保存时自动带出） |
| quote_no | TEXT | 报价单号，自动生成 `BJ-20260913-001`，可改 |
| version | INTEGER | 版本号，默认 1 |
| parent_id | INTEGER | 上一版本 id（用于版本链对比） |
| quote_date | TEXT | 报价日期 |
| valid_until | TEXT | 有效期至 |
| currency | TEXT | 币种，默认人民币 |
| status | TEXT | 草稿 / 已报出 / 已中标 / 已落标 / 已过期 |
| total_amount | REAL | 合计（服务端算，只读） |
| tax_note | TEXT | 税率说明（如「含13%增值税」） |
| competitor | TEXT | 竞对名称（落标时填） |
| competitor_price | REAL | 对手报价（落标时填） |
| lose_reason | TEXT | 落标原因 |
| remark | TEXT | 备注 |
| created_at / updated_at / deleted_at | TEXT | |

**`quotation_items` 报价明细表**

| 字段 | 类型 | 说明 |
|---|---|---|
| id | INTEGER PK | |
| quotation_id | INTEGER | 所属报价单 |
| seq | INTEGER | 行号（排序） |
| valve_type | TEXT | 阀门类型（字典 `valve_type`） |
| size_range | TEXT | 口径（字典 `size_range`） |
| pressure_rating | TEXT | 压力等级（字典 `pressure_rating`） |
| body_material | TEXT | 阀体材质（字典 `body_material`） |
| connection_type | TEXT | 连接方式（字典 `connection_type`） |
| quantity | REAL | 数量 |
| unit | TEXT | 单位（台/套） |
| unit_price | REAL | 单价 |
| discount | REAL | 折扣（0.1 = 让 10%） |
| subtotal | REAL | 小计（服务端算） |
| delivery_days | INTEGER | 交货期（天） |
| remark | TEXT | 行备注 |

> **不新建"产品目录表"**：阀门的阀种/口径/压力/材质等都是既有字典项，
> 组合起来就是规格，单独建产品库反而增加维护负担。
> **报价模板**（预设常用规格组合、一键带出明细行）按决策 4 顺延到后续版本，
> 届时可在本表基础上加一张 `quotation_templates`，不影响现有结构。

### 5.3 接口

| 方法 | 路径 | 说明 |
|---|---|---|
| GET | `/api/projects/:id/quotations` | 某项目的报价单列表（含合计、状态） |
| GET | `/api/quotations/:id` | 报价单详情（含明细行） |
| POST | `/api/quotations` | 新增（可同时提交明细行） |
| PUT | `/api/quotations/:id` | 修改（明细行整体覆盖） |
| POST | `/api/quotations/:id/copy` | 复制为新版本 |
| POST | `/api/quotations/:id/status` | 改状态（终态时返回是否需要同步项目的提示） |
| POST | `/api/quotations/:id/apply-to-project` | **中标回填**（决策 3）：把合计写入项目合同额、阶段置已签约、投标结果置已中标 |
| DELETE | `/api/quotations/:id` | 软删除（进回收站） |
| POST | `/api/quotations/:id/export` | 导出 Excel 报价单 |
| GET | `/api/customers/:id/quotations` | 客户维度报价单（跨项目报价记录，用于比价） |

### 5.4 前端

- **项目详情**新增「报价单」标签页（与「回款计划」「实收流水」并列）
- 报价单抽屉：上半部分单头字段，下半部分明细表（可增删行，行内编辑单价/数量/折扣，小计与合计实时算）
- 报价单详情：可打印（浏览器打印样式适配 A4）、可导出 Excel、可复制新版本
- **改为「已中标」时**：弹出确认框，逐条列出将被修改的项目字段
  （合同额 `旧值 → 新值`、阶段、投标结果），确认后才写入
- **客户详情**新增「报价记录」区块

### 5.5 难点与风险

| 风险 | 说明 | 应对 |
|---|---|---|
| 明细行编辑体验 | 表格式行内编辑 + 增删排序，是本次交互最复杂的部分 | 照 `payment-drawer` 的既有模式做；先实现"增删 + 行内编辑"，排序用上下移按钮 |
| 金额一致性 | 前端算与后端算可能不一致 | **以服务端为准**：保存时服务端重算小计与合计并回写，前端仅作即时预览 |
| 版本对比 | 版本多了想比较差异 | 第一版只做"复制新版本 + 并排查看两个版本明细"；差异高亮列入后续 |
| 单据编号冲突 | 同一天多张单 | 编号 = 日期 + 当日序号，服务端在事务内取号，避免并发重号 |
| 导出排版 | 报价单是"给客户看的单据"，不是数据表 | 单独写一个排版函数（表头合并、边框、金额右对齐、A4 列宽），不复用数据导出模板 |
| 与项目金额关系 | 报价合计 ≠ 合同额（中标后才是） | 报价阶段**不动**项目合同额；改为「已中标」时弹确认框，确认后才回填（决策 3） |

**难度：高**（本批唯一的高难度项：2 张表 + 主从表单 + 版本管理 + 单据导出）
**工作量：2 天**

---

## 6. 后续开发计划（本批不做，列入排期）

| 优先级 | 功能 | 难度 | 工作量 | 备注 |
|---|---|---|---|---|
| **A2** | **合同/质保期到期预警**（本批顺延） | 低 | 0.5 天 | 方案已就绪（见 §3）；实施时需给项目表加 `accepted_at` / `warranty_months`，质保期按**验收日**起算 |
| **B2** | **报价模板**（决策 3 顺延） | 中 | 1 天 | 预设常用规格组合，一键带出明细行；加 `quotation_templates` 表 |
| C1 | 经营月报导出 | 低 | 1 天 | 复用 Excel 生成机制 |
| C2 | 重复客户合并 | 中 | 1 天 | 需处理 5 张关联表的外键迁移 |
| C3 | 数据体检（缺字段清单） | 低 | 0.5 天 | 与 A3 的"坐标异常提示"可合并实现 |
| C4 | 操作日志可回滚 | 中 | 1.5 天 | 需按实体保存快照，注意与审计日志的职责区分 |
| D1 | 全局搜索（Ctrl+K） | 中 | 1 天 | 现有顶栏搜索框是占位，可激活它 |
| D2 | 首页看板自定义 | 低 | 1 天 | 区块排序与显示开关，存 localStorage |
| D3 | 深色主题细节打磨 | 低 | 0.5 天 | 附件预览、图表提示框 |
| — | 落标分析 | 低 | 0.5 天 | **B1 已顺手落库竞对与落标原因**，此步只做汇总视图 |
| — | 回款核销界面 | 中 | 1 天 | 部分收款分摊到多条计划 |
| — | Windows 系统通知 | 低 | 0.5 天 | 需浏览器授权；本次未做，可随时补 |
| — | 报价单版本差异高亮 | 中 | 1 天 | 多版本并排比较 |

**建议执行顺序**：~~本批（A1+A3+B1）~~ ✅ 已完成 → **下一版从 A2 开始** → C3 → 落标分析 → C1 → 报价模板 → C2 → C4 → D1 → D2 → 回款核销 → D3

> **下一版（1.12）建议第一件事就是 A2**：它的方案已完整（§3），且是唯一涉及"钱"的提醒
> （质保期到了意味着质保金该收、续单机会来了），价值高于其它待办。
> 实施时注意：需给项目表加 `accepted_at`（验收日期）与 `warranty_months`（质保月数）两列，
> 质保期按**验收日**起算；这两个新字段必须同步补进 `pm.js` 的列表 SELECT
> ——那正是 1.10 修过的同类缺陷（列表字段缺失会导致从列表页编辑时被清空）。

---

## 7. 本批实施计划

| 步骤 | 内容 |
|---|---|
| 1 | **数据库 v4 → v5 迁移**：新建 `quotations` / `quotation_items` 两张表；补索引与预置设置项（本批**不加**项目表字段） |
| 2 | A1 提醒（软件内）：设置项 + `GET /api/reminders/due` + 顶栏角标与提醒面板 |
| 3 | A1 提醒（邮件）：`server/notify/smtp.js` 手写 SMTP 客户端 + 邮件模板与调度 + 设置页配置区与「测试发送」 |
| 4 | A3 地图点：`GET /api/map/customer-points` + scatter 系列 + 省略策略与侧栏提示 |
| 5 | B1 报价单：服务层 + 10 个接口 + 项目/客户详情页 + 明细编辑 + 中标回填 + Excel 报价单导出 |
| 6 | 测试：3 个新套件（提醒判定与邮件客户端、地图取点、报价单全流程含导出文件校验） |
| 7 | 全量回归（当前 712 项 + 新增），确认无退化 |
| 8 | 升版本号至 **1.11**，追加 `CHANGELOG.md`，提交并推送 |

### 验收标准（本批新增 4 项，与既有 31+8 项并行有效）

| # | 验收项 | 判定标准 |
|---|---|---|
| **R1** | 跟进滚动提醒（软件内） | 到提醒时间且存在到期/逾期跟进时出现提醒；点击直达客户；「稍后」当日不再弹；未开机次日启动补一次 |
| **R2** | 邮件提醒 | **关闭时零外部网络请求**（与离线承诺一致）；开启后能按设定时间发出当日提醒，无待办则不发；中文主题与正文不乱码；失败不重试轰炸、连续 3 次失败自动降级；授权码在界面与日志中均脱敏 |
| **R3** | 地图客户点 | 有坐标的客户在图上按真实位置显示；点击可进详情；点多时按策略省略并明确提示省略数量；坐标异常有提示 |
| **R4** | 报价单管理 | 可建单含明细、金额服务端自动算；可复制新版本；状态可流转；**改为已中标时可一键把合计回填项目合同额（含确认步骤与变更日志）**；可导出 Excel 报价单；软删除可还原 |

### 决策点状态

4 个决策点**均已确认**（见 §0 决策记录），本批范围为 **A1 + A3 + B1** 三项，无遗留待确认事项。

---

## 8. 风险与回退

- **数据库迁移可回退**：v5 只做「建表」（本批不加任何既有表的列），不动既有数据。
  迁移前框架会自动备份，恢复走既有「设置 → 备份与恢复」；
  即使不恢复，多出的两张表不影响旧版本读取。
- **不影响既有功能**：A1 是新增查询与提醒面板，A3 是新增地图图层，
  B1 是新增表与详情页签，都不修改既有字段语义。
- **报价单与项目金额的边界**：报价阶段绝不动项目合同额；
  仅当状态改为「已中标」且你确认后，才回填（含变更日志，可追溯）。
- **离线约束**：A3/B1 为纯本地功能，零外部网络请求；
  A1 的软件内提醒同样零请求，**仅邮件部分在开启后联网**，关闭时零请求（验收 R2 专项验证）。
- **A2 顺延的说明**：原 §3 的合同/质保期预警方案保留在文档中，
  启用时需给项目表加 `accepted_at` / `warranty_months` 两列；
  **届时应沿用"质保期按验收日起算"的结论**，并同步把这两个字段补进 `pm.js` 的列表 SELECT
  ——这正是 1.10 修过的同类缺陷（列表字段缺失会导致从列表页编辑时被清空）。
