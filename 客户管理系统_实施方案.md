# 客户管理系统 · 实施方案（评审稿 v1.4）

> 用途：**阀门行业**个人自用客户与项目管理系统
> 状态：**阶段一、阶段二已完成并通过验收；阶段三开发中**
> 日期：2026-02
>
> **v1.1 修订记录**
> - 客户表字段由 20 个扩充到 **63 个**，按阀门行业业务链条重做（新增：客户主体类型、最终用户、设计院、EPC 总包、采购模式、阀门类型/口径/压力/材质、认证要求、投标资质、账期与质保金、供应商编码、介绍人等）
> - 下拉选项由 9 类扩充到 **18 类**，全部按阀门行业重做（下游行业覆盖石油/化工/电力/冶金/水处理/船舶/造纸/制糖/食品饮料/制药/空分等）
> - 项目表新增招投标相关字段（投标日期、开标结果、失标原因、交货期），项目阶段细化为 14 个
> - 首页新增「招投标日历」卡片与「下游行业成交额占比」图表
> - 第 7 节新增 **7.1 质量门禁**硬性条款：**24 项验收测试全量执行，不得跳过、不得降低标准、一票否决**
> - 工作量由 5.5 天调整为 6.5 天（含 0.5 天全量验收测试）
>
> **v1.2 修订记录**
> - 明确「下游行业」「客户主体类型」等字典类字段**支持手动新增自定义值**，并写入硬性验收项（第 3 节、第 7 节）
> - 新增模块：**首页新疆分布地图（第 10 节）**——ECharts 本地 GeoJSON 矢量地图，离线可用、可缩放、真实地理位置、坐标可校验
> - 数据表由 12 张增至 **13 张**（新增 `region` 行政区划表）
> - 验收标准由 24 项增至 **31 项**；工作量调整为 **约 8.5 天**
>
> **v1.3 修订记录**（本轮）
> - **招标信息采集模块经决策后取消**：先集中做好主体功能，采集改为后续可选升级项。本文档已彻底移除采集引擎、采集数据表、采集界面与相关验收项，不含半成品模块
> - 保留结论备查：招标信息采集的合规边界与可行路线见 **附录 A**（将来若要做，直接按附录执行）
> - 客户数据进入系统的途径确定为：**手动录入 + Excel 导入**（另有邮件订阅解析可作为零风险后续升级项）
> - 客户「个人联系方式不自动采集」原则**予以保留并扩大适用**：本软件不内置任何爬虫，不主动采集任何个人信息
>
> **v1.4 修订记录**（本轮）
> - 按确认结果**删除客户表的「注册资金」字段**（`customers.reg_capital`），数据库结构升级到 **v2**；升级走迁移框架，自动备份旧库并保留其余数据
> - 明确招标信息采集的**调度规则：每 24 小时采集一次、有则更新无则不更**（见 **A.7**），作为将来启用采集模块时的固定规则
> - 采集模块本身仍为「待启用」状态（v1.3 已决策取消），需单独确认后再开发
>
> **v1.5 修订记录**（本轮）
> - **招标信息采集模块已开发完成并启用**（按附录 A 执行，路线选定为 **A.6 推荐的「邮件订阅解析」**）：
>   - 数据库结构升级到 **v4**：新增 `collect_sources` / `collect_staging` / `collect_logs` 三张表，项目表增加 5 个溯源字段（`source_url` / `source_platform` / `collected_at` / `source_notice_id` / `confidence`），数据表由 13 张增至 **16 张业务表**
>   - **零依赖实现**：手写 IMAP 客户端（基于 Node 内置 `tls`）与 MIME 解析器，未引入任何第三方包，保持「断网可装可用」
>   - 采集范围按确认结果收窄为：**仅新疆本地项目 + 仅阀门相关关键词**
>   - 铁律代码级强制：来源类型白名单（只实现邮件订阅）、来源默认全部停用、IMAP 全程只读（`EXAMINE` + `BODY.PEEK`，不标记已读、不改动邮箱）
>   - **不采集任何个人联系方式**：检测到姓名/手机号/邮箱只记录类型标记，字段与摘要均不含原值，界面引导用户点原公告自行查看
>   - 采集结果一律先入**暂存区**，人工审核后才写正式项目库（未经确认绝不入库）
>   - **离线可用性不受影响**（验收 3.4 专项验证）：未启用任何来源时，采集模块启动与访问均产生**零外部网络请求**

---

## 0. 已确认的决策清单

| # | 决策项 | 结论 |
|---|---|---|
| 1 | 使用场景 | 仅本机单机使用，双击 .bat 自动打开浏览器 |
| 2 | 数据存储 | **不使用云数据库**，全部数据存放于本机 `D:\BJXT\data\` |
| 3 | 账务深度 | 完整做：合同金额 + 回款计划 + 实收流水 + 自动算欠款 + 逾期提醒 |
| 4 | 使用规模 | 个人自用，客户 500 个以内，无多人协作 |
| 5 | 登录 | 无需登录、无账号体系 |
| 6 | 附件 | 支持上传文件，支持图片/PDF 在线预览 |
| 7 | 备份 | 仅本机，但保留两份副本到不同文件夹 |
| 8 | 导入导出 | 真 Excel（.xlsx），本地放置一个表格库文件，运行时完全离线 |
| 9 | 待办 | 独立「待办中心」模块 + 首页今日视图 |
| 10 | 安全 | 回收站（软删除）+ 操作日志，两者都做 |
| 11 | **自定义字典** | 下游行业、客户主体类型等 18 类选项**均可在软件内手动新增/修改/删除**，无需改代码、无需改数据库 |
| 12 | **数据录入方式** | 手动录入 + Excel 导入（**不含任何自动采集/爬虫功能**） |
| 13 | **首页分布地图** | 新疆离线矢量地图展示客户分布，可缩放，地理形状与真实地理位置相符 |
| 14 | **无个人信息采集** | 软件不内置任何爬虫；不自动采集任何个人姓名、手机号等个人信息 |

## 1. 技术方案（零依赖 · 完全离线）

| 层 | 选型 | 说明 |
|---|---|---|
| 运行时 | Node.js v24.21.0（本机已具备） | 无需安装额外环境 |
| 数据库 | SQLite 单文件，使用 Node 内置 `node:sqlite` | **无需 npm install、无需编译原生模块** |
| 后端 | Node 内置 `http` 模块 + 自写轻量路由 | 不引入 express 等框架，永不因依赖升级损坏 |
| 前端 | Vue 3 全局构建版（本地 vendor 目录） + 原生 CSS | 零构建步骤，改完刷新浏览器即生效 |
| Excel | SheetJS 单文件版（本地 vendor 目录） | 仅开发时下载一次，运行时不联网 |
| 启动 | `启动.bat` / `停止.bat` | 重复双击只打开页面，不会启动第二个进程 |

**目录规划**

```
D:\BJXT\
├── 启动.bat                双击启动
├── 停止.bat                停止服务
├── 客户管理系统.url         桌面快捷方式用
├── server\                 仅服务端代码
│   ├── server.js           入口：HTTP 服务 + 路由 + 静态文件
│   ├── db.js               数据库连接、建表、结构迁移
│   ├── routes\             各模块 API
│   └── map\                坐标转换（GCJ-02 / BD-09 → WGS84）
├── web\                    全部前端静态资源（由 server 统一提供）
│   ├── index.html
│   ├── css\  js\
│   └── vendor\             第三方库本地化：vue.js / echarts.js / xlsx.js
│       └── map\xinjiang\   新疆行政区划 GeoJSON（本地）
└── data\                   ★ 全部数据（拷走此文件夹 = 完整迁移）
    ├── crm.db              SQLite 数据库
    ├── attachments\        附件存储（按 年\月 分目录）
    └── backups\            自动备份（保留最近 30 份）
        └── mirror\         第二份副本（滚动保留 7 份）
```

**为什么不用云数据库（结论记录）**：本方案已用「本地 + 双份自动备份」覆盖数据安全需求，同时获得秒级响应、断网可用、零月费、零凭据泄露风险的收益。数据访问层做了隔离封装，将来若需要多设备访问，改动范围有限。

## 2. 数据库设计（13 张表）

通用约定：所有表含 `id`（自增主键）、`created_at`、`updated_at`；需软删除的表含 `deleted_at`（为空表示未删除）。

### 2.1 `customers` 客户主表

**按 8 个区块分组。**表单中「基础信息」常显，其余 7 个区块折叠，按需展开填写（个人自用不必全部填满）。

**① 基础信息（常显）**

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| name | TEXT | ✔ | 客户全称（工商注册全称，用于投标与合同） |
| short_name | TEXT | ✔ | 简称（列表显示用，建议必填，否则长名字列表页放不下） |
| type | TEXT | ✔ | 客户主体类型（字典）——**决定后续跟进方式** |
| industry | TEXT | ✔ | 下游行业（字典：石油/化工/电力/冶金…） |
| source | TEXT | | 客户来源（字典） |
| level | TEXT | | 等级 A/B/C |
| status | TEXT | ✔ | 状态，默认「潜在」 |
| owner | TEXT | | 归属业务员（个人自用默认本人，预留字段） |
| tags | — | | 标签（多对多，见 2.3） |

**② 联系方式**

| 字段 | 类型 | 说明 |
|---|---|---|
| phone | TEXT | 公司总机 |
| fax | TEXT | 传真（工业客户仍有在用） |
| website | TEXT | 网址 |
| email | TEXT | 公司邮箱 |
| wechat | TEXT | 微信 |
| credit_code | TEXT | 统一社会信用代码（投标、开票、查资质用） |

**③ 地址信息**

| 字段 | 类型 | 说明 |
|---|---|---|
| province / city / district | TEXT | 省 / 市 / 区 |
| address | TEXT | 详细地址（发货与拜访用） |
| zip_code | TEXT | 邮编 |

**④ 企业概况**

| 字段 | 类型 | 说明 |
|---|---|---|
| enterprise_nature | TEXT | 企业性质（央企/国企/民营/外资/合资/上市公司/政府机构） |
| parent_group | TEXT | 所属集团——**央企二级/三级单位必须记，投标主体和集团口径不同** |
| scale | TEXT | 企业规模（大型/中型/小型/微型） |
| founded_at | DATE | 成立日期 |
| employees | TEXT | 员工人数区间 |
| legal_person | TEXT | 法定代表人 |
| is_listed | INTEGER | 是否上市（0/1） |

**⑤ 行业属性（阀门业务专用）**

| 字段 | 类型 | 说明 |
|---|---|---|
| purchase_mode | TEXT | 采购模式（终端直采/EPC总包配套/设计院上图/贸易商分销/框架协议/年度招标） |
| end_user | TEXT | **最终用户**——经销商/工程公司建档时必填，你的阀门最终装到哪个厂 |
| design_institute | TEXT | 关联设计院——**上图是阀门行业的关键打法，单独记** |
| epc_contractor | TEXT | 关联工程公司 / EPC 总包 |
| valve_types | TEXT | 关注/常用阀门类型（多选：球阀/闸阀/截止阀/止回阀/蝶阀/调节阀/安全阀/疏水阀等） |
| drive_mode | TEXT | 常用驱动方式（手动/气动/电动/液动/电液联动） |
| body_material | TEXT | 常用阀体材质（碳钢WCB/304/316L/双相钢/合金钢/铸铁/铜/塑料） |
| pressure_rating | TEXT | 常用压力等级（PN16/PN25/PN40/PN64/PN100/Class150/300/600/900/1500/2500） |
| size_range | TEXT | 常用口径范围（如 DN50~DN600） |
| design_standard | TEXT | 常用设计标准（GB/API/ANSI/ASME/DIN/JIS/EN） |
| connection_type | TEXT | 连接方式（法兰/对焊/螺纹/承插焊/对夹） |
| cert_required | TEXT | 认证要求（多选，见下「资质合规」） |

**⑥ 采购与商务条件**

| 字段 | 类型 | 说明 |
|---|---|---|
| annual_demand | REAL | 年需求量预估（万元/年）——**用于排优先级** |
| purchase_cycle | TEXT | 采购周期（月度/季度/半年/年度/项目制随机） |
| account_period | TEXT | 账期（款到发货/月结30/60/90天/货到付款/预付+尾款） |
| warranty_ratio | REAL | 质保金比例（%）——阀门行业通常是 5%~10% |
| warranty_months | INTEGER | 质保期（月），常见 12/18/24 |
| payer | TEXT | 付款方（客户本身/集团财务/项目业主） |
| tender_platform | TEXT | 常用招标平台（中石油/中石化/中海油/国家能源/中招联合/中国采招网/其他） |

**⑦ 资质合规**

| 字段 | 类型 | 说明 |
|---|---|---|
| qualification | TEXT | 客户资质要求（多选：特种设备制造许可证TS/API 6D/API 608/CE/PED/防爆认证/SIL/防火认证/其他） |
| has_ts_license | INTEGER | 是否要求 TS 特种设备许可证（0/1） |
| has_explosion_proof | INTEGER | 是否要求防爆认证（0/1，化工石化常见） |
| quality_grade | TEXT | 客户分级/信用评级（如中石油一级供应商、中石化入网单位） |
| supplier_code | TEXT | 客户方供应商编码——**入网后必有，投标和订单都要填** |
| credit_rating | TEXT | 我方对客户的信用评级（优/良/一般/差/有欠款） |

**⑧ 业务关系与统计（多为自动字段）**

| 字段 | 类型 | 说明 |
|---|---|---|
| introducer | TEXT | 介绍人 / 关键引荐人——**工业客户圈子里这个信息最值钱** |
| competitor | TEXT | 主要竞争对手（该客户目前在用哪个品牌） |
| customer_since | DATE | 合作起始日期 |
| last_order_at | DATE | 最近下单日期 |
| next_follow_at | DATETIME | **下次跟进时间（提醒核心字段）** |
| last_follow_at | DATETIME | 最近跟进时间（自动更新） |
| follow_count | INTEGER | 跟进次数（自动累计） |
| deal_amount | REAL | 累计成交额（由项目自动汇总） |
| remark | TEXT | 备注 |
| deleted_at | DATETIME | 软删除标记 |

索引：`name`、`short_name`、`phone`、`status`、`type`、`industry`、`next_follow_at`、`supplier_code`、`deleted_at`

> **关于字段数量**：字段听起来多，但 SQLite 里没有任何成本，真正影响体验的只是表单布局。我的处理方式：录入时只要求填 4 个必填项（名称/简称/主体类型/行业）就能保存，其余 7 个区块折叠隐藏，跟进过程中逐步补全。列表页只显示 8~10 列，其余在详情页看。
>
> **v1.4 变更**：已按确认结果移除「注册资金」字段，客户表可写字段由 59 个减为 58 个。

### 2.2 `contacts` 联系人（一客户多人）

| 字段 | 类型 | 说明 |
|---|---|---|
| customer_id | INTEGER | 外键 → customers |
| name | TEXT ✔ | 姓名 |
| position | TEXT | 职位（字典：总经理/采购经理/采购员/技术工程师/设备主管/项目经理/设计选型/库管/财务/其他） |
| department | TEXT | 所属部门（采购部/技术部/设备部/工程部/生产部/财务部） |
| mobile | TEXT | 手机号 |
| wechat / email / phone | TEXT | 微信 / 邮箱 / 座机 |
| is_decision | INTEGER | 是否决策人（0/1） |
| is_primary | INTEGER | 是否主联系人（0/1，每客户唯一） |
| influence | TEXT | 影响力（关键决策/技术把关/推荐影响/一般对接）——**工业客户里技术工程师常有一票否决权** |
| birthday | DATE | 生日（用于提醒） |
| remark | TEXT | 备注 |
| deleted_at | DATETIME | 软删除 |

### 2.3 `tags` 标签 / `customer_tags` 关联

- `tags`：`name`、`color`、`sort`
- `customer_tags`：`customer_id`、`tag_id`（多对多）

### 2.4 `followups` 跟进记录

| 字段 | 类型 | 说明 |
|---|---|---|
| customer_id | INTEGER ✔ | 所属客户 |
| project_id | INTEGER | 可关联项目（可空） |
| followed_at | DATETIME ✔ | 跟进时间，默认当前 |
| method | TEXT | 电话 / 微信 / 上门拜访 / 邮件 / 短信 / 会议 / 其他 |
| content | TEXT ✔ | 跟进内容 |
| result | TEXT | 结果（字典，可自定义） |
| next_plan | TEXT | 下次计划 |
| next_at | DATETIME | 下次跟进时间（保存后同步写入客户的 next_follow_at） |

### 2.5 `projects` 项目

| 字段 | 类型 | 说明 |
|---|---|---|
| name | TEXT ✔ | 项目名称（建议格式：客户简称+装置/项目名+年份） |
| customer_id | INTEGER ✔ | 所属客户 |
| stage | TEXT ✔ | 阶段，默认「信息收集」（14 个阶段见第 3 节 C 组） |
| progress | INTEGER | 进度 0–100 |
| end_user | TEXT | 最终用户（经销商/EPC 项目必填） |
| design_institute | TEXT | 设计院（涉及上图时必填） |
| valve_needs | TEXT | 项目阀门需求（多选：类型/口径/压力/材质，多个值逗号分隔） |
| quantity | INTEGER | 数量（台/套） |
| contract_amount | REAL | 合同金额（未签约填 0） |
| signed_at | DATE | 签约日期 |
| bid_date | DATE | 投标日期 |
| bid_result | TEXT | 投标结果（未投标/已投标待开标/已中标/未中标/已废标） |
| win_rate_note | TEXT | 中标/失标原因（价格/技术/交期/关系/资质）——**复盘用，避免重复踩坑** |
| start_date / end_date | DATE | 开始 / 预计结束日期 |
| delivery_date | DATE | 合同交货期 |
| owner | TEXT | 负责人（默认本人） |
| received_amount | REAL | 已回款（**派生缓存，由 payments 自动汇总，不可手填**） |
| payment_status | TEXT | 回款状态（未开始/部分回款/已结清，自动计算） |
| remark | TEXT | 备注 |
| data_origin | TEXT | 数据来源（手动录入 / Excel 导入） |
| deleted_at | DATETIME | 软删除 |

派生字段（不入库，实时计算）：
- `欠款 = contract_amount − received_amount`
- `是否逾期 = 存在 status='计划' 且 plan_date < 今天 且未核销的记录`

### 2.6 `payments` 回款（计划与实收共用一表）

| 字段 | 类型 | 说明 |
|---|---|---|
| project_id | INTEGER ✔ | 所属项目 |
| customer_id | INTEGER | 冗余存一份，便于按客户统计 |
| type | TEXT ✔ | `计划` = 计划收款；`实收` = 实际收款 |
| amount | REAL ✔ | 金额 |
| plan_date | DATE | 计划收款日（type=计划时必填） |
| actual_date | DATE | 实际收款日（type=实收时必填） |
| method | TEXT | 收款方式（银行转账/现金/微信/支付宝/支票/其他） |
| plan_id | INTEGER | 实收记录可选关联到某条计划（用于核销） |
| voucher | TEXT | 凭证（关联附件） |
| remark | TEXT | 备注 |

**资金计算规则**（全部自动，不需要人工填欠款）
```
已回款额 = Σ payments where type='实收'
欠款     = 合同金额 − 已回款额
逾期金额 = Σ payments where type='计划' 且 plan_date < 今天 且未被核销
回款率   = 已回款额 ÷ 合同金额
```

### 2.7 `tasks` 待办事项

| 字段 | 类型 | 说明 |
|---|---|---|
| title | TEXT ✔ | 待办标题 |
| customer_id / project_id | INTEGER | 可关联客户或项目（均可空） |
| due_at | DATETIME | 截止时间 |
| priority | TEXT | 高 / 中 / 低 |
| status | TEXT | 待办 / 已完成 |
| done_at | DATETIME | 完成时间 |
| source | TEXT | 来源：手动 / 跟进计划 / 回款计划（自动生成） |
| remark | TEXT | 备注 |

### 2.8 `attachments` 附件

| 字段 | 类型 | 说明 |
|---|---|---|
| owner_type | TEXT ✔ | customer / project / followup / payment |
| owner_id | INTEGER ✔ | 关联对象 ID |
| file_name | TEXT ✔ | 原始文件名 |
| file_path | TEXT ✔ | 相对路径（年\月\uuid.ext） |
| file_size | INTEGER | 字节 |
| mime_type | TEXT | 用于判断是否可预览 |
| category | TEXT | 分类：合同/报价单/方案/资质/凭证/其他 |
| remark | TEXT | 备注 |

支持预览：`image/*`（jpg/png/gif/webp）、`application/pdf`；其余类型提供下载。

### 2.9 `activity_logs` 操作日志

| 字段 | 说明 |
|---|---|
| entity_type / entity_id | 操作对象（客户/项目/回款/待办…） |
| action | create / update / delete / restore |
| summary | 人类可读摘要，如「把客户等级从 B 改为 A」 |
| detail | 变更前后 JSON |
| created_at | 时间 |

### 2.10 `dict` 字典 · `settings` 设置

- `dict`：`category`（customer_type/industry/source/stage/follow_method/result/payment_method）、`value`、`color`、`sort`、`enabled`
- `settings`：键值对，存提醒提前天数、备份路径、每页条数、主题、公司名等

### 2.11 `region` 行政区划（地图模块用）

| 字段 | 说明 |
|---|---|
| code | 行政区划代码 |
| name | 名称（地区/自治州/市/县） |
| level | 层级：省 / 地州 / 县市 |
| parent_code | 上级代码 |
| lng / lat | 中心点经纬度（**WGS84**） |
| geojson_path | 本地边界文件路径（`web/vendor/map/xinjiang/*.json`） |
| customer_count | 客户数（统计缓存） |

## 3. 下拉选项初始值（阀门行业版 · 共 18 类）

> 已按阀门/流体控制行业的实际业务重做。关注维度：下游行业、客户主体类型（决定打法）、采购模式、技术参数、投标资质。

**A. 客户画像类**

| 类别 | 初始选项 |
|---|---|
| 下游行业 | 石油、化工、电力、冶金、水处理、船舶、造纸、制糖、食品饮料、制药、空分、天然气、煤化工、LNG、热力供暖、环保脱硫脱硝、新能源、核电、矿山、纺织印染、水泥建材、其他 |
| 客户主体类型 | 终端用户、设计院、工程公司/EPC总包、贸易商/经销商、阀门厂家OEM配套、市政水务单位、其他 |
| 采购模式 | 终端直采、EPC总包配套、设计院上图、贸易商分销、框架协议、年度招标、电商平台 |
| 企业性质 | 央企、国企、民营企业、外资企业、合资企业、上市公司、政府机构、事业单位、其他 |
| 客户来源 | 老客户介绍、朋友推荐、设计院推荐、工程公司配套、展会（如中国国际流体机械展/阀门世界）、B2B平台（阿里巴巴/慧聪/中国供应商）、电话开发、网络推广、主动上门、招投标、其他 |

**B. 阀门产品参数类**（用于记录客户的技术偏好与询价范围）

| 类别 | 初始选项 |
|---|---|
| 阀门类型 | 球阀、闸阀、截止阀、止回阀、蝶阀、调节阀、安全阀、疏水阀、减压阀、隔膜阀、旋塞阀、柱塞阀、排污阀、仪表阀、其他 |
| 驱动方式 | 手动、气动、电动、液动、电液联动、气液联动、涡轮传动 |
| 阀体材质 | 碳钢 WCB、不锈钢 304、不锈钢 316、不锈钢 316L、双相钢 2205、双相钢 2507、合金钢 WC6/WC9、低温钢 LCB、铸铁、球墨铸铁、铜、衬氟、塑料 PVC/PP/CPVC、钛材、其他 |
| 压力等级 | PN10、PN16、PN25、PN40、PN64、PN100、PN160、Class150、Class300、Class600、Class900、Class1500、Class2500、10K、20K |
| 公称口径 | DN15、DN20、DN25、DN32、DN40、DN50、DN65、DN80、DN100、DN125、DN150、DN200、DN250、DN300、DN350、DN400、DN500、DN600、DN800、DN1000 以上 |
| 设计标准 | GB、JB、HG、API、ANSI/ASME、DIN、JIS、EN、ISO、其他 |
| 连接方式 | 法兰连接、对焊连接、承插焊、螺纹连接、对夹式、卡箍、卡套 |
| 认证要求 | 特种设备制造许可证 TS、API 6D、API 608、API 600、API 609、CE、PED、SIL、防爆认证 ATEX/IECEx、防火认证 API 607/6FA、饮用水卫生认证、其他 |

**C. 商务与流程类**

| 类别 | 初始选项 |
|---|---|
| 账期 | 款到发货、预付30%+发货前付清、预付30%+验收后付清、月结30天、月结60天、月结90天、货到付款、按进度付款 |
| 客户等级 | A 重点客户、B 普通客户、C 潜在客户 |
| 客户状态 | 潜在、跟进中、已报价、已成交、暂停合作、已流失 |
| 项目阶段 | 信息收集、初步接洽、技术交流、方案选型、询价报价、投标/议价、已中标/已签约、生产执行、发货交付、安装调试、验收结项、质保期内、已暂停、已终止 |
| 跟进方式 | 电话、微信、邮件、上门拜访、技术交流、参加展会、投标/开标、客户来访考察、寄送样品/资料、其他 |
| 跟进结果 | 有意向、需再跟进、已技术交流、已选型、已报价、已投标、待决策、已中标、已成交、价格无优势、技术不满足、暂搁置、无需求 |
| 收款方式 | 银行转账、承兑汇票、电汇、现金、微信、支付宝、信用证 L/C、其他 |

**D. 人员与联系人**

| 类别 | 初始选项 |
|---|---|
| 联系人职位 | 总经理、副总经理、采购经理、采购员、技术总工、技术工程师、设计选型工程师、设备部主管、项目经理、工程部经理、库管、财务、其他 |
| 联系人影响力 | 关键决策、技术把关、推荐影响、一般对接 |

以上全部可在「功能设置 → 字典管理」中增删改、排序、配色。**每类初始值都可随时修改，不影响已录入数据**（改的是选项列表，不是历史记录的值）。

### 3.1 自定义字典（你明确要求的能力，独立列为硬性验收项）

以下要求适用于**全部 18 类字典**，尤其是「下游行业」与「客户主体类型」：

| 能力 | 说明 |
|---|---|
| 新增自定义值 | 在「功能设置 → 字典管理」中，任选一类 → 点「新增」→ 填名称、选颜色、定排序 → 保存后**该选项立即出现在所有相关表单下拉框中**，无需重启软件、无需改代码 |
| 修改 / 重命名 | 改名后**历史数据同步显示新名称**（字典用 ID 关联，不是存文本），不会出现「改名后老数据变成空白」 |
| 停用 / 删除 | **停用**：下拉框不再出现，但历史数据保留显示（推荐用这个）；**删除**：需二次确认，已引用的数据自动归入「未分类」 |
| 排序与配色 | 上下移动调整顺序，可指定颜色（用于列表标签与地图配色） |
| 导入导出字典 | 支持把手动添加的字典导出备份，换电脑时一键导入 |
| 界面内联新增 | 在客户表单里填写行业时，下拉框底部直接有「+ 新增行业」，输入即建，**不用跳转到设置页** |

**典型用法举例**（阀门行业常见需要你手动补的）：
- 下游行业新增：氢能、储能、半导体、光伏、锂电、汽车制造、航空航天、军工、盐化工、氯碱、氟化工、精细化工
- 客户主体类型新增：维保服务商、代理商（区域独家）、第三方检测机构、科研院所、行业协会、融资租赁公司
- 阀门类型新增：刀闸阀、陶瓷阀、衬胶阀、管夹阀、料浆阀、真空阀、低温阀、轨道球阀
- 认证要求新增：API 6A、API 594、API 598、NACE MR0175、TA-Luft、SIL3

## 4. 页面结构与交互

### 4.0 全局框架
- 左侧固定导航 4 项：**首页总览 / 客户管理 / 项目管理 / 待办中心**，底部「功能设置」
- 顶栏：全局搜索框（`Ctrl+K`，搜客户名/联系人/手机号/项目名）+ 主题切换
- 新增/编辑统一使用**右侧抽屉表单**，保存后不跳页、列表原地刷新

### 4.1 首页总览
1. **四张数字卡**：客户总数（含本月新增）｜进行中项目（含合同总额）｜本月回款（含环比）｜今日待跟进（含逾期数，红色）
2. **今日 + 逾期待跟进客户**（左上，最重要）：客户名、主体类型、下游行业、上次跟进、已隔天数，行内按钮「记跟进」
3. **临期 / 逾期回款计划**（右上）：项目名、客户、计划日期、金额、逾期天数标红
4. **招投标日历**（新增，阀门行业必需）：按开标日期排序，显示 `投标日期 · 开标日期 · 保证金退还日`；投标日与开标日 7 天内标黄、已过期未填结果标红
5. **新疆客户分布地图**（新增，详见第 10 节）：新疆地州级色阶地图，颜色深浅 = 客户数量，可缩放、可下钻到县市、可点击查看该区域客户清单
6. **图表**：近 6 个月签约额与回款额趋势（柱线组合）、项目阶段分布（环形，14 个阶段）、**下游行业成交额占比**（阀门行业最有价值的分析维度）、客户转化漏斗
7. **今日待办**：勾选即完成
8. **最近动态**：取操作日志最近 10 条

### 4.2 客户管理
**列表页**：关键词搜索｜筛选（主体类型 / 下游行业 / 状态 / 等级 / 来源 / 采购模式 / 企业性质 / 认证要求 / 标签 / 地区）｜排序（下次跟进时间·创建时间·成交额·年需求量）｜表格列：客户名、主体类型、下游行业、等级、状态、主联系人、手机、下次跟进、累计成交额、操作。批量操作：打标签、改状态、删除。
常用快捷筛选（阀门口径）：**我的重点客户（A 级）**、**只看向上图的（设计院）**、**有欠款的客户**、**超 30 天未跟进**。
默认排序：按「下次跟进时间」升序，逾期行红色高亮置顶。

**详情页**（客户名 + 5 个标签页）
| 标签页 | 内容 |
|---|---|
| 基本信息 | 全部字段；右侧显示成交额、跟进次数、最近跟进 |
| 联系人 | 卡片列表，可设主联系人 / 决策人 |
| 跟进记录 | 时间轴，新增跟进可同时设置下次跟进时间（自动生成待办） |
| 关联项目 | 该客户所有项目 + 合同额 + 回款进度条 |
| 附件 | 拖拽上传，图片/PDF 点击预览 |

### 4.3 项目管理
**列表页**：表格视图 / 看板视图（按阶段分列，卡片可拖动改阶段）；筛选：阶段、客户、回款状态、日期区间；列含合同额、已回款、欠款、回款率、进度。
**详情页**（5 个标签页）
| 标签页 | 内容 |
|---|---|
| 基本信息 | 项目字段 + 欠款金额、回款率、逾期标记 |
| 回款计划 | 计划列表，显示已核销/未核销；「新增计划」；「登记实收」 |
| 实收流水 | 实际收款列表，可关联凭证附件 |
| 待办 | 该项目的待办事项 |
| 附件 | 合同、报价单、方案 |

### 4.4 待办中心
四个视图：**今日** / **本周** / **逾期**（红色） / **已完成**；支持快速添加（一行输入 + 截止时间）、优先级标记、关联跳转。

### 4.5 功能设置
| 分区 | 功能 |
|---|---|
| 字典管理 | **18 类**下拉选项的增删改、排序、配色 |
| 标签管理 | 标签增删改、改色、合并 |
| 提醒规则 | 跟进提醒提前天数、回款提醒提前天数、是否启用生日提醒 |
| 备份与恢复 | 立即备份、备份列表、**从备份恢复**、打开数据目录、自动备份开关与保留份数 |
| 导入导出 | 客户 / 联系人 / 项目 / 回款 Excel 模板下载与导入（含预览、查重、错误报告）；全量导出 xlsx |
| 回收站 | 已删除的客户/项目/跟进，支持还原与彻底删除 |
| 操作日志 | 按对象与时间检索变更记录 |
| 界面偏好 | 主题、每页条数、首页卡片显示项 |

## 5. 关键业务规则

1. **自动回填**：保存跟进记录 → 更新客户 `last_follow_at`、`follow_count`、`next_follow_at`；若设了下次时间 → 自动生成一条待办
2. **自动算钱**：新增/修改/删除实收流水 → 实时刷新项目 `received_amount` 与 `payment_status`（客户 `deal_amount` 同步刷新）
3. **自动待办**：回款计划创建时 → 按提醒天数自动生成待办
4. **查重**：新建客户时若「名称相同」或「手机号相同」→ 弹窗提示并展示已有记录
5. **软删除**：客户/项目/跟进/待办删除进回收站；回收站 30 天后自动清理（可设置）
6. **日志**：所有写操作记录摘要 + 变更前后值
7. **备份**：每天首次启动自动备份一次，`backups\` 保留最近 30 份，`backups\mirror\` 保留最近 7 份（第二份副本，防误删与硬盘故障）
8. **表单校验**：客户名称必填；合同金额与回款金额 ≥ 0 且为数字；计划收款日必填；实收金额不得超过欠款（超出时警告但允许，标记为「超收」）

## 6. 启动与日常使用

- **首次**：双击 `启动.bat` → 自动检测 Node → 建库建表 → 启动服务 → 打开浏览器 `http://127.0.0.1:8899`
- **日常**：双击 `启动.bat` 即可（已在运行则只打开页面）；关闭黑窗口或双击 `停止.bat` 结束
- **备份**：无需操作，全自动；想手动备份点设置里「立即备份」
- **迁移**：整个 `data` 文件夹拷到新机器同目录即可

## 7. 开发计划与验收标准

| 阶段 | 内容 | 预计 |
|---|---|---|
| 1 | 骨架：.bat + 服务 + 建表（13 表）+ 布局导航 + 静态资源本地化 | 0.5 天 ✅ |
| 2 | 客户 + 联系人 + 标签 + 跟进记录 | 2 天 ✅ |
| 3 | 项目（14 阶段 + 招投标字段）+ 回款计划 + 实收流水 + 待办中心 | 1.5 天 |
| 4 | 首页总览 + 18 类字典管理（含自定义/内联新增/改名同步）+ 备份 + Excel 导入导出 + 回收站 + 日志 | 2 天 |
| 5 | 附件上传与预览、打磨、空状态、校验、查重 | 0.5 天 |
| 6 | **新疆分布地图**：本地 GeoJSON 本地化 + 色阶渲染与下钻 + 坐标录入与转换 + 在线核对入口 | 1 天 |
| 7 | **全量验收测试（31 项）+ 出具测试报告 + 修复回归** | 1 天 |
| | **合计** | **约 8.5 天** |

> 说明：招标信息采集模块为**待启用**状态（见附录 A），不计入本表；启用时追加约 2.5 天。

### 7.1 质量门禁（硬性条款，不可协商）

> 本节为用户明确要求，属于交付前提，任何情况下不得变通。

1. **全量执行**：下方 31 项验收测试必须逐条实际执行，**不得跳过任何一条、不得降低任何一条的判定标准、不得以"功能已实现"为由免测**。
2. **一票否决**：31 项中任何一项未通过，即视为**整体未通过**，不予交付。
3. **禁止降标**：不得为了让测试通过而放宽判定条件（例如把"3 秒内打开"改成"10 秒内打开"、把"100 条导入无报错"改成"50 条"）。若某条确实无法达成，**由用户决定如何修改需求，开发方不得自行降低标准**。
4. **证据留存**：每项测试须留下可复核的证据（截图 / 操作记录 / 日志输出），汇总为《验收测试报告》，随交付一并提供，逐项标注「通过 / 未通过 + 证据位置」。
5. **变更回归**：验收通过后如有任何代码修改，**必须重新执行全部 31 项测试**，不得只测改动部分。
6. **测试数据**：每项测试使用的数据须真实录入系统（不少于 500 条客户、20 个项目、30 笔回款的实测数据集），不得用假接口或硬编码数据冒充。
7. **交付内容**：`启动.bat`、完整代码、`data` 目录说明、《验收测试报告》、使用说明。
8. **地图模块合规**：地图边界数据须来自国家地理信息公共服务平台（天地图）等公开合规来源；界面保留审图号标注位；**确认无任何在线地图瓦片依赖**（保证离线可用）。

### 7.2 测试覆盖维度（每项标准须覆盖）

正常流程 · 边界值（空值 / 0 / 极大值 / 超长文本 / 特殊字符）· 异常场景（必填缺失 / 格式错误 / 重复录入 / 断网）· 空状态（无数据时页面不报错）· 数据一致性（增删改后所有关联汇总同步）· 并发（双击启动 / 多标签页同时操作）· 重启后数据持久 · 备份可恢复 · 导入导出中文不乱码 · 附件各种类型 · 断网可用性 · **字典自定义后的全链路生效** · **地图地理准确性、坐标转换与离线渲染**

### 7.3 验收标准（31 项，全部必须通过）

| # | 验收项 | 判定标准 |
|---|---|---|
| 1 | 一键启动 | 双击 `启动.bat` 后 **3 秒内**浏览器自动打开；全程无需任何手工配置 |
| 2 | 首次初始化 | 全新环境下自动建库建 12 张表；二次启动不重复初始化、不报错 |
| 3 | 数据零外流 | 断网后启动与全部功能正常；抓包或核对配置确认**无任何外部网络请求** |
| 4 | 主流程闭环 | 建客户→记跟进→建项目→加回款计划→登记实收，全程数据正确保存并即时刷新 |
| 5 | 金额自动计算 | 登记实收后，项目欠款/回款率/回款状态自动变化，无需手填；客户累计成交额同步 |
| 6 | 逾期提醒 | 计划收款日过期后，首页与项目页出现红色逾期提醒，逾期天数正确 |
| 7 | 跟进自动回填 | 保存跟进记录后，客户最近跟进时间/跟进次数/下次跟进时间自动更新，并生成对应待办 |
| 8 | 阀门行业字段 | 客户表字段全部可保存、可显示、可编辑；8 个区块折叠展开正常；多选类字段（阀门类型/认证要求）存取正确；已移除的「注册资金」不再出现 |
| 9 | 筛选与排序 | 主体类型/下游行业/采购模式/认证要求/标签等全部筛选条件生效且结果准确；快捷筛选（A 级/设计院/有欠款/超 30 天未跟进）结果正确 |
| 10 | 招投标日历 | 投标日/开标日 7 天内标黄；已过开标日未填结果标红；按开标日排序正确 |
| 11 | 项目阶段看板 | 14 个阶段分列显示完整；拖动改阶段后数据与统计同步更新；阶段分布环形图同步 |
| 12 | 软删除与还原 | 删除客户后可在回收站还原，关联的联系人/跟进/项目数据完整恢复，无孤儿数据 |
| 13 | 操作日志 | 所有增删改均被记录，可查到变更前后值；摘要可读（如「客户等级 B → A」） |
| 14 | 表单校验 | 必填缺失/金额填负数或文字/日期格式错误/实收超欠款，均有明确提示；余额与超收处理符合规则 |
| 15 | 查重提示 | 录入同名或同手机号客户时，弹出提示并展示已有记录 |
| 16 | Excel 导出 | 导出的 xlsx 用 Excel 正常打开，**中文不乱码**，数值列可参与计算 |
| 17 | Excel 导入 | 按模板导入 **100 条客户全部成功**，含错误行报告；导入后列表与统计正确 |
| 18 | 附件预览 | 上传图片与 PDF 均可在页面内直接预览；其他类型可下载；大文件（≥10MB）不卡死 |
| 19 | 备份与恢复 | `backups\` 与 `backups\mirror\` 中均存在可用当日备份；**从备份恢复后数据与备份前完全一致** |
| 20 | 端口与重启 | 重复双击 `启动.bat` 不启动第二个进程、不报端口占用；关闭窗口后端口释放，可再次启动 |
| 21 | 数据持久 | 录入数据后关闭程序并重启，全部数据完整存在 |
| 22 | 500 条性能 | 录入 500 条客户后，列表翻页、搜索、筛选均在 **1 秒内**响应 |
| 23 | 空状态 | 无任何数据时，首页/列表/详情页均正常显示引导提示，不出现报错或空白崩坏 |
| 24 | 浏览器兼容 | Edge 与 Chrome 下页面布局与功能均正常，无控制台报错 |
| 25 | **自定义行业** | 在设置页新增自定义下游行业（如「氢能」），保存后**立即出现在客户表单下拉框**中，无需重启；选中后能正常保存并在列表/详情/筛选中显示 |
| 26 | **自定义主体类型** | 同上，新增客户主体类型（如「维保服务商」）后全流程可用 |
| 27 | **字典改名与内联新增** | 字典改名后**历史数据同步显示新名称、不丢值**；停用项从下拉消失但历史数据保留；表单内下拉框底部「+ 新增」可直接建立新选项 |
| 28 | **新疆地图** | 首页正确渲染新疆地图（含各地州完整边界）；**地理形状与真实地理位置相符**（与标准地图比对，边界无明显变形与缺失）；客户分布数量与列表统计一致 |
| 29 | **地图交互** | 鼠标滚轮/按钮缩放、拖拽平移流畅；点击地州下钻到县市；点击区域显示该区客户清单并可跳转客户详情；重置视图按钮可用 |
| 30 | **地图坐标准确性** | 客户地址能正确归属到对应地州与县市；手工录入坐标的客户可在地图上精确定位；**坐标转换正确**（GCJ-02/BD-09 转 WGS84 后落点误差在可视范围内）；「在线地图核对」按钮能正确打开对应位置 |
| 31 | **地图离线可用** | 断网状态下地图正常渲染与交互（纯本地 GeoJSON，无在线瓦片依赖） |

### 7.4 招标采集模块验收标准（v1.5 新增，8 项）

> 采集模块是后续追加的功能，验收标准独立列出，与 §7.3 的 31 项**并行有效**。

| # | 验收项 | 判定标准 |
|---|---|---|
| C1 | 默认关闭与零外流 | 所有来源初始为停用；未启用来源时，软件启动、采集页访问、采集调用均**不产生任何外部网络请求** |
| C2 | 只读保证 | IMAP 全程只使用 `EXAMINE` 与 `BODY.PEEK[]`，**不发送** `STORE`/`EXPUNGE`/`APPEND`/`COPY`/`MOVE`；不把邮件标记为已读、不移动或删除邮件 |
| C3 | 个人信息红线 | 公告中的姓名/手机号/邮箱/身份证**不写入数据库任何字段**；仅记录命中类型标记并在界面提示查看原公告；正文摘要已逐行剔除含个人信息的行 |
| C4 | 来源溯源 | 每条采集结果必须带原文链接（缺失即丢弃）；审核入库的项目带 `source_url`/`source_platform`/`source_notice_id`/`confidence` 溯源字段 |
| C5 | 24 小时调度与增量 | 每 24 小时采集一次；未到时间自动跳过；**内容无变化时整条跳过**（不重写、指纹与更新时间均不变）；内容有变时**只更新变化字段**并写日志记录改了什么 |
| C6 | 人工审核闭环 | 采集结果先入暂存区；未经审核**绝不写入正式项目库**；缺审核动作或缺归属客户均被拒绝；已入库记录不能重复入库、也不能再被标记忽略 |
| C7 | 合规信息 | 界面明确说明「唯一会联网的功能、默认关闭、不抓取网站、不采集个人信息」；来源配置处给出各邮箱授权码获取指引；错误口令、缺失授权码均有明确提示 |
| C8 | 失败与审计 | 单次失败只记日志并提示，不重试轰炸；**连续 3 次失败自动暂停该来源**；审计日志记录采集/更新/审核动作且保留 ≥180 天（实现为 200 天） |

## 9. 首页新疆客户分布地图

### 9.1 需求实现

| 你的要求 | 实现方式 |
|---|---|
| 新疆省份地图 | 地州级矢量地图（14 个地州市：乌鲁木齐、克拉玛依、吐鲁番、哈密、昌吉、博尔塔拉、巴音郭楞、阿克苏、克孜勒苏、喀什、和田、伊犁、塔城、阿勒泰，含自治区直辖县级市） |
| 看到客户分布位置 | 色阶渲染：颜色深浅 = 该地州客户数量；悬停显示「客户数 / 成交额」；点击下钻到县市地图 |
| 地图可以缩放 | ECharts `roam` 支持滚轮缩放 + 拖拽平移；另设「放大 / 缩小 / 重置视图」按钮 |
| 与真实地理位置相符 | 使用官方行政区划边界 GeoJSON（**WGS84 坐标**），形状、相对位置、边界与标准地图一致 |

### 9.2 技术实现（完全本地化，不依赖任何在线地图服务）

- 渲染库：ECharts + GeoJSON 组件，**全部放本地 `web/vendor/`**，断网可用
- 边界数据：新疆地州级 + 县市级 GeoJSON，本地存于 `web/vendor/map/xinjiang/`
- 底图策略：采用**矢量底图**而非在线瓦片。原因：在线瓦片必须联网且需 API Key，会破坏"断网完全可用"这一已确认的验收前提。矢量地图同样满足"缩放 + 地理位置相符"
- **在线核对入口**：需要看街道级细节时，提供「在在线地图打开」按钮（高德/百度），仅在点击时联网，不影响软件离线运行

### 9.3 坐标来源与精确度

| 方式 | 说明 |
|---|---|
| 地址自动归属 | 根据客户「省/市/区」自动定位到地州与县市中心点（`region` 表内置新疆全部区划及中心坐标） |
| 手工精确坐标 | 客户表单增加「经度 / 纬度」字段，可从高德坐标拾取器复制粘贴 |
| 坐标转换 | 内置 GCJ-02（高德/腾讯）与 BD-09（百度）→ WGS84 的转换算法（**纯本地计算**），三种坐标系均可正确落点 |
| 定位校验 | 「在线地图核对」一键打开该坐标对应的在线地图，可人工确认位置是否正确 |

### 9.4 数据呈现

- 地州色阶地图 + 右侧「地州客户数排行」条形榜（与地图联动高亮）
- 点击某地州 → 下钻到县市；点击某县市 → 列出该区域客户清单，可直达客户详情
- 辅助统计：近 6 个月各地州新增客户趋势
- 位置：首页左栏，占一整行；高度约 320px，可点击「放大」全屏查看

### 9.5 合规提示

- 地图用于**个人自用**，不对外发布、不印制、不公开传播
- 边界数据取自国家地理信息公共服务平台（天地图）公开服务，符合国家标准地图的边界表达
- 界面角落保留**审图号标注位**（可配置）。如将来需要对外使用或正式发布，必须使用自然资源主管部门提供的标准地图并标注审图号
- 参考：[新疆维吾尔自治区自然资源厅《登载使用地图规范指南》](https://zrzyt.xinjiang.gov.cn/xjgtzy/tzgg/202011/c0e1f60f85774e1c80f6ba9306ef678f.shtml)、[2023 版标准地图发布信息](https://www.csgpc.org/detail/21952.html)

## 10. 风险与对策

| 风险 | 影响 | 对策 |
|---|---|---|
| 硬盘损坏 / 误删 data 目录 | 数据全失 | 双份自动备份到不同文件夹；建议定期把 `backups\` 拷到移动硬盘 |
| 端口 8899 被占用 | 无法启动 | 启动脚本自动探测并顺延端口，同时用记事本打开配置项供手工修改 |
| 浏览器缓存旧页面 | 改了功能看不到 | 静态资源加版本号参数，启动时自动刷新 |
| 附件占用空间过大 | 磁盘紧张 | 设置单文件上限（默认 50MB），界面显示已用空间 |
| 误操作改错数据 | 数据不准 | 操作日志可追溯 + 回收站可还原 |
| 单机无法多端访问 | 手机上看不了 | 现阶段不需要；数据层已隔离，将来可平滑扩展 |
| **采集合规风险** | 法律风险 | 不适用：本次交付**不含任何采集/爬虫功能**；将来若实现，按附录 A 的合规要求执行 |
| **地图版权与审图合规** | 使用风险 | 采用天地图公开边界数据；个人自用不对外传播；界面保留审图号标注位，对外使用前须改用标准地图 |
| **坐标系统不统一导致落点偏移** | 地图显示不准 | 内置 GCJ-02 / BD-09 → WGS84 转换；提供「在线地图核对」按钮人工校验 |

---

## 附录 A. 招标信息采集（**已按本附录实现并启用** · 见 v1.5 修订记录）

> 本文档 v1.3 曾决策暂不实现，仅保留可行性结论；**v1.5 已按本附录落地开发**，
> 路线选定为 A.6 推荐的「**邮件订阅解析**」（零法律风险、免费、不依赖网站改版）。
> 下文 A.1~A.7 的合规边界与调度规则均已代码级实现；A.8 为原始设计要点，
> 实际实现与它的差异见本节末尾「实现说明」。

### A.1 需求原意
抓取与库中客户相关联的**新项目信息**：项目名称、建设地点、项目金额、涉及行业、总包单位、建设单位、设计院、单位联系方式（有则抓、无则空）、招标时间（有则抓、无则空）。要求过程安全、可靠、不违反中国法律法规。

### A.2 法律边界（有官方明确标准）
国家数据局发布的专家解读《[自动化程序收集公开数据的合法边界](https://www.nda.gov.cn/sjj/zwgk/zjjd/0409/20260409183035544651552_pc.html)》确立了**"四不"原则**，任何采集实现都必须逐条满足：

| "四不"原则 | 落地要求 |
|---|---|
| 不非法侵入他人网络 | 只访问无需登录的公开页面；绝不绕过登录墙、验证码、付费墙 |
| 不干扰网络服务正常运行 | 单源请求间隔 ≥ 8 秒、日请求 ≤ 200、遇 429/503 立即退避并当日停止 |
| 不破坏有效技术措施 | 绝不破解、规避、伪造任何反爬机制 |
| 不损害个人和组织合法权益 | **个人联系方式一律不采集**；尊重 robots.txt 与网站声明；数据仅自用 |

配套参考：《[招标公告和公示数据接口规范（试行）](https://smggzy.sm.gov.cn/smwz/AttachStorage/ebc775d8-6fca-eaa8-5fb6-1b2f0f0ee8c7/%e6%8b%9b%e6%a0%87%e5%85%ac%e5%91%8a%e5%92%8c%e5%85%ac%e7%a4%ba%e6%95%b0%e6%8d%ae%e6%8e%a5%e5%8f%a3%e8%a7%84%e8%8c%83%ef%bc%88%e8%af%95%e8%a1%8c%ef%bc%89.docx)》、《[公共资源交易平台系统数据规范 V2.0](https://www.gov.cn/zhengce/zhengceku/2018-12/31/content_5435038.htm)》。

### A.3 实测数据（2026-02 robots.txt 探测结果）

| 数据源 | 实测 | 处理方式 |
|---|---|---|
| 中国招标投标公共服务平台 | **`Disallow: /`（全站禁止）** | ⛔ 不采集，仅可走官方数据接口（需企业主体申请） |
| 全国公共资源交易平台 | 未声明（404） | 白名单内限频采集 |
| 新疆公共资源交易中心 | 未声明（404） | 白名单内限频采集（新疆本地项目主力来源） |
| 采购与招标网 | 部分目录禁止 | 仅采集允许路径，避开 `/BuserCenter/` `/JsonHandler/` `/help/` |

### A.4 个人联系方式红线（已确认采纳）
- ✅ 可采集：项目名称、建设地点、金额、行业、建设单位/总包单位/设计院**单位名称**、招标时间、公告编号、原文链接
- ⛔ 不采集：**个人姓名、手机号、邮箱**。公告中出现时仅在界面提示「原公告含联系人信息，请点击来源链接自行查看」，由你通过合法业务渠道自行获取后录入
- ⚠️ 公告中以单位名义公开的机构电话/邮箱，仅在正文明确公开时采集并标注来源

> 原理：对公告中"公开的个人信息"再行收集受《个人信息保护法》约束，需在合理范围内且不得超出原公开目的使用。

### A.5 三条铁律（实现时必须代码级强制）
1. **白名单制**：数据源须在白名单内，否则引擎直接拒绝执行
2. **默认关闭**：预置数据源初始全部禁用，必须手动开启
3. **robots.txt 实时校验**：每次采集前重新拉取，`Disallow: /` 即拒绝执行并写审计日志

### A.6 路线选择

| 路线 | 合规性 | 门槛 |
|---|---|---|
| 官方数据接口 | ★★★★★ | 需企业/个体工商户主体实名申请 |
| 商业授权数据服务商 | ★★★★★ | 需付费 |
| 白名单限频采集 | ★★★★ | 免费，覆盖率略低 |
| **邮件订阅解析** | ★★★★★ | 免费、**零法律风险**，推荐作为首选或兜底 |

**推荐组合**：邮件订阅解析（零风险、免费）+ 白名单限频采集（补充覆盖）；如需更大覆盖再走官方接口或商业授权。

### A.7 采集调度规则（**已确认：每 24 小时采集一次**）

| 项目 | 规则 |
|---|---|
| 采集频率 | **每 24 小时采集一次** |
| 增量策略 | **有则更新，无则不更** —— 抓不到新公告就保持原样：不重写、不产生重复记录、不改动已有数据的更新时间 |
| 去重键 | 公告编号（`notice_id`）。同一公告再次出现时按「内容有变才覆盖」处理；完全一致则整条跳过 |
| 变更判定 | 只对**内容确实发生变化的字段**执行更新，并写入操作日志（可追溯改了什么） |
| 执行时机 | 每日首次启动软件时自动触发；若当日已采集过则跳过（避免频繁开关软件造成重复抓取） |
| 未开机补偿 | 若某日未开机，次日启动时执行一次，**不累积补跑** |
| 手动触发 | 采集页提供「立即采集」按钮，但同样受 24 小时节流与限频约束 |
| 失败处理 | 单次失败只记日志并在界面提示，不做重试轰炸；连续 3 次失败自动暂停该数据源 |

### A.8 实现要点（若将来启动）

采集引擎七步流程（robots 检测 → 配置校验 → 限频拉取 → 解析抽取 → 个人信息检测 → 客户匹配打分 → 写入暂存区）→ **按 A.7 规则做增量判定** → 人工审核 → 写入正式项目库。

- 采集结果**未经人工确认绝不入正式库**
- 每条数据必须带来源 URL（缺失即丢弃）
- 审计日志保留 ≥ 180 天
- 新增 `collect_sources` / `collect_staging` / `collect_logs` 三张表
- 项目表增加 `source_url` / `source_platform` / `collected_at` / `source_notice_id` / `confidence` 五个溯源字段
- 界面在采集任务处显示「上次采集时间 / 下次采集时间 / 本次新增与更新条数」

预计工作量约 **2.5 天**（含调度与增量判定逻辑）。

### A.9 实现说明（v1.5 实际落地）

**路线**：邮件订阅解析（A.6 推荐方案），**不实现任何网页抓取**。
这条路线下 A.3 的 robots.txt 限制、A.5 的限频要求都不再适用（软件不访问任何网站），
但 A.4 的个人信息红线与 A.7 的调度规则**全部保留并代码级强制**。

**为什么要手写 IMAP 与 MIME**：本项目坚持零依赖（Node 内置模块 + 前端全本地），
而 Node 没有内置 IMAP；为不破坏「断网可装可用」，基于内置 `tls` 手写了 IMAP 子集，
并自研 MIME 解析（multipart / base64 / quoted-printable / GBK / RFC 2047）。

**与 A.8 原设计的差异**：

| A.8 原设计 | 实际实现 | 原因 |
|---|---|---|
| 七步流程含 robots 检测、限频拉取 | 改为：配置校验 → 24h 调度 → 只读拉取 → 解析抽取 → 个人信息检测 → 客户匹配 → 增量写暂存 | 不走网页抓取，robots 与限频无对象；改为对邮箱的**只读保证**（`EXAMINE` + `BODY.PEEK`） |
| 新增 3 张表 | 一致：`collect_sources` / `collect_staging` / `collect_logs` | — |
| 项目表 5 个溯源字段 | 一致 | — |
| 审计日志 ≥180 天 | 保留 **200 天**（`LOG_KEEP_DAYS`），启动采集时自动清理过期 | 留余量 |
| 采集类型可扩展（公开接口/授权服务商/白名单抓取） | 仅实现 `subscription`；其它类型即使手工写入库也**拒绝执行** | 铁律一：白名单制 |
| — | 新增：**连续 3 次失败自动暂停该来源** | A.7 失败处理要求 |
| — | 新增：**按关键词与地区过滤**（仅新疆 + 仅阀门类词） | 用户确认的采集范围 |

**实现位置**：

| 文件 | 职责 |
|---|---|
| `server/collect/imap.js` | 零依赖 IMAP 客户端（TLS、SASL PLAIN、modified UTF-7、只读取信） |
| `server/collect/mail.js` | MIME 解析（头字段、multipart、传输编码、字符集、HTML 转文本） |
| `server/collect/extract.js` | 招标字段抽取、个人信息检测、地区/关键词过滤、客户匹配打分、内容指纹 |
| `server/collect/service.js` | 采集引擎、24h 调度、增量判定、暂存区、审核入库、审计日志 |
| `server/routes/collect.js` | 接口层 |
| `web/js/pages/collect.js` | 采集页（待审核 / 采集来源 / 采集记录） |

**测试**：`tools/test-collect-*.js` 五个套件共 **188 项断言**，
配 `tools/make-mail-fixtures.js`（8 封多平台形态邮件夹具）与
`.fixtures/mock-imap.js`（本地模拟 IMAP 服务器，跑真实协议、不接触真实邮箱）。

**离线承诺的处理**：采集是软件里唯一会联网的功能，独立成子模块且**默认关闭**；
关闭状态下启动与访问均零外部请求（验收项 3.4 专项验证），原有「断网可用」验收标准不受影响。

---
