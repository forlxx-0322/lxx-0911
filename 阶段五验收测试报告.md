# 阶段五验收测试报告

> 项目：客户管理系统（阀门行业版）
> 阶段：**阶段五 · 附件上传与预览**
> 测试日期：2026-02
> 测试方式：API 测试 38 项 + 真实浏览器交互测试 14 项（含浏览器内构造文件拖拽上传）
> 结论：**阶段五验收通过；全项目累计 289 项测试全部通过**

---

## 一、本阶段交付内容

| # | 交付物 | 说明 |
|---|---|---|
| 1 | 附件服务 `server/services/attachment.js` | 上传、列表、下载、删除、用量统计、无主文件清理 |
| 2 | 附件路由 `server/routes/attachment.js` | 9 个接口，含原始文件流式响应 |
| 3 | **Range 请求支持** `server/server.js` `sendFile()` | PDF 在浏览器内翻页预览必需；含 ETag 条件请求 |
| 4 | 附件面板 `web/js/components-attachment.js` | 拖拽上传、分类、图片/PDF 预览、缩放、下载、删除、编辑 |
| 5 | 附件与存储设置页 `web/js/settings-attachment.js` | 用量统计、上限配置、清理无主文件、备份提示 |
| 6 | 客户详情「附件」标签页 | 合同扫描件、营业执照等归到客户名下 |
| 7 | 项目详情「附件」标签页 | 报价单、技术方案、图纸归到项目名下 |

---

## 二、关键技术决策

### 2.1 为什么用 base64 走 JSON 上传，而不是 multipart

解析 `multipart/form-data` 需要引入依赖或手写解析器。本软件的定位是**零第三方依赖 + 单机自用**，而附件上限默认 50MB，走 base64（体积增约 33%）完全可接受。

好处：服务端保持零依赖；与现有请求通道、错误处理、日志完全一致；前端还能用 `FileReader` 边读边报进度。

代价：大文件传输体积增加 33%（本地回环传输，无实际影响）。

### 2.2 为什么必须实现 Range 请求

浏览器内置 PDF 阅读器**不会**一次性下载整个 PDF，而是发 `Range: bytes=0-65535` 之类的分片请求。如果不支持 Range：

- Chrome 里 PDF 预览会一直转圈或直接失败
- 图片能显示（一次性加载），但 PDF 不行

所以 `sendFile()` 完整实现了：
- `Range: bytes=start-end`（正常分片）
- `Range: bytes=-N`（取最后 N 字节）
- 越界返回 `416` + `Content-Range: bytes */total`
- `Accept-Ranges: bytes` 声明
- `ETag` + `If-None-Match` → `304`

### 2.3 安全设计

| 风险 | 措施 |
|---|---|
| 上传可执行文件 | 扩展名白名单（pdf/doc/docx/xls/xlsx/ppt/pptx/图片/txt/csv/压缩包/dwg/dxf），`.exe` 等一律拒绝 |
| 路径穿越 | 文件名禁止含 `/` `\`；磁盘上用 uuid 命名，不暴露原文件名 |
| 读取越权 | 读取时校验路径必须位于 attachments 目录内 |
| 撑爆磁盘 | 单文件大小受设置项控制，前端先拦一次避免白传 |
| 数据库写入失败产生孤儿文件 | 写入数据库失败时回滚已落盘的文件 |
| 中文文件名乱码 | 下载头按 RFC 5987 双重编码（`filename` + `filename*=UTF-8''`） |

---

## 三、API 测试明细（38 项）

### A. 上传（第 1–6、13 项）

| # | 验收项 | 结果 | 实测数据 |
|---|---|---|---|
| 1 | 上传图片附件成功 | ✅ | 70 字节，分类=资质 |
| 2 | 图片被标记为可预览 | ✅ | `previewable=true, is_image=true` |
| 3 | 上传 PDF 并识别为可预览 | ✅ | `is_pdf=true` |
| 4 | 上传到项目，MIME 按扩展名推断 | ✅ | xlsx → spreadsheetml 类型，`previewable=false` |
| 5 | **文件真实落盘且内容与原文件逐字节一致** | ✅ | 磁盘 70 字节，`Buffer.equals()` = true |
| 6 | **存储路径按年月分目录 + uuid 命名** | ✅ | `2026/09/e2a55ed8c5924975848fdc0074c391cb.png` |
| 13 | 上传 1.5MB 大文件成功 | ✅ | 1,572,864 字节 |

### B. 下载与预览（第 7–12 项）

| # | 验收项 | 结果 | 实测数据 |
|---|---|---|---|
| 7 | 下载返回正确 Content-Type 与内容 | ✅ | `image/png`，内容逐字节一致 |
| 8 | 图片使用 inline 内联显示 | ✅ | `inline; filename=...` |
| 9 | PDF 也 inline 且支持 Range | ✅ | `accept-ranges=bytes` |
| 10 | 非预览类型触发下载 | ✅ | `.xlsx` → `attachment;` |
| 11 | **中文文件名按 RFC 5987 编码** | ✅ | `filename*=UTF-8''%E8%90%A5%E4%B8%9A...` |
| 12 | 带 ETag 与 Content-Length | ✅ | |

### C. Range 请求（第 14–18 项）—— PDF 预览的关键

| # | 验收项 | 结果 | 实测数据 |
|---|---|---|---|
| 14 | **Range 请求返回 206 与正确分片** | ✅ | `bytes 0-1023/1572864`，1024 字节，内容逐字节一致 |
| 15 | **中间位置分片正确** | ✅ | 取第 1,000,000~1,000,099 字节，内容一致 |
| 16 | **末尾分片（bytes=-N）正确** | ✅ | 取末 500 字节，内容一致 |
| 17 | 越界 Range 返回 416 | ✅ | `content-range: bytes */1572864` |
| 18 | ETag 条件请求返回 304 | ✅ | 省流量 |

### D. 列表与统计（第 19–21 项）

| # | 验收项 | 结果 | 实测数据 |
|---|---|---|---|
| 19 | 按归属列出附件并汇总大小 | ✅ | 客户下 2 个附件，按分类分组正确 |
| 20 | 客户与项目附件相互隔离 | ✅ | 项目下 2 个，客户下 2 个，互不串 |
| 21 | 用量统计含数据库记录与磁盘实际占用 | ✅ | 记录 4 个 / 磁盘 4 个 |

### E. 校验与安全（第 22–30 项）

| # | 验收项 | 结果 | 实测数据 |
|---|---|---|---|
| 22 | **可执行文件被拒绝** | ✅ | `.exe` → `EXT_NOT_ALLOWED` |
| 23 | 无扩展名被拒绝 | ✅ | `NO_EXT` |
| 24 | **文件名含路径分隔符被拒绝** | ✅ | `../../evil.pdf` → `BAD_FILENAME` |
| 25 | 归属记录不存在时拒绝 | ✅ | `OWNER_NOT_FOUND` |
| 26 | 非法归属类型被拒绝 | ✅ | `BAD_OWNER_TYPE` |
| 27 | 空内容被拒绝 | ✅ | `NO_CONTENT` |
| 28 | **超大小上限被拒绝并提示可调整** | ✅ | 「可在「功能设置 → 提醒与偏好」中调整上限」 |
| 29 | 不存在的附件返回 404 | ✅ | 元数据与文件均 404 |
| 30 | **附件路径穿越被阻止** | ✅ | 编码后的 `../../../data/crm.db` → 404 |

### F. 修改、删除与清理（第 31–37 项）

| # | 验收项 | 结果 | 实测数据 |
|---|---|---|---|
| 31 | 可修改分类与备注 | ✅ | |
| 32 | 非法分类回退为「其他」 | ✅ | |
| 33 | **删除附件同时移除磁盘文件** | ✅ | 删除前存在 → 删除后不存在 |
| 34 | 删除后不再出现在列表 | ✅ | |
| 35 | 删除是软删除（记录可追溯） | ✅ | |
| 36 | **清理无主文件（磁盘有、库里没有）** | ✅ | 清理 1 个，释放 6 B |
| 37 | 常见类型均可上传 | ✅ | docx / dwg / zip / csv / jpg 5/5 |

---

## 四、真实浏览器交互测试（14 项）

**本阶段的浏览器测试有一个特点：在页面里用 `new File()` + `DataTransfer` 构造真实文件对象，通过 `drop` 事件触发上传**——完整走一遍「拖拽 → FileReader 读为 base64 → 上传接口 → 落盘 → 列表刷新」，而不是只调接口。

| # | 验收项 | 结果 | 实测数据 |
|---|---|---|---|
| 1 | 客户详情页新增「附件」标签页 | ✅ | 标签页：基本信息/联系人/跟进记录/关联项目/**附件**/变更记录 |
| 2 | 附件面板渲染（拖拽区/上传按钮/分类/空状态） | ✅ | 空状态文案正确 |
| 3 | **浏览器内拖拽上传图片成功** | ✅ | 「营业执照交互测试xxx.png」出现在列表，提示「已上传 1 个附件」 |
| 4 | 上传时选择的分类生效 | ✅ | 归入「合同」分组 |
| 5 | 上传 PDF 并显示 PDF 标识 | ✅ | 列表 2 个，分组：合同(1) / 资质(1) |
| 6 | **点击预览打开预览层，PDF 用 iframe 内联** | ✅ | src=`/api/attachments/11/file` |
| 7 | **图片预览可缩放** | ✅ | `scale(1)` → `scale(1.5)`，显示 150% |
| 8 | ESC 可关闭预览层 | ✅ | |
| 9 | 附件真实落盘且大小正确 | ✅ | PNG 70B、PDF 395B，均落盘 |
| 10 | 预览用的图片 URL 可直接取回 | ✅ | 内容逐字节一致，`inline` |
| 11 | **删除走确认弹窗并可成功删除** | ✅ | 弹窗文案含「文件将从磁盘移除，此操作不可恢复」 |
| 12 | 项目详情页也有「附件」标签页 | ✅ | 标签页：基本信息/回款计划/实收流水/待办/**附件**/变更记录 |
| 13 | 设置页「附件与存储」面板渲染 | ✅ | 4 项用量统计、上限 50MB、含备份提示 |
| 14 | **全程无未捕获的 JS 错误** | ✅ | 0 条错误 |

---

## 五、本阶段发现并修复的问题

| # | 问题 | 性质 | 处理 |
|---|---|---|---|
| 1 | 阶段四测试断言「设置页渲染 8 个标签页」因新增「附件与存储」而失败 | 测试过期 | 改为「≥8 个且逐个可切换」，后续再加页面不会误报 |
| 2 | 阶段三测试断言「项目详情页渲染 5 个标签页」同样过期 | 测试过期 | 改为「≥5 个且含必需标签页」 |
| 3 | 阶段二测试断言「详情页渲染出 5 个标签页」同样过期 | 测试过期 | 同上；并补充校验必须包含「基本信息」与「联系人」 |
| 4 | 阶段五测试取空状态文案时取到了隐藏标签页（`v-show` 元素仍在 DOM） | 测试不严谨 | 限定只取当前可见的附件面板 |

> 本阶段后端逻辑**一次通过 38 项测试**，未发现功能性缺陷。

---

## 六、全量回归（累计 289 项）

| 测试套件 | 项数 | 结果 |
|---|---|---|
| 阶段一：骨架与运行环境 | 23 | ✅ 23/23 |
| 阶段二：客户管理 API | 45 | ✅ 45/45 |
| 阶段二：前端数据契约 | 19 | ✅ 19/19 |
| 阶段二：浏览器交互 | 11 | ✅ 11/11 |
| 阶段三：项目/回款/待办 API | 43 | ✅ 43/43 |
| 阶段三：浏览器交互 | 11 | ✅ 11/11 |
| 阶段四：总览/设置/备份/导入导出 API | 40 | ✅ 40/40 |
| 阶段四：浏览器交互（含真实下载 Excel） | 15 | ✅ 15/15 |
| **阶段五：附件 API** | **38** | ✅ 38/38 |
| **阶段五：浏览器交互（含真实拖拽上传）** | **14** | ✅ 14/14 |
| 数据库结构迁移 | 10 | ✅ 10/10 |
| **合计** | **289** | **✅ 289/289** |

---

## 七、数据库与文件系统最终状态

| 项目 | 值 |
|---|---|
| 数据库文件 | `data/crm.db`，结构版本 **v2** |
| 数据 | 客户 / 项目 / 回款 / 待办 / 附件 / 日志 均为 0 条（测试数据已清理） |
| 字典 | 22 类 / 237 项 |
| **附件目录** | `data/attachments/` **无残留文件**（测试附件已随记录删除并清理无主文件） |

---

## 八、复现方式

```bat
:: 前置：服务运行中（双击 启动.bat）

node tools\test-phase5-api.js               :: 38 项 附件 API 测试
node tools\test-phase5-ui-interaction.js    :: 14 项 浏览器交互（含真实拖拽上传）
node tools\cleanup-test-data.js --logs      :: 清理测试数据
```

---

## 九、使用说明

| 操作 | 方式 |
|---|---|
| 上传附件 | 客户详情 / 项目详情 → 「附件」标签页 → 拖入文件或点击选择（支持多选） |
| 选择分类 | 上传前在右上角下拉选「合同 / 报价单 / 方案 / 资质 / 凭证 / 图纸 / 其他」 |
| 预览 | 图片与 PDF 点击文件名或「预览」按钮，图片可缩放（也支持 `+` / `-` / `0` / `ESC` 快捷键） |
| 下载 | 点「下载」按钮，任何类型都可下载 |
| 改分类/备注 | 点「编辑」 |
| 调整大小上限 | 功能设置 → 附件与存储 |
| 释放空间 | 功能设置 → 附件与存储 → 清理无主文件 |
| 迁移数据 | 拷贝整个 `data` 文件夹（**注意：附件文件在 data/attachments，不在数据库备份里**） |

---

## 十、待办

| 项目 | 状态 |
|---|---|
| 阶段六：新疆客户分布地图（离线矢量图、14 地州下钻、坐标转换、在线核对） | ⏳ |
| 工业园区标注（技术方案已就绪，待阶段六时确认是否开发） | ⏳ 待定 |
| 阶段七：31 项全量验收测试 + 修复回归 | ⏳ |
