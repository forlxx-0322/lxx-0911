# 阶段六验收测试报告

> 项目：客户管理系统（阀门行业版）
> 阶段：**阶段六 · 新疆客户分布地图**
> 测试日期：2026-02
> 测试方式：API 测试 39 项 + 真实浏览器交互测试 21 项（含 canvas 真实点击下钻）
> 结论：**阶段六验收通过；全项目累计 351 项测试全部通过**
> 说明：**工业园区标注功能经确认不做**，本阶段不含该功能

---

## 一、本阶段交付内容

| # | 交付物 | 说明 |
|---|---|---|
| 1 | **地图数据本地化** `tools/fetch-map-data.js` | 下载新疆地州级 + 县市级边界，生成索引，灌入 region 表 |
| 2 | **坐标转换模块** `server/map/coord.js` | WGS84 ↔ GCJ-02 ↔ BD-09 互转（纯本地算法，不联网） |
| 3 | **地图服务** `server/map/service.js` | 边界读取与缓存、客户分布统计、区域客户明细、最近地州推荐 |
| 4 | **地图路由** `server/routes/map.js` | 8 个接口（状态/区划/分布/明细/边界/最近/转换/在线链接） |
| 5 | **地图组件** `web/js/components-map.js` | 色阶地图、缩放平移、点击下钻、客户清单、在线核对 |
| 6 | **坐标工具** `web/js/components-coord.js` | 粘贴任意地图坐标自动转换、偏移量提示、最近地州推荐 |
| 7 | 客户表新增 `region_code` / `region_name` | 按要求自动归属地州，支持按区域筛选（结构升级至 **v3**） |
| 8 | 首页新增「新疆客户分布」卡片 | 地图 + 地州客户数排行 |
| 9 | 客户列表新增「归属地州」筛选 | 高级筛选中可选任一地州 |

---

## 二、地图数据现状

| 项目 | 值 |
|---|---|
| 数据来源 | 国家地理信息公共服务平台公开边界服务（阿里 DataV 行政区划接口） |
| 地州级 | **24 个**（14 个地州市 + 10 个自治区直辖县级市，含石河子、阿拉尔、图木舒克等兵团市） |
| 县市级 | **96 个**（14 个地州下辖；10 个兵团市无下辖县市，属正常） |
| 文件数 / 体积 | 16 个文件，**929 KB** |
| 坐标系 | **WGS84**（与库中客户坐标一致，无需转换即可对齐） |
| 存放位置 | `web/vendor/map/xinjiang/` |
| 离线能力 | **完全本地**，断网可用（已在浏览器中实测验证） |

> 一级行政区划以 24 个要素呈现的原因：新疆除 14 个地州市外，还有 10 个自治区直辖县级市（兵团城市），它们直接归自治区管辖，在地图上应与地州同级显示。

---

## 三、关键技术决策

### 3.1 为什么用本地 GeoJSON 而不是在线瓦片

验收要求「断网后所有功能正常」。在线瓦片必须联网且需要 API Key，会让地图成为唯一的断网失效点。本地矢量地图同样满足「可缩放 + 地理位置相符」，且：
- 断网完全可用
- 不依赖任何第三方服务与密钥
- 体积仅 929 KB

### 3.2 为什么必须做坐标转换

从高德/百度复制的坐标不能直接用：

| 坐标系 | 使用者 | 与 WGS84 的差距 |
|---|---|---|
| WGS84 | GPS设备、GeoJSON 边界数据 | 基准 |
| GCJ-02 | 高德、腾讯、谷歌中国 | 偏移 200~700 米 |
| BD-09 | 百度 | 偏移更大 |

**实测结果**：乌鲁木齐从高德复制的坐标与真实位置相差 **265 米**——直接使用会把客户标到隔壁厂区。

系统内部统一存 WGS84，前端提供坐标工具自动转换；生成在线核对链接时再把 WGS84 转回各平台自己的坐标系，保证点开链接看到的位置是准的。

### 3.3 归属地州用「自动匹配 + 可手动覆盖」而不是坐标推断

不能用「离哪个地州最近就属于哪个」——客户可能在两地交界处，而行政区划归属与直线距离无关。因此：
- **按地址文本匹配**：优先用「区/县」精确匹配，再退回「市/地区」匹配
- **匹配不到就不猜**：地址为空时 `region_code` 保持为空，单独归入「未归属」统计，不混入其他地州
- **可手动指定**：客户表单有「归属地州」下拉
- 坐标工具的「最近地州」仅作**参考展示**，必须用户点「填入该地州」才写入

---

## 四、API 测试明细（39 项）

### A. 地图数据与区划（第 1–11 项）

| # | 验收项 | 结果 | 实测数据 |
|---|---|---|---|
| 1 | 地图数据已本地化且状态可查 | ✅ | 24 个地州、96 个县市、16 个文件、929 KB |
| 2 | 含自治区直辖县级市（兵团市） | ✅ | 共 24 个（含石河子、阿拉尔等） |
| 3 | 区划清单含省/地州/县市三级 | ✅ | 省 1、地州 24、县市 96，共 121 条 |
| 4 | 地州含中心坐标 | ✅ | 乌鲁木齐中心 (87.617733, 43.792818) |
| 5 | 地州边界文件要素完整 | ✅ | 115 KB，24 个要素 |
| 6 | 边界含 adcode/name/center | ✅ | 前端注册地图必需字段齐备 |
| 7 | **坐标为 WGS84 且落在新疆范围** | ✅ | 经度 73.51~96.39°E，纬度 34.33~49.18°N |
| 8 | 边界可经接口获取 | ✅ | 接口返回 24 个要素 |
| 9 | **县市级边界可下钻获取** | ✅ | 乌鲁木齐下辖 8 个区县，名称正确 |
| 10 | 不存在的边界返回 404 | ✅ | |
| 11 | **全部地州边界已本地化** | ✅ | 14 个地州文件齐备（10 个兵团市无下辖县市，属正常） |

### B. 客户归属与分布统计（第 12–23 项）

| # | 验收项 | 结果 | 实测数据 |
|---|---|---|---|
| 12 | 准备 4 个测试客户 | ✅ | 3 个有地址坐标，1 个无归属 |
| 13 | **保存客户时按地址自动归属地州** | ✅ | 「乌鲁木齐市/天山区」→ 650100 |
| 14 | 按「地区」也能匹配 | ✅ | 「喀什地区/喀什市」→ 653100 |
| 15 | **无地址客户不强行归属（不猜测）** | ✅ | region_code 为空串 |
| 16 | 分布统计按地州聚合 | ✅ | 乌鲁木齐 2 家，喀什 1 家 |
| 17 | **无归属客户单独统计** | ✅ | 未归属 1 家，总数 4 / 有坐标 3 / 无坐标 1 |
| 18 | 分布统计含年需求量汇总 | ✅ | 乌鲁木齐合计 800 万（500+300） |
| 19 | 返回全部 24 个地州（含 0 客户的） | ✅ | 其中 2 个有客户 |
| 20 | 可只统计有坐标的客户 | ✅ | 3 家 |
| 21 | 点击地州获取客户明细 | ✅ | 乌鲁木齐 2 家 |
| 22 | 明细含跟进信息与项目数 | ✅ | 可直接跳转客户详情 |
| 23 | 不存在的区域返回 404 | ✅ | |

### C. 坐标转换（第 24–29 项）—— 地图准不准的关键

| # | 验收项 | 结果 | 实测数据 |
|---|---|---|---|
| 24 | **WGS84 → GCJ-02 偏移量合理** | ✅ | 偏移 265 米（脚本独立复算 265.0 米，一致） |
| 25 | **GCJ-02 → WGS84 往返精度高** | ✅ | 往返偏差 **0.0000 米** |
| 26 | 百度坐标（BD-09）可转 WGS84 | ✅ | BD09(87.6261, 43.8330) → WGS84(87.6168, 43.8256) |
| 27 | 坐标系名称支持中文 | ✅ | 「高德」被识别为 gcj02 |
| 28 | 非法经纬度被拒绝 | ✅ | 「经纬度必须是数字」 |
| 29 | 超范围经纬度被拒绝 | ✅ | 「经纬度超出有效范围」 |

> 转换算法在 `server/map/coord.js` 中实现，**纯本地计算、不联网、无依赖**。

### D. 推荐与在线核对（第 30–34 项）

| # | 验收项 | 结果 | 实测数据 |
|---|---|---|---|
| 30 | **按坐标推荐最近地州（辅助，不自动写入）** | ✅ | 乌鲁木齐(3.6km) → 昌吉(33km) → 五家渠(39km) |
| 31 | 距离数值计算正确 | ✅ | 服务端 3646 米，独立复算 3646 米 |
| 32 | 非法坐标不崩溃 | ✅ | 返回错误信息 |
| 33 | 生成在线核对链接（高德/百度） | ✅ | 两个平台链接均正确 |
| 34 | **链接中坐标已转成平台自身坐标系** | ✅ | 链接中不含原始 WGS84 值，避免点开偏位 |

### E. 筛选、变更与缓存（第 35–38 项）

| # | 验收项 | 结果 | 实测数据 |
|---|---|---|---|
| 35 | 客户列表返回归属地州字段 | ✅ | 4 条均含 region_code/region_name/经纬度，其中 3 条有归属 |
| 35b | **客户列表支持按归属地州筛选** | ✅ | 按乌鲁木齐筛出 2 家 |
| 36 | **修改地址后归属自动跟着变** | ✅ | 喀什 → 伊犁哈萨克自治州（654000） |
| 37 | **地址清空后归属同步清空** | ✅ | 不留错位归属 |
| 38 | 地图数据缓存可重载 | ✅ | 数据更新后无需重启 |

---

## 五、真实浏览器交互测试（21 项）

**本阶段最有价值的一项**：通过在 canvas 上派发**真实鼠标事件**点击地图区域，验证下钻链路。

| # | 验收项 | 结果 | 实测数据 |
|---|---|---|---|
| 1 | 首页渲染地图 canvas 且尺寸正常 | ✅ | 5 个 canvas；地图 979×520 显示区 |
| 2 | **地图真的画出了内容** | ✅ | 采样 canvas 像素：非白像素占比 **16%**（有边界色块） |
| 3 | 工具条与面包屑正常 | ✅ | 面包屑「新疆维吾尔自治区」 |
| 4 | 右侧地州排行有数据 | ✅ | |
| 5 | 排行按客户数排序，乌鲁木齐居首 | ✅ | 乌鲁木齐市:3 / 喀什地区:1 |
| 6 | **经纬度可换算成屏幕坐标** | ✅ | 乌鲁木齐在屏幕 (819, 437) |
| 7 | **canvas 真实点击触发下钻** | ✅ | 面包屑变为「新疆维吾尔自治区 / 乌鲁木齐市」，出现「返回全疆」 |
| 8 | **下钻后右侧展示该地州客户清单** | ✅ | 侧栏标题「乌鲁木齐市」，客户 3 条 |
| 9 | 可返回全疆视图 | ✅ | 面包屑复位 |
| 10 | 点排行条目也能下钻 | ✅ | 客户 3 条 |
| 11 | 地图支持缩放与拖拽 | ✅ | roam=true，zoom=1.15 |
| 12 | **地图可缩放（zoom 生效）** | ✅ | 1.15 → 2.2 |
| 13 | **坐标工具粘贴高德坐标自动转 WGS84** | ✅ | 偏移量 265 米，转换结果含 87.6168 |
| 14 | **显示偏移量（告知不转换会偏多少）** | ✅ | 「偏移量 265 米（未转换直接使用会导致地图上偏这么多）」 |
| 15 | 推荐最近地州 | ✅ | 3 个候选 |
| 16 | **点「填入」写回表单且是转换后的值** | ✅ | 经度 87.6168，纬度 43.8256 |
| 17 | 客户表单有「归属地州」下拉 | ✅ | 25 个选项（24 地州 + 空） |
| 18 | 客户列表筛选含「归属地州」 | ✅ | 12 个筛选下拉中含地州选项 |
| 19 | **全程零外部域名请求** | ✅ | 地图交互期间 5 个请求全部指向本地 |
| 20 | **边界数据已内嵌页面（可离线重绘）** | ✅ | 注册地图内含 24 个区域边界，离线重绘成功 |
| 21 | **全程无未捕获的 JS 错误** | ✅ | 0 条错误 |

---

## 六、本阶段发现并修复的缺陷

| # | 问题 | 严重度 | 根因与修复 |
|---|---|---|---|
| 1 | **首页 5 张图表只渲染出 2 张** | **高** | 图表组件用 `v-else` 在「空状态」与「canvas」之间切换，`empty` 变化时 canvas 元素被销毁又重建，`$refs.el` 失效导致 `echarts.init(undefined)` 报错。修复：canvas 常驻 DOM，空状态与错误提示改为浮层覆盖。这是阶段四引入的缺陷，当时测试没覆盖到「图表与空状态交替出现」的场景。 |
| 2 | **图表容器尺寸变化时不变形** | 中 | 缺少尺寸监听，卡片折叠、布局变化后图表被压成 100×100。修复：加 `ResizeObserver` + setOption 后主动 resize。 |
| 3 | 组件卸载时 ResizeObserver 回调报错 | 中 | 回调可能在 `dispose()` 之后触发，此时实例已释放。修复：加 `_disposing` 标记，卸载期间跳过 resize。 |
| 4 | **客户列表未返回 region_code 字段** | 中 | 后端列表 SELECT 未包含新字段，前端拿不到归属。修复：补入 SELECT 并加入筛选白名单。 |
| 5 | 索引在迁移前创建导致启动失败 | **高** | `region_code` 的索引放在 INDEXES 数组里，而建索引发生在迁移之前，旧库还没有该列 → `no such column`。修复：把该索引移到 v3 迁移脚本内部，并注释说明执行顺序约束。 |
| 6 | 坐标工具事件名与 `v-model` 冲突 | 中 | `apply()` 与抽屉开关都用 `update:modelValue`，会互相触发。修复：拆成独立的 `apply` 事件。 |
| 7 | 地图实例无法取到（影响测试与调试） | 中 | ECharts 6 无法用 `getInstanceByDom` 反查实例；Vue 生产构建也不把组件实例挂到 DOM（`__vueParentComponent` 取不到）。修复：新增 `CRM.charts` 实例注册表，图表组件主动登记，父组件与调试工具通过 `chartKey` 获取。 |

### 测试自身的问题（已修正，非产品缺陷）

| # | 问题 | 处理 |
|---|---|---|
| 1 | 边界坐标范围断言写窄了（写 73~96°E，实际含 96.39°E） | 按新疆实际范围 73.5~96.4°E / 34.3~49.2°N 修正 |
| 2 | 列表断言取第一条（按跟进时间排序，恰好是无归属客户） | 改为检查全部行含字段且统计有归属的条数 |
| 3 | 坐标工具取错抽屉（内层抽屉在 DOM 中排后） | 改用「最后一个 .drawer」定位 |
| 4 | 点击坐标超出视口（y=1682 > 1100） | 先 `scrollIntoView` 再计算坐标 |
| 5 | 用 CDP offline 模拟测地图离线（连 localhost 也拦了，属假失败） | 改为验证「零外部请求 + 边界数据内嵌」，这才是真正要保证的 |
| 6 | 结构版本、标签页数量等硬编码断言随迭代过期 | 改为从代码读取版本号 / 用 `>=` 灵活断言 |

---

## 七、全量回归（累计 351 项）

| 测试套件 | 项数 | 结果 |
|---|---|---|
| 阶段一：骨架与运行环境 | 23 | ✅ 23/23 |
| 阶段二：客户管理 API | 45 | ✅ 45/45 |
| 阶段二：前端数据契约 | 19 | ✅ 19/19 |
| 阶段二：浏览器交互 | 11 | ✅ 11/11 |
| 阶段三：项目/回款/待办 API | 43 | ✅ 43/43 |
| 阶段三：浏览器交互 | 11 | ✅ 11/11 |
| 阶段四：总览/设置/备份/导入导出 API | 40 | ✅ 40/40 |
| 阶段四：浏览器交互（含真实下载 Excel） | 15 | ✅ 15/15 |
| 阶段五：附件 API | 38 | ✅ 38/38 |
| 阶段五：浏览器交互（含真实拖拽上传） | 14 | ✅ 14/14 |
| **阶段六：地图 API** | **39** | ✅ 39/39 |
| **阶段六：浏览器交互（含 canvas 真实点击下钻）** | **21** | ✅ 21/21 |
| 数据库结构迁移 | 10 | ✅ 10/10 |
| **合计** | **351** | **✅ 351/351** |

---

## 八、数据库最终状态

| 项目 | 值 |
|---|---|
| 数据库文件 | `data/crm.db` |
| 结构版本 | **v3**（客户表新增 region_code / region_name，68 列） |
| 行政区划 | **121 条**（省 1、地州 24、县市 96） |
| 数据 | 客户 / 项目 / 回款 / 待办 / 附件 / 日志 均为 0 条（测试数据已清理） |
| 字典 | 22 类 / 237 项 |
| 地图数据 | `web/vendor/map/xinjiang/` 16 个文件，929 KB |

---

## 九、复现方式

```bat
:: 前置：服务运行中（双击 启动.bat）

:: 地图数据本地化（仅首次需要，联网执行一次）
node tools\fetch-map-data.js

node tools\test-phase6-api.js               :: 39 项 地图 API 测试
node tools\test-phase6-ui-interaction.js    :: 21 项 浏览器交互（含下钻与坐标工具）
node tools\cleanup-test-data.js --logs      :: 清理测试数据
```

---

## 十、使用说明

| 操作 | 方式 |
|---|---|
| 看客户分布 | 首页 → 「新疆客户分布」卡片；颜色越深客户越多 |
| 缩放平移 | 滚轮缩放、拖拽平移；也可用「重置视图」恢复 |
| 下钻到县市 | **点击地图上的地州**，或点右侧排行条目 |
| 看某区域客户 | 点击区域后，右侧列出该区域全部客户，点击可跳转详情 |
| 核对客户位置 | 客户清单里点「核对」，在在线地图上打开（按需联网） |
| 录入客户坐标 | 客户编辑 → 地址信息 → 「坐标工具」→ 粘贴高德/百度坐标 → 自动转换 → 填入 |
| 归属地州 | 填了「市/地区」保存后自动归属；也可在下拉里手动指定 |
| 按地州筛选客户 | 客户管理 → 高级筛选 → 归属地州 |
| 未归属客户 | 地图右侧会提示数量；补全地址后自动归入统计 |

### 坐标录入建议流程

1. 在客户编辑抽屉里点「坐标工具」
2. 点「拾取器」打开高德坐标拾取器（**仅此步需要联网**），搜索客户地址
3. 复制坐标粘回系统的输入框
4. 系统自动转成 WGS84 并显示偏移量
5. 点「在线核对转换结果」确认位置正确
6. 点「填入这两个坐标」

> **注意**：地州归属由「市/地区」字段自动匹配，**不需要**坐标。坐标只用于在地图上精确定位客户点，可选填。

---

## 十一、待办

| 项目 | 状态 |
|---|---|
| 工业园区标注 | **经确认不做**（技术方案已归档在 `新增功能_工业园区标注_技术方案.md`，将来若要做可直接参考） |
| 阶段七：全量验收测试（方案第 7 节 31 项）+ 修复回归 | ⏳ 最后一步 |
