# 阶段四验收测试报告

> 项目：客户管理系统（阀门行业版）
> 阶段：**阶段四 · 首页总览 / 设置 / 备份恢复 / 导入导出 / 回收站 / 操作日志**
> 测试日期：2026-02
> 测试方式：API 测试 40 项 + 真实浏览器交互测试 15 项（含真实文件下载与读回验证）
> 结论：**阶段四验收通过；全项目累计 237 项测试全部通过**

---

## 一、本阶段交付内容

| # | 交付物 | 说明 |
|---|---|---|
| 1 | 首页总览 `server/services/dashboard.js` + `web/js/pages/home.js` | 6 张数字卡、待跟进客户、回款计划、招投标日历、4 张 ECharts 图表、今日待办、最近动态 |
| 2 | 备份恢复服务 `server/services/backup.js` | SQLite 页级快照、主备份 + 镜像双份、轮转清理、完整性校验、恢复前自动兜底备份 |
| 3 | 导入导出服务 `server/services/excel.js` | 58 列客户模板、三类实体导入导出、逐行校验与错误报告、字典自动补全 |
| 4 | 系统路由 `server/routes/system.js` | 总览 / 设置 / 备份 / 导入导出 / 日志 共 16 个接口 |
| 5 | 图表组件 `web/js/components-chart.js` | ECharts 封装（创建/更新/自适应/销毁） |
| 6 | 字典管理 `web/js/settings-dict.js` | 22 类选项增删改、停用/启用、排序、改名（同步历史数据） |
| 7 | 备份面板 `web/js/settings-backup.js` | 状态、策略、备份列表、两步确认恢复 |
| 8 | 导入导出面板 `web/js/settings-data.js` | 模板下载、三步导入向导、问题清单导出 |
| 9 | 其他面板 `web/js/settings-misc.js` | 标签管理、提醒与偏好、回收站、操作日志 |
| 10 | 输入弹窗组件 | `CRM.prompt`（替代原生 `window.prompt`） |

---

## 二、API 测试明细（40 项）

### A. 首页总览（第 0–11 项）

| # | 验收项 | 结果 | 实测数据 |
|---|---|---|---|
| 0 | 准备完整业务数据 | ✅ | 客户/联系人/跟进/项目/回款/待办 |
| 1 | 总览接口返回全部数据块 | ✅ | 11 个数据块 |
| 2 | 数字卡数据正确 | ✅ | 客户数、本月新增、进行中项目、本月回款、今日待跟进、逾期数全部正确 |
| 3 | **欠款与逾期回款统计正确** | ✅ | 总欠款 = 合同 800,000 − 已收 100,000 = 700,000 |
| 4 | 待跟进客户含逾期天数与主联系人 | ✅ | 逾期 2 天，主联系人姓名与手机正确 |
| 5 | 逾期回款计划含未收余额 | ✅ | 计划 240,000 / 已收 100,000 / 未收 140,000 |
| 6 | 招投标日历含投标日期与预警级别 | ✅ | 按开标日排序 |
| 7 | 趋势图返回 6 个月签约与回款 | ✅ | 月份连续、金额正确 |
| 8 | 项目阶段分布含金额 | ✅ | |
| 9 | **下游行业成交额占比正确** | ✅ | 化工 800,000 |
| 10 | 客户转化漏斗 5 层有序 | ✅ | 建档→已跟进→有项目→已签约→已成交 |
| 11 | 今日待办与最近动态有数据 | ✅ | |

### B. 设置项（第 12–14 项）

| # | 验收项 | 结果 | 实测数据 |
|---|---|---|---|
| 12 | 设置接口返回全部键值与说明 | ✅ | 13 项 |
| 13 | 设置项可修改并持久化 | ✅ | |
| 14 | **未知设置键被忽略**（防写入垃圾数据） | ✅ | `updated` 为空数组 |

### C. 备份与恢复（第 15–24 项）

| # | 验收项 | 结果 | 实测数据 |
|---|---|---|---|
| 15 | 备份列表返回目录与保留策略 | ✅ | 主目录 / 镜像 / 30 / 7 |
| 16 | 手动备份创建成功 | ✅ | 476 KB |
| 17 | **备份文件落盘且清单非空** | ✅ | 数据库 476 KB，清单 478 字节 |
| 18 | **清单是完整可解析 JSON** | ✅ | 结构版本 v2，记录 14 张表的行数 |
| 19 | **镜像副本已生成**（第二份） | ✅ | 476 KB |
| 20 | 备份可校验（完整性 + 内容） | ✅ | `integrity_check = ok`，14 张表 |
| 21 | **备份内容与当前数据一致** | ✅ | 可抽查到具体记录与金额 |
| 22 | 非法备份名与目录穿越被拒绝 | ✅ | 目录穿越 → HTTP 400 |
| 23 | 最新一份备份不允许删除 | ✅ | `KEEP_LATEST` 保护 |
| 24 | 恢复第一步只校验不写数据 | ✅ | 返回预览：将替换的客户/项目/待办数 |

### D. 导入导出（第 25–35 项）

| # | 验收项 | 结果 | 实测数据 |
|---|---|---|---|
| 25 | 模板返回字段定义与示例 | ✅ | 客户 58 列，必填 2 列 |
| 26 | 导出客户数据 | ✅ | 2 条 × 58 列 |
| 27 | 导出项目与联系人数据 | ✅ | |
| 28 | 导入预校验识别有效/错误行 | ✅ | 5 行 → 有效 2 / 无效 3 |
| 29 | **校验细节全部正确** | ✅ | 见下方说明 |
| 30 | 执行导入只导入有效行 | ✅ | 导入 2 条，未通过 3 条，失败 0 |
| 31 | 导入数据真实入库 | ✅ | 字段值正确 |
| 32 | **字典外值自动补进字典** | ✅ | 「测试新行业XYZ」自动入字典 |
| 33 | 导入项目成功 | ✅ | 客户存在性校验通过 |
| 34 | 导入项目时客户不存在会报错 | ✅ | 「找不到客户…」 |
| 35 | 单次导入行数上限保护 | ✅ | 超 5000 行 → HTTP 400 |

**第 29 项细节**（这是导入功能最核心的价值，逐条验证）：
- 数字列 `'1200'` → `1200`（正确转型）
- 是/否列 `'是'` → `1`
- 日期 `'2010/5/20'` → `'2010-05-20'`（斜杠格式归一）
- 非法数字 `'abc'` → `0`（不报错、不写脏数据）
- 字典外值 → 给出提示但不阻止导入
- 必填缺失 → 明确报错
- 文件内重复 → 指出「文件内第 N 行已存在同名」
- 库内重复 → 指出「库中已存在同名客户」

### E. 操作日志与回收站（第 36–39 项）

| # | 验收项 | 结果 | 实测数据 |
|---|---|---|---|
| 36 | 日志列表带中文标签与统计 | ✅ | 按对象类型/操作类型聚合计数 |
| 37 | 日志可按对象类型筛选 | ✅ | |
| 38 | 日志可关键词搜索 | ✅ | |
| 39 | 回收站支持客户/项目/待办三类 | ✅ | |

---

## 三、真实浏览器交互测试（15 项）

通过 Chrome DevTools Protocol 驱动 **Chrome 152**，包含**真实文件下载与读回校验**。

| # | 验收项 | 结果 | 实测数据 |
|---|---|---|---|
| 1 | 首页渲染 6 张数字卡 | ✅ | 客户总数 / 进行中项目 / 本月回款 / 今日待跟进 / 逾期回款 / 未收欠款 |
| 2 | **4 张 ECharts 图表真实渲染** | ✅ | 4 个 canvas 均有实际尺寸（595×260 等），非空白 |
| 3 | 首页各数据块渲染 | ✅ | 列表行 16 条 + 招投标表格 |
| 4 | 首页无加载错误 | ✅ | |
| 5 | 设置页渲染 8 个标签页 | ✅ | 字典/标签/提醒/备份/导入导出/回收站/日志/关于 |
| 6 | 7 个设置标签页均可切换渲染 | ✅ | 逐个点击验证 |
| 7 | **字典管理新增选项立即生效** | ✅ | 选项数 24 → 25，提示「已新增」 |
| 8 | **新选项立即出现在客户表单下拉** | ✅ | 下拉 26 项，含新选项 |
| 9 | **真实下载 Excel 模板并落盘** | ✅ | 客户导入模板.xlsx（26 KB） |
| 10 | 模板是合法 xlsx（zip 头 PK） | ✅ | 文件头校验 |
| 11 | **真实导出数据并落盘** | ✅ | 客户数据_20260913.xlsx（23 KB） |
| 12 | **导出的 Excel 可读回且内容正确** | ✅ | 用 SheetJS 解析：**表头 58 列**（含客户全称/简称/下游行业/供应商编码），数据行含本次测试客户 |
| 13 | 备份面板渲染完整 | ✅ | 状态 + 策略 + 列表 + 自动备份开关 |
| 14 | 回收站与操作日志面板渲染 | ✅ | 回收站 3 分类，日志 50 行 + 3 个筛选下拉 |
| 15 | **全程无未捕获的 JS 错误** | ✅ | 0 条错误 |

> 第 12 项是真正的端到端闭环：浏览器生成 xlsx → 下载到磁盘 → Node 侧用 SheetJS 读回 → 校验表头文字与数据行内容。这证明了「导出的文件用 Excel 打开中文不乱码、列名正确」这条验收标准。

---

## 四、本阶段发现并修复的缺陷

| # | 问题 | 严重度 | 根因与修复 |
|---|---|---|---|
| 1 | **用户手动设置的「下次跟进时间」会被清空** | **高** | `syncCustomerFollowStats()` 无条件用跟进记录派生值覆盖客户字段。后果：只要在客户上手动设了下次跟进日期，之后**记一条不带下次时间的跟进就会把它清空**，导致首页「今日待跟进」与列表逾期标红全部失效。修复：改为 `COALESCE(派生值, 现值)`——**有值才覆盖**，保留用户手动设置。 |
| 2 | **`backup` 用法错误（备份会直接崩）** | **高** | 我按实例方法 `db.backup(path)` 编写，实测 `node:sqlite` 的备份是**模块级函数** `backup(db, destPath)`，实例上没有该方法。修复：`const { backup: sqliteBackup } = require('node:sqlite')`。 |
| 3 | 首页总览接口 500（`ambiguous column name: name`） | 高 | 多表 JOIN 下 `GROUP BY name` 与外层别名歧义；漏斗查询在相关子查询里引用外层别名。修复：GROUP BY 改用完整表达式；漏斗拆成独立查询。 |
| 4 | `backup.js` require 路径错误（服务无法启动） | 高 | 写成 `require('./db')`，实际应为 `require('../db')`。修复后补了「全项目 require 路径可解析 + 模块可加载」的自动校验。 |
| 5 | 备份目录遗留 `-wal` / `-shm` 文件 | 低 | 校验备份时会打开该库，产生 WAL 边车文件（不影响有效性）。已清理，并确认不是数据损坏。 |

### 特别说明：一次差点误判的排查

`Get-ChildItem` 显示备份清单为 **0 KB**，我一度以为是清单写入失败。实际用 Node 按 UTF-8 读取后确认文件为 **464 字节、JSON 完整可解析**——是 PowerShell 把小于 0.5 KB 的文件四舍五入显示成了 0 KB。同理，控制台里清单的 `reason` 字段显示为乱码，也是 PowerShell 控制台编码问题，文件本身是正常 UTF-8。

**但这次排查也暴露了测试的漏洞**：原来第 17 项只检查清单文件「存在」，没检查「内容非空且可解析」。已补强为校验字节数 > 50 且能 `JSON.parse` 成功、含时间/版本/各表行数。**这类"文件存在但内容为空"的问题，只有断言内容才能真正拦住。**

---

## 五、全量回归（累计 237 项）

| 测试套件 | 项数 | 结果 |
|---|---|---|
| 阶段一：骨架与运行环境 | 23 | ✅ 23/23 |
| 阶段二：客户管理 API | 45 | ✅ 45/45 |
| 阶段二：前端数据契约 | 19 | ✅ 19/19 |
| 阶段二：浏览器交互 | 11 | ✅ 11/11 |
| 阶段三：项目/回款/待办 API | 43 | ✅ 43/43 |
| 阶段三：浏览器交互 | 11 | ✅ 11/11 |
| **阶段四：总览/设置/备份/导入导出 API** | **40** | ✅ 40/40 |
| **阶段四：浏览器交互（含真实文件下载）** | **15** | ✅ 15/15 |
| 数据库结构迁移 | 10 | ✅ 10/10 |
| **合计** | **237** | **✅ 237/237** |

---

## 六、数据库最终状态（交付基线）

| 项目 | 值 |
|---|---|
| 数据库文件 | `data/crm.db` |
| 结构版本 | **v2**（13 业务表 + schema_version，36 个索引） |
| 字典 | 22 类 / 237 项 |
| 客户 / 项目 / 回款 / 待办 / 日志 | 0 条（测试数据已清理，孤儿数据为 0） |
| 备份 | 4 份（含 1 份迁移前备份），主目录与镜像目录均有 |

---

## 七、复现方式

```bat
:: 前置：服务运行中（双击 启动.bat）

node tools\test-phase4-api.js               :: 40 项 API 测试
node tools\test-phase4-ui-interaction.js    :: 15 项 浏览器交互（含真实下载）
node tools\cleanup-test-data.js --logs      :: 清理测试数据
```

---

## 八、与方案的对应关系

| 方案要求 | 实现情况 |
|---|---|
| 首页 4 张数字卡 | ✅ 扩展为 6 张（增加逾期回款数、未收欠款） |
| 今日 + 逾期待跟进客户列表 | ✅ 含天数、主联系人；可一键记跟进 |
| 临期 / 逾期回款计划 | ✅ 逾期红色标注，显示未收余额与逾期天数 |
| 招投标日历 | ✅ 按开标日排序；已过开标日未填结果标红 |
| 趋势图 / 阶段分布 / 行业占比 / 转化漏斗 | ✅ 4 张 ECharts 图表 |
| 今日待办 + 最近动态 | ✅ 可勾选完成 |
| 18 类字典增删改、停用、排序 | ✅ 22 类；**改名同步历史数据**；系统内置项只能停用 |
| 标签管理 | ✅ 含客户数统计、改名、改色 |
| 提醒规则 | ✅ 跟进/回款提前天数、生日提醒、每页条数 |
| 每日自动备份 + 双份副本 + 保留份数 | ✅ 主 30 份 / 镜像 7 份，可配置 |
| 立即备份、从备份恢复 | ✅ 恢复两步确认 + 恢复前自动兜底备份 |
| 模板下载、导入（预览/查重/错误报告） | ✅ 58 列模板含填写说明页；三步向导 |
| 全量导出 xlsx | ✅ 浏览器生成，中文表头 |
| 回收站（还原） | ✅ 客户/项目/跟进三类 |
| 操作日志（检索 + 导出） | ✅ 按对象/操作/日期/关键词筛选，可导出 Excel |
| **附件上传与预览** | ⏳ 阶段五 |
| **新疆地图** | ⏳ 阶段六 |
| **工业园区标注** | ⏳ 待阶段六时确认是否开发 |
